# NPLogic 배포 가이드

> **마지막 업데이트**: 2026-01-21  
> **대상**: 주니어 개발자 / 신규 인수인계 팀  
> **플랫폼**: Windows 10/11 (64-bit)

---

## 📋 목차

1. [개요](#개요)
2. [빌드 환경 요구사항](#1-빌드-환경-요구사항)
3. [개발 빌드 (Debug)](#2-개발-빌드-debug)
4. [배포 빌드 (Release)](#3-배포-빌드-release)
5. [Self-contained 배포](#4-self-contained-배포)
6. [설정 파일 구성](#5-설정-파일-구성)
7. [Python 환경 배포](#6-python-환경-배포)
8. [설치 패키지 생성](#7-설치-패키지-생성)
9. [업데이트 배포](#8-업데이트-배포)
10. [체크리스트](#체크리스트)

---

## 개요

### 배포 방식

NPLogic은 **Self-contained 배포**를 사용합니다:

| 배포 방식 | 장점 | 단점 |
|----------|------|------|
| **Framework-dependent** | 작은 파일 크기 | 사용자 PC에 .NET 설치 필요 |
| **Self-contained** ⭐ | .NET 없이 실행 가능 | 파일 크기 큼 (~150MB) |

### 배포 아키텍처

```
배포 패키지
├── NPLogic.App.exe          # 메인 실행 파일
├── NPLogic.*.dll            # 프로젝트 DLL들
├── *.dll                    # .NET 런타임 + 종속성
├── appsettings.json         # 설정 파일 ⚠️ API 키 포함
├── Assets/
│   └── Maps/*.html          # 지도 HTML
├── Resources/
│   └── mapping_template.json
├── backend/                 # Python OCR 서버 (선택)
│   └── registry_ocr/
└── logs/                    # 실행 시 생성
```

---

## 1. 빌드 환경 요구사항

### 필수 소프트웨어

| 소프트웨어 | 버전 | 다운로드 |
|-----------|------|----------|
| **.NET SDK** | 10.0 이상 | [dotnet.microsoft.com](https://dotnet.microsoft.com/download) |
| **Visual Studio 2022** | 17.0+ | [visualstudio.microsoft.com](https://visualstudio.microsoft.com/) |
| **Git** | 최신 | [git-scm.com](https://git-scm.com/) |

### Visual Studio 워크로드

Visual Studio Installer에서 설치:
- ✅ .NET 데스크톱 개발
- ✅ .NET 8.0 런타임 (또는 10.0)

### 버전 확인

```bash
# .NET SDK 버전 확인
dotnet --version
# 출력: 10.0.xxx

# Git 버전 확인
git --version
```

---

## 2. 개발 빌드 (Debug)

### 2.1 Visual Studio에서

1. `NPLogic.sln` 열기
2. 솔루션 구성을 **Debug**로 설정
3. **F5** (디버그 실행) 또는 **Ctrl+F5** (실행만)

### 2.2 명령줄에서

```bash
# 프로젝트 루트에서
cd c:\Users\pwm89\dev\nplogic

# 솔루션 복원 (처음 한 번)
dotnet restore NPLogic.sln

# Debug 빌드
dotnet build NPLogic.sln --configuration Debug

# 실행
dotnet run --project src/NPLogic.App/NPLogic.App.csproj
```

### 2.3 출력 경로

Debug 빌드 결과물:
```
src/NPLogic.App/bin/Debug/net10.0-windows/win-x64/
```

---

## 3. 배포 빌드 (Release)

### 3.1 Visual Studio에서

1. 솔루션 구성을 **Release**로 변경
2. **빌드 > 솔루션 빌드** (Ctrl+Shift+B)

### 3.2 명령줄에서

```bash
# Release 빌드
dotnet build NPLogic.sln --configuration Release
```

### 3.3 출력 경로

Release 빌드 결과물:
```
src/NPLogic.App/bin/Release/net10.0-windows/win-x64/
```

---

## 4. Self-contained 배포

### 4.1 Publish 명령

**Self-contained** 배포는 .NET 런타임을 함께 포함하여 사용자 PC에 .NET이 설치되어 있지 않아도 실행 가능합니다.

```bash
# 기본 Publish
dotnet publish src/NPLogic.App/NPLogic.App.csproj \
  --configuration Release \
  --runtime win-x64 \
  --self-contained true \
  --output ./publish
```

### 4.2 최적화 옵션

```bash
# 최적화된 Publish (ReadyToRun 활성화)
dotnet publish src/NPLogic.App/NPLogic.App.csproj \
  --configuration Release \
  --runtime win-x64 \
  --self-contained true \
  -p:PublishReadyToRun=true \
  --output ./publish
```

### 4.3 현재 프로젝트 설정

`NPLogic.App.csproj`에 이미 설정되어 있습니다:

```xml
<PropertyGroup>
  <!-- Self-contained 배포 설정 -->
  <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  <SelfContained>true</SelfContained>
  <PublishSingleFile>false</PublishSingleFile>
  <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
  
  <!-- Publish 최적화 -->
  <PublishReadyToRun>true</PublishReadyToRun>
  <PublishTrimmed>false</PublishTrimmed>
</PropertyGroup>
```

### 4.4 Publish 결과물

```
publish/
├── NPLogic.App.exe              # 실행 파일 (~1MB)
├── NPLogic.App.dll              # 메인 어셈블리
├── NPLogic.Core.dll             # Core 프로젝트
├── NPLogic.Data.dll             # Data 프로젝트
├── NPLogic.UI.dll               # UI 프로젝트
├── appsettings.json             # ⚠️ 설정 파일
├── Microsoft.*.dll              # .NET 런타임 DLL들
├── System.*.dll                 # 시스템 DLL들
├── Supabase.dll                 # Supabase 클라이언트
├── MaterialDesign*.dll          # UI 라이브러리
├── CommunityToolkit.Mvvm.dll    # MVVM 툴킷
├── ClosedXML.dll                # Excel 처리
├── Serilog*.dll                 # 로깅
├── WebView2Loader.dll           # WebView2
├── Assets/
│   └── Maps/
│       └── *.html               # 지도 HTML 파일들
├── Resources/
│   └── mapping_template.json    # 매핑 템플릿
└── runtimes/
    └── win-x64/
        └── native/              # 네이티브 DLL들
```

---

## 5. 설정 파일 구성

### 5.1 appsettings.json 구조

```json
{
  "Supabase": {
    "Url": "https://your-project-id.supabase.co",
    "Key": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning"
    }
  },
  "Python": {
    "ExecutablePath": "python",
    "OcrScriptPath": "python/ocr_processor.py",
    "RecommendScriptPath": "python/recommend_processor.py"
  },
  "App": {
    "DefaultPageSize": 50,
    "MaxFileUploadMB": 100
  }
}
```

### 5.2 환경별 설정

#### 개발 환경

```json
{
  "Supabase": {
    "Url": "https://dev-project.supabase.co",
    "Key": "dev-anon-key"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    }
  }
}
```

#### 운영 환경

```json
{
  "Supabase": {
    "Url": "https://prod-project.supabase.co",
    "Key": "prod-anon-key"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  }
}
```

### 5.3 보안 주의사항

> ⚠️ **중요**: `appsettings.json`에는 Supabase API 키가 포함됩니다.
> - Git에 커밋하지 마세요 (`.gitignore`에 포함됨)
> - 배포 시 올바른 운영 키로 교체하세요
> - `service_role` 키는 절대 포함하지 마세요 (anon key만 사용)

---

## 6. Python 환경 배포

### 6.1 Python 요구사항

| 패키지 | 버전 | 용도 |
|--------|------|------|
| Python | 3.10+ | 런타임 |
| pytesseract | 최신 | OCR 엔진 인터페이스 |
| Pillow | 최신 | 이미지 처리 |
| pdf2image | 최신 | PDF → 이미지 변환 |
| pandas | 최신 | 데이터 처리 |
| supabase | 최신 | DB 연동 (추천용) |

### 6.2 방법 A: 사용자 PC에 Python 설치 (권장하지 않음)

사용자가 직접 Python을 설치해야 하므로 불편합니다.

### 6.3 방법 B: 내장 Python 배포 (권장)

#### PyInstaller로 실행 파일 생성

```bash
cd python

# PyInstaller 설치
pip install pyinstaller

# OCR 프로세서 빌드
pyinstaller --onefile --name ocr_processor ocr_processor.py

# 추천 프로세서 빌드
pyinstaller --onefile --name recommend_processor recommend_processor.py
```

#### 결과물 배치

```
publish/
├── NPLogic.App.exe
├── python/
│   ├── ocr_processor.exe      # Python 없이 실행 가능
│   └── recommend_processor.exe
└── ...
```

### 6.4 방법 C: OCR 서버 (현재 방식)

프로젝트에 OCR 서버가 이미 빌드되어 있다면:

```
publish/
├── backend/
│   └── registry_ocr/
│       └── registry_ocr.exe   # OCR 서버 실행 파일
└── ...
```

### 6.5 Tesseract OCR 배포

Tesseract OCR도 함께 배포해야 합니다:

```
publish/
├── Tesseract-OCR/
│   ├── tesseract.exe
│   ├── tessdata/
│   │   └── kor.traineddata   # 한글 학습 데이터
│   └── ...
└── ...
```

또는 설치 프로그램에서 자동 설치되도록 구성.

---

## 7. 설치 패키지 생성

### 7.1 Inno Setup 사용 (권장)

[Inno Setup](https://jrsoftware.org/isinfo.php)은 무료 설치 프로그램 생성 도구입니다.

#### 설치 스크립트 예시 (nplogic.iss)

```iss
[Setup]
AppName=NPLogic
AppVersion=1.0.0
DefaultDirName={autopf}\NPLogic
DefaultGroupName=NPLogic
OutputDir=installer
OutputBaseFilename=NPLogic_Setup_1.0.0
Compression=lzma2
SolidCompression=yes
ArchitecturesInstallIn64BitMode=x64

[Files]
; 메인 애플리케이션
Source: "publish\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs

; Python 실행 파일 (옵션)
Source: "python\dist\*.exe"; DestDir: "{app}\python"; Flags: ignoreversion

; Tesseract OCR (옵션)
Source: "Tesseract-OCR\*"; DestDir: "{app}\Tesseract-OCR"; Flags: ignoreversion recursesubdirs

[Icons]
Name: "{group}\NPLogic"; Filename: "{app}\NPLogic.App.exe"
Name: "{autodesktop}\NPLogic"; Filename: "{app}\NPLogic.App.exe"

[Run]
; 설치 후 실행 옵션
Filename: "{app}\NPLogic.App.exe"; Description: "NPLogic 실행"; Flags: postinstall skipifsilent nowait

[Registry]
; WebView2 사용 시 필요할 수 있음
Root: HKLM; Subkey: "SOFTWARE\Microsoft\EdgeUpdate\ClientState\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}"; Flags: uninsdeletekeyifempty
```

#### 빌드

```bash
# Inno Setup Compiler 실행
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" nplogic.iss
```

### 7.2 MSIX 패키지 (Microsoft Store용)

Visual Studio의 **Windows Application Packaging Project** 사용:

1. 솔루션에 새 프로젝트 추가
2. **Windows Application Packaging Project** 선택
3. NPLogic.App을 Applications에 추가
4. 패키지 매니페스트 설정
5. **프로젝트 > 패키지 만들기**

### 7.3 ZIP 배포 (간단)

```bash
# PowerShell
Compress-Archive -Path .\publish\* -DestinationPath NPLogic_v1.0.0.zip
```

---

## 8. 업데이트 배포

### 8.1 수동 업데이트

1. 새 버전 빌드 및 Publish
2. 설치 프로그램 또는 ZIP 배포
3. 사용자가 다운로드 후 덮어쓰기 설치

### 8.2 자동 업데이트 구현 (선택)

```csharp
public class UpdateService
{
    private const string UpdateCheckUrl = "https://api.example.com/nplogic/version";
    
    public async Task<bool> CheckForUpdatesAsync()
    {
        using var client = new HttpClient();
        var response = await client.GetStringAsync(UpdateCheckUrl);
        var latestVersion = JsonSerializer.Deserialize<VersionInfo>(response);
        
        var currentVersion = Assembly.GetExecutingAssembly().GetName().Version;
        
        return latestVersion.Version > currentVersion;
    }
    
    public async Task DownloadAndInstallUpdateAsync(string downloadUrl)
    {
        // 1. 임시 폴더에 다운로드
        // 2. 현재 앱 종료
        // 3. 업데이터 실행 (별도 exe)
        // 4. 파일 교체 후 재시작
    }
}
```

### 8.3 버전 관리

`NPLogic.App.csproj`에 버전 추가:

```xml
<PropertyGroup>
  <Version>1.0.0</Version>
  <AssemblyVersion>1.0.0.0</AssemblyVersion>
  <FileVersion>1.0.0.0</FileVersion>
</PropertyGroup>
```

---

## 체크리스트

### 배포 전 체크리스트

- [ ] Release 모드로 빌드했는가?
- [ ] 모든 테스트 통과했는가?
- [ ] `appsettings.json`에 운영 Supabase 키가 설정되었는가?
- [ ] `appsettings.json`에 `service_role` 키가 없는가?
- [ ] 버전 번호가 업데이트되었는가?
- [ ] Python 종속성이 포함되었는가? (필요시)
- [ ] Tesseract OCR이 포함되었는가? (필요시)
- [ ] 지도 HTML 파일이 포함되었는가?
- [ ] 매핑 템플릿이 포함되었는가?

### 배포 후 체크리스트

- [ ] 설치 프로그램이 정상 실행되는가?
- [ ] 앱이 정상 시작되는가?
- [ ] 로그인이 되는가?
- [ ] 데이터 조회가 되는가?
- [ ] OCR 기능이 동작하는가? (필요시)
- [ ] 추천 기능이 동작하는가? (필요시)
- [ ] 엑셀 내보내기가 되는가?
- [ ] 로그 파일이 생성되는가?

### 트러블슈팅

#### "VCRUNTIME140.dll이 없습니다" 오류

- Visual C++ 재배포 패키지 설치 필요
- 설치 프로그램에 포함하거나 사전 설치 안내

#### "WebView2 런타임이 없습니다" 오류

- Microsoft Edge WebView2 Runtime 설치 필요
- [다운로드 링크](https://developer.microsoft.com/microsoft-edge/webview2/)

#### 앱 시작 시 충돌

1. `logs/` 폴더의 로그 파일 확인
2. `appsettings.json` 파일 존재 확인
3. Supabase URL/Key 올바른지 확인

---

## 📁 최종 배포 구조 예시

```
NPLogic_v1.0.0/
├── NPLogic.App.exe              # 메인 실행 파일
├── appsettings.json             # 설정 (운영용)
├── *.dll                        # 모든 종속성
├── Assets/
│   └── Maps/
│       ├── cadastral_map.html   # 지적도
│       ├── roadview.html        # 로드뷰
│       └── aerial_map.html      # 위성지도
├── Resources/
│   └── mapping_template.json
├── python/                      # Python 실행 파일
│   ├── ocr_processor.exe
│   └── recommend_processor.exe
├── Tesseract-OCR/               # OCR 엔진
│   ├── tesseract.exe
│   └── tessdata/
│       └── kor.traineddata
├── runtimes/
│   └── win-x64/
│       └── native/
└── logs/                        # 실행 시 생성됨
```

---

## 🔗 관련 문서

- [ENVIRONMENT_SETUP.md](./ENVIRONMENT_SETUP.md) - 개발 환경 설정
- [PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md) - 프로젝트 구조
- [SERVICE_GUIDE.md](../api/SERVICE_GUIDE.md) - 서비스 가이드

---

**문서 작성일**: 2026-01-21  
**작성자**: NPLogic 개발팀
