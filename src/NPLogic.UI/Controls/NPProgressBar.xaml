<UserControl x:Class="NPLogic.UI.Controls.NPProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="24" d:DesignWidth="300">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Label -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{StaticResource SmallTextStyle}"
                   Margin="0,0,0,4"
                   Visibility="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource NullToVisibilityConverter}}"/>

        <!-- Progress Bar Container -->
        <Border Grid.Row="1"
                Background="{StaticResource BackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Height="8">
            <Grid ClipToBounds="True">
                <Border x:Name="ProgressFill"
                        Background="{StaticResource PrimaryBrush}"
                        HorizontalAlignment="Left"
                        CornerRadius="3"
                        Width="0"/>
            </Grid>
        </Border>

        <!-- Percentage Text -->
        <TextBlock Grid.Row="2"
                   x:Name="PercentageText"
                   Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0:0}%}"
                   Style="{StaticResource SmallTextStyle}"
                   HorizontalAlignment="Right"
                   Margin="0,4,0,0"
                   Visibility="{Binding ShowPercentage, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
    
</UserControl>

