<UserControl x:Class="NPLogic.Controls.NPToast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="400"
             Loaded="UserControl_Loaded">
    
    <Border x:Name="ToastBorder"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16"
            MinWidth="300"
            MaxWidth="500"
            Opacity="0">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                            Opacity="0.2" 
                            BlurRadius="10" 
                            ShadowDepth="2"/>
        </Border.Effect>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <Border Grid.Column="0"
                    x:Name="IconBorder"
                    Width="40"
                    Height="40"
                    CornerRadius="20"
                    Background="{StaticResource InfoBgBrush}"
                    Margin="0,0,12,0">
                <Path x:Name="IconPath"
                      Width="20"
                      Height="20"
                      Fill="{StaticResource InfoBrush}"
                      Stretch="Uniform"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
            </Border>

            <!-- Message -->
            <StackPanel Grid.Column="1" 
                       VerticalAlignment="Center">
                <TextBlock x:Name="TitleText"
                          Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Style="{StaticResource BodyTextStyle}"
                          FontWeight="{StaticResource FontWeightSemiBold}"
                          Margin="0,0,0,4"
                          Visibility="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource NullToVisibilityConverter}}"/>
                
                <TextBlock x:Name="MessageText"
                          Text="{Binding Message, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Style="{StaticResource SmallTextStyle}"
                          TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Column="2"
                    Style="{StaticResource IconButton}"
                    Width="24"
                    Height="24"
                    Margin="12,0,0,0"
                    VerticalAlignment="Top"
                    Click="CloseButton_Click"
                    Visibility="{Binding ShowCloseButton, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z"
                      Fill="{StaticResource TextSecondaryBrush}"
                      Width="12"
                      Height="12"
                      Stretch="Uniform"/>
            </Button>
        </Grid>
    </Border>
    
</UserControl>

