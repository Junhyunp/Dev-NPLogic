<UserControl x:Class="NPLogic.Controls.NPDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search and Filter Bar (Optional) -->
        <Grid Grid.Row="0" 
              Margin="0,0,0,16"
              Visibility="{Binding ShowSearchBar, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox"
                     Grid.Column="0"
                     Style="{StaticResource NPTextBox}"
                     Width="300"
                     HorizontalAlignment="Left"
                     TextChanged="SearchBox_TextChanged"/>

            <StackPanel Grid.Column="1" 
                       Orientation="Horizontal"
                       HorizontalAlignment="Right">
                <ContentPresenter Content="{Binding FilterContent, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </StackPanel>
        </Grid>

        <!-- DataGrid -->
        <DataGrid x:Name="InnerDataGrid"
                  Grid.Row="1"
                  Style="{StaticResource NPDataGrid}"
                  ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                  AutoGenerateColumns="{Binding AutoGenerateColumns, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  CanUserAddRows="{Binding CanUserAddRows, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  CanUserDeleteRows="{Binding CanUserDeleteRows, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectionMode="{Binding SelectionMode, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  ColumnHeaderStyle="{StaticResource NPDataGridColumnHeader}"
                  RowStyle="{StaticResource NPDataGridRow}"
                  CellStyle="{StaticResource NPDataGridCell}"/>
    </Grid>
    
</UserControl>

