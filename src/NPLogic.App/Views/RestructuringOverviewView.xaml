<UserControl x:Class="NPLogic.Views.RestructuringOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1400"
             d:DataContext="{d:DesignInstance Type=vm:RestructuringOverviewViewModel}"
             Loaded="RestructuringOverviewView_Loaded">
    
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="회생 개요" Style="{StaticResource H2TextStyle}" Margin="0,0,0,8"/>
                <TextBlock Text="회생 절차 중인 차주 현황 및 관리" Style="{StaticResource BodyTextStyle}" Opacity="0.7"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Excel 내보내기" Command="{Binding ExportToExcelCommand}"
                        Style="{StaticResource SecondaryButton}" Padding="20,10" Margin="0,0,8,0"/>
                <Button Content="새로고침" Command="{Binding RefreshCommand}"
                        Style="{StaticResource PrimaryButton}" Padding="20,10"/>
            </StackPanel>
        </Grid>

        <!-- 통계 카드 -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#E57373" CornerRadius="12" Padding="24">
                <StackPanel>
                    <TextBlock Text="회생 차주 수" FontSize="14" Foreground="White" Opacity="0.9"/>
                    <TextBlock FontSize="36" FontWeight="Bold" Foreground="White" Margin="0,8,0,0">
                        <Run Text="{Binding TotalRestructuringCount}"/>
                        <Run Text="건" FontSize="16" FontWeight="Normal"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="24">
                <StackPanel>
                    <TextBlock Text="회생 차주 총 OPB" FontSize="14" Opacity="0.7"/>
                    <TextBlock FontSize="28" FontWeight="Bold" Margin="0,8,0,0" Foreground="#E57373">
                        <Run Text="{Binding TotalRestructuringOpb, StringFormat=N0}"/>
                        <Run Text="원" FontSize="14" FontWeight="Normal"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 메인 컨텐츠 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 회생 차주 목록 -->
            <Border Grid.Column="0" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="회생 차주 목록" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,12"/>

                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding RestructuringBorrowers}" 
                             SelectedItem="{Binding SelectedBorrower}"
                             Background="Transparent" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,10" Margin="0,2">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                                            <Ellipse Width="8" Height="8" Fill="#E57373" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding BorrowerName}" FontWeight="SemiBold"/>
                                        </StackPanel>
                                        <TextBlock Grid.Row="1" Text="{Binding BorrowerNumber}" FontSize="12" Opacity="0.7" Margin="16,4,0,0"/>
                                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="16,4,0,0">
                                            <TextBlock Text="OPB: " FontSize="12" Opacity="0.7"/>
                                            <TextBlock Text="{Binding Opb, StringFormat=N0}" FontSize="12"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- 상세 정보 -->
            <Border Grid.Column="2" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedBorrower}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <!-- 차주 정보 -->
                        <TextBlock Text="차주 정보" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,16"/>
                        <Grid Margin="0,0,0,24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0">
                                <TextBlock Text="차주번호" FontSize="11" Opacity="0.6"/>
                                <TextBlock Text="{Binding SelectedBorrower.BorrowerNumber}" FontWeight="SemiBold" Margin="0,4,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1">
                                <TextBlock Text="차주명" FontSize="11" Opacity="0.6"/>
                                <TextBlock Text="{Binding SelectedBorrower.BorrowerName}" FontWeight="SemiBold" Margin="0,4,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="2">
                                <TextBlock Text="차주유형" FontSize="11" Opacity="0.6"/>
                                <TextBlock Text="{Binding SelectedBorrower.BorrowerType}" FontWeight="SemiBold" Margin="0,4,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0">
                                <TextBlock Text="물건 수" FontSize="11" Opacity="0.6"/>
                                <TextBlock FontWeight="SemiBold" Margin="0,4,0,0">
                                    <Run Text="{Binding SelectedBorrower.PropertyCount}"/>
                                    <Run Text="건"/>
                                </TextBlock>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="1">
                                <TextBlock Text="OPB" FontSize="11" Opacity="0.6"/>
                                <TextBlock FontWeight="SemiBold" Margin="0,4,0,0" Foreground="#E57373">
                                    <Run Text="{Binding SelectedBorrower.Opb, StringFormat=N0}"/>
                                    <Run Text="원"/>
                                </TextBlock>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="2">
                                <TextBlock Text="근저당설정액" FontSize="11" Opacity="0.6"/>
                                <TextBlock FontWeight="SemiBold" Margin="0,4,0,0">
                                    <Run Text="{Binding SelectedBorrower.MortgageAmount, StringFormat=N0}"/>
                                    <Run Text="원"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>

                        <!-- 상태 표시 -->
                        <TextBlock Text="회생 상태" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,12"/>
                        <WrapPanel Margin="0,0,0,24">
                            <Border Background="#E57373" CornerRadius="4" Padding="12,6" Margin="0,0,8,0">
                                <TextBlock Text="회생 진행 중" Foreground="White" FontWeight="SemiBold"/>
                            </Border>
                            <Border Background="{StaticResource BackgroundColor}" CornerRadius="4" Padding="12,6" Margin="0,0,8,0"
                                    Visibility="{Binding SelectedBorrower.IsOpened, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="개회" FontWeight="SemiBold"/>
                            </Border>
                        </WrapPanel>

                        <!-- 대출 정보 -->
                        <TextBlock FontWeight="SemiBold" FontSize="16" Margin="0,0,0,12">
                            <Run Text="채권 정보 ("/>
                            <Run Text="{Binding BorrowerLoans.Count, Mode=OneWay}"/>
                            <Run Text="건)"/>
                        </TextBlock>
                        <DataGrid ItemsSource="{Binding BorrowerLoans}"
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                                  BorderThickness="1" BorderBrush="{StaticResource BorderColor}"
                                  Background="Transparent" MaxHeight="250">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="계좌일련번호" Binding="{Binding AccountSerial}" Width="120"/>
                                <DataGridTextColumn Header="대출과목" Binding="{Binding LoanType}" Width="100"/>
                                <DataGridTextColumn Header="원금잔액" Binding="{Binding LoanPrincipalBalance, StringFormat=N0}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Loan Cap (1안)" Binding="{Binding LoanCap1, StringFormat=N0}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Foreground" Value="#64B5F6"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- 로딩 오버레이 -->
        <Border Grid.RowSpan="3" Background="#80000000" CornerRadius="8"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200"/>
                <TextBlock Text="로딩 중..." Foreground="White" Margin="0,16,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

