<UserControl x:Class="NPLogic.Views.EvaluatorHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:EvaluatorHomeViewModel}"
             Loaded="EvaluatorHomeView_Loaded">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 영역 -->
        <Border Grid.Row="0" 
                Background="{StaticResource SecondaryBrush}"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="{Binding DisplayName}" 
                               Foreground="White"
                               FontSize="20"
                               FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Border Background="#80FFFFFF" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                            <TextBlock Text="평가자" 
                                       Foreground="{StaticResource SecondaryBrush}"
                                       FontSize="12"
                                       FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Foreground="#B0FFFFFF" FontSize="12" VerticalAlignment="Center">
                            <Run Text="진행중인 프로그램:"/>
                            <Run Text="{Binding CurrentProgramName, FallbackValue='N/A'}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="새로고침" 
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Foreground="White"
                        Background="Transparent"
                        BorderBrush="#80FFFFFF"
                        Padding="16,8"/>
            </Grid>
        </Border>

        <!-- 통계 카드 -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 전체 -->
            <Border Grid.Column="0" 
                    Background="{StaticResource BackgroundBrush}"
                    CornerRadius="8"
                    Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="8" ShadowDepth="2"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="배정된 물건" 
                               Style="{StaticResource CaptionTextStyle}"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalPropertyCount}" 
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- 대기 -->
            <Border Grid.Column="2" 
                    Background="{StaticResource BackgroundBrush}"
                    CornerRadius="8"
                    Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="8" ShadowDepth="2"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="대기" 
                               Style="{StaticResource CaptionTextStyle}"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding PendingCount}" 
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{StaticResource WarningBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- 진행중 -->
            <Border Grid.Column="4" 
                    Background="{StaticResource BackgroundBrush}"
                    CornerRadius="8"
                    Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="8" ShadowDepth="2"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="진행중" 
                               Style="{StaticResource CaptionTextStyle}"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding ProcessingCount}" 
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{StaticResource InfoBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- 완료 -->
            <Border Grid.Column="6" 
                    Background="{StaticResource BackgroundBrush}"
                    CornerRadius="8"
                    Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="8" ShadowDepth="2"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="완료" 
                               Style="{StaticResource CaptionTextStyle}"
                               Opacity="0.7"/>
                    <TextBlock Text="{Binding CompletedCount}" 
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 나의 진행 상황 -->
        <Border Grid.Row="2" 
                Background="{StaticResource BackgroundBrush}"
                CornerRadius="8"
                Padding="16"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="나의 진행 상황:" 
                           VerticalAlignment="Center"
                           Style="{StaticResource BodyTextStyle}"
                           FontWeight="SemiBold"
                           Margin="0,0,16,0"/>
                
                <ProgressBar Grid.Column="1" 
                             Value="{Binding OverallProgressPercent}" 
                             Maximum="100"
                             Height="20"
                             Foreground="{StaticResource SuccessBrush}"/>
                
                <TextBlock Grid.Column="2" 
                           VerticalAlignment="Center"
                           Style="{StaticResource BodyTextStyle}"
                           FontWeight="Bold"
                           Margin="16,0,0,0">
                    <Run Text="{Binding OverallProgressPercent}"/>
                    <Run Text="% 완료 ("/>
                    <Run Text="{Binding CompletedCount}"/>
                    <Run Text="/"/>
                    <Run Text="{Binding TotalPropertyCount}"/>
                    <Run Text="건)"/>
                </TextBlock>
            </Grid>
        </Border>

        <!-- 물건 목록 -->
        <Border Grid.Row="3"
                Background="{StaticResource BackgroundBrush}"
                CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="8" ShadowDepth="2"/>
            </Border.Effect>

            <Grid>
                <!-- 로딩 표시 -->
                <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                      Panel.ZIndex="10">
                    <Border Background="#80FFFFFF"/>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                        <TextBlock Text="데이터 로드 중..." 
                                  Style="{StaticResource BodyTextStyle}"
                                  HorizontalAlignment="Center"
                                  Opacity="0.7"
                                  Margin="0,16,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- 데이터 그리드 -->
                <DataGrid x:Name="PropertiesDataGrid"
                          ItemsSource="{Binding Properties}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="All"
                          HorizontalGridLinesBrush="{StaticResource TableBorderBrush}"
                          VerticalGridLinesBrush="{StaticResource TableBorderBrush}"
                          HeadersVisibility="Column"
                          Background="Transparent"
                          BorderThickness="0"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                    
                    <DataGrid.Columns>
                        <!-- 차주번호 -->
                        <DataGridTextColumn Header="차주번호" 
                                          Binding="{Binding PropertyNumber}" 
                                          Width="100"
                                          IsReadOnly="True"/>
                        
                        <!-- 차주명 -->
                        <DataGridTextColumn Header="차주명" 
                                          Binding="{Binding DebtorName}" 
                                          Width="100"
                                          IsReadOnly="True"/>
                        
                        <!-- 담보번호 -->
                        <DataGridTextColumn Header="담보번호" 
                                          Binding="{Binding CollateralNumber}" 
                                          Width="100"
                                          IsReadOnly="True"/>
                        
                        <!-- 물건종류 -->
                        <DataGridTextColumn Header="물건종류" 
                                          Binding="{Binding PropertyType}" 
                                          Width="80"
                                          IsReadOnly="True"/>
                        
                        <!-- 감정확인 -->
                        <DataGridTemplateColumn Header="감정확인" Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding AppraisalConfirmed, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center"
                                              Click="ProgressCheckBox_Click"
                                              Tag="appraisal_confirmed"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <!-- 경매개시 -->
                        <DataGridTemplateColumn Header="경매개시" Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox SelectedItem="{Binding AuctionStart1, UpdateSourceTrigger=PropertyChanged}"
                                              SelectionChanged="AuctionStartComboBox_SelectionChanged"
                                              Tag="auction_start_1"
                                              Padding="2"
                                              BorderThickness="0">
                                        <ComboBoxItem Content=""/>
                                        <ComboBoxItem Content="개시"/>
                                        <ComboBoxItem Content="미개시"/>
                                    </ComboBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <!-- 선순위 -->
                        <DataGridTemplateColumn Header="선순위" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding SeniorRightsReview, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center"
                                              Click="ProgressCheckBox_Click"
                                              Tag="senior_rights_review"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <!-- 평가 -->
                        <DataGridTemplateColumn Header="평가" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding AppraisalConfirmed, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center"
                                              Click="ProgressCheckBox_Click"
                                              Tag="appraisal_confirmed"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <!-- 경매일정 -->
                        <DataGridTextColumn Header="경매일정" 
                                          Binding="{Binding AuctionScheduleDate, StringFormat=yyyy-MM-dd}" 
                                          Width="90"
                                          IsReadOnly="True"/>
                        
                        <!-- QA -->
                        <DataGridTemplateColumn Header="QA" Width="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource ErrorBrush}"
                                            CornerRadius="10"
                                            Padding="6,2"
                                            HorizontalAlignment="Center"
                                            Visibility="{Binding QaUnansweredCount, Converter={StaticResource IntToVisibilityConverter}}">
                                        <TextBlock Text="{Binding QaUnansweredCount}"
                                                   Foreground="White"
                                                   FontSize="11"
                                                   FontWeight="Bold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <!-- 권리분석 -->
                        <DataGridTemplateColumn Header="권리분석" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RightsAnalysisStatus}" Value="completed">
                                                        <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding RightsAnalysisStatus}" Value="processing">
                                                        <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="11" HorizontalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="대기"/>
                                                    <Setter Property="Foreground" Value="{StaticResource DeepNavy700Brush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RightsAnalysisStatus}" Value="completed">
                                                            <Setter Property="Text" Value="완료"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding RightsAnalysisStatus}" Value="processing">
                                                            <Setter Property="Text" Value="진행중"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <!-- 상세 -->
                        <DataGridTemplateColumn Header="작업" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="상세" 
                                           Command="{Binding DataContext.ViewPropertyDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           CommandParameter="{Binding}"
                                           Style="{StaticResource PrimaryButton}"
                                           Padding="8,2"
                                           FontSize="11"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BlueGray50Brush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                            <Setter Property="Foreground" Value="{StaticResource DeepNavy700Brush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="8,10"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BlueGray200Brush}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BlueGray100Brush}"/>
                            <Setter Property="Padding" Value="4"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{StaticResource DeepNavy700Brush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>

                <!-- 물건이 없을 때 -->
                <Grid Visibility="{Binding TotalPropertyCount, Converter={StaticResource ZeroToVisibilityConverter}}"
                      Background="#F8FAFC">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="FileDocumentOutline" Width="64" Height="64" Foreground="#CBD5E1"/>
                        <TextBlock Text="배정된 물건이 없습니다" 
                                   FontSize="14" 
                                   Foreground="#94A3B8"
                                   Margin="0,16,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>

