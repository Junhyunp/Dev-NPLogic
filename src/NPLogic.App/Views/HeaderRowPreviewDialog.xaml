<Window x:Class="NPLogic.Views.HeaderRowPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="제목행 확인"
        Height="500" Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinWidth="600" MinHeight="350"
        Background="{DynamicResource BackgroundBrush}">
    
    <Window.Resources>
        <!-- 로컬 색상 정의 -->
        <SolidColorBrush x:Key="LocalPrimaryBrush" Color="#1565C0"/>
        <SolidColorBrush x:Key="LocalSecondaryTextBrush" Color="#607D8B"/>
        <SolidColorBrush x:Key="LocalSuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="LocalInfoBrush" Color="#2196F3"/>
        <SolidColorBrush x:Key="LocalBorderBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="LocalSurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="LocalBlueGray50Brush" Color="#ECEFF1"/>
        <SolidColorBrush x:Key="LocalBlueGray100Brush" Color="#CFD8DC"/>
        <SolidColorBrush x:Key="LocalSelectedRowBrush" Color="#E3F2FD"/>
        
        <!-- 로컬 버튼 스타일 -->
        <Style x:Key="LocalPrimaryButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Background" Value="{StaticResource LocalPrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style x:Key="LocalSecondaryButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
            <Setter Property="BorderBrush" Value="{StaticResource LocalBorderBrush}"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 헤더 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock FontSize="{StaticResource FontSizeLarge}" 
                       FontWeight="Bold"
                       Foreground="{StaticResource LocalPrimaryBrush}">
                <Run Text="제목행 확인 -"/>
                <Run Text="{Binding SheetName, Mode=OneWay}"/>
            </TextBlock>
            <TextBlock Text="아래 미리보기에서 제목행(헤더)으로 사용할 행을 선택하세요" 
                       FontSize="{StaticResource FontSizeBody}"
                       Foreground="{StaticResource LocalSecondaryTextBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- 현재 선택 정보 -->
        <Border Grid.Row="1" 
                Background="{StaticResource LocalBlueGray50Brush}"
                CornerRadius="6"
                Padding="16,12"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="현재 제목행:" 
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <Border Background="{StaticResource LocalPrimaryBrush}"
                            CornerRadius="4"
                            Padding="12,4">
                        <TextBlock x:Name="CurrentHeaderRowText"
                                   Text="1행"
                                   Foreground="White"
                                   FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>
                
                <TextBlock Grid.Column="2"
                           Text="행을 클릭하여 제목행으로 선택"
                           FontSize="{StaticResource FontSizeBody}"
                           Foreground="{StaticResource LocalSecondaryTextBrush}"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
        
        <!-- 데이터 미리보기 -->
        <Border Grid.Row="2" 
                BorderBrush="{StaticResource LocalBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Background="{StaticResource LocalSurfaceBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- 테이블 헤더 -->
                <Border Grid.Row="0" 
                        Background="{StaticResource LocalBlueGray50Brush}"
                        CornerRadius="8,8,0,0"
                        Padding="16,10">
                    <Grid>
                        <TextBlock Text="시트 미리보기 (처음 10행)" FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}"/>
                        <TextBlock Text="{Binding TotalRowsText}" 
                                   HorizontalAlignment="Right"
                                   FontSize="{StaticResource FontSizeBody}"
                                   Foreground="{StaticResource LocalSecondaryTextBrush}"/>
                    </Grid>
                </Border>
                
                <!-- 데이터 그리드 -->
                <DataGrid x:Name="PreviewDataGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          GridLinesVisibility="All"
                          HorizontalGridLinesBrush="{StaticResource LocalBlueGray100Brush}"
                          VerticalGridLinesBrush="{StaticResource LocalBlueGray100Brush}"
                          HeadersVisibility="Column"
                          Background="Transparent"
                          BorderThickness="0"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          SelectionChanged="PreviewDataGrid_SelectionChanged">
                    
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource LocalBlueGray50Brush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource LocalSelectedRowBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource LocalBlueGray100Brush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="{StaticResource FontSizeSmall}"/>
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource LocalBlueGray100Brush}"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="{StaticResource LocalBlueGray100Brush}"/>
                            <Setter Property="Padding" Value="6"/>
                            <Setter Property="FontSize" Value="{StaticResource FontSizeSmall}"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Grid>
        </Border>
        
        <!-- 안내문 -->
        <TextBlock Grid.Row="3" 
                   Margin="0,12,0,16"
                   FontSize="{StaticResource FontSizeBody}"
                   Foreground="{StaticResource LocalSecondaryTextBrush}"
                   TextWrapping="Wrap">
            <Run Text="※ 선택한 행이 컬럼 헤더(제목행)로 사용됩니다."/>
            <LineBreak/>
            <Run Text="※ 일반적으로 '차주번호', '물건번호' 등의 컬럼명이 있는 행을 선택하세요."/>
        </TextBlock>
        
        <!-- 버튼 -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="취소" 
                    Click="Cancel_Click"
                    Style="{StaticResource LocalSecondaryButton}"
                    Padding="24,10"
                    Margin="0,0,8,0"/>
            <Button Content="확인" 
                    Click="Confirm_Click"
                    Style="{StaticResource LocalPrimaryButton}"
                    Padding="24,10"/>
        </StackPanel>
    </Grid>
</Window>
