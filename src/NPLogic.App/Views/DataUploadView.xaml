<UserControl x:Class="NPLogic.Views.DataUploadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1400"
             d:DataContext="{d:DesignInstance Type=vm:DataUploadViewModel}"
             AllowDrop="True"
             Drop="DataUploadView_Drop"
             DragOver="DataUploadView_DragOver">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 영역 -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="데이터 업로드" 
                           Style="{StaticResource H2TextStyle}"
                           Margin="0,0,0,4"/>
                <TextBlock Text="Excel 파일 또는 PDF 파일을 업로드하여 물건 데이터를 일괄 등록하세요" 
                           Style="{StaticResource BodyTextStyle}"
                           Opacity="0.7"/>
            </StackPanel>

            <!-- 탭 선택 버튼 -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <RadioButton x:Name="ExcelTabRadio"
                             GroupName="UploadType"
                             IsChecked="True"
                             Style="{StaticResource {x:Type ToggleButton}}"
                             Margin="0,0,8,0">
                    <RadioButton.Template>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="border"
                                    Background="{StaticResource BlueGray100Brush}"
                                    CornerRadius="6"
                                    Padding="16,8"
                                    Cursor="Hand">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="iconText" Text="📊" FontSize="{StaticResource FontSizeH4}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBlock x:Name="labelText" Text="Excel 업로드" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                    <Setter TargetName="iconText" Property="Foreground" Value="White"/>
                                    <Setter TargetName="labelText" Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </RadioButton.Template>
                </RadioButton>

                <RadioButton x:Name="PdfTabRadio"
                             GroupName="UploadType"
                             Style="{StaticResource {x:Type ToggleButton}}">
                    <RadioButton.Template>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="border"
                                    Background="{StaticResource BlueGray100Brush}"
                                    CornerRadius="6"
                                    Padding="16,8"
                                    Cursor="Hand">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="iconText" Text="📄" FontSize="{StaticResource FontSizeH4}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBlock x:Name="labelText" Text="PDF 업로드" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                    <Setter TargetName="iconText" Property="Foreground" Value="White"/>
                                    <Setter TargetName="labelText" Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </RadioButton.Template>
                </RadioButton>
            </StackPanel>
        </Grid>

        <!-- 메인 컨텐츠 (가로 2분할) -->
        <Grid Grid.Row="1">
            <!-- Excel 업로드 패널 -->
            <Grid Visibility="{Binding IsChecked, ElementName=ExcelTabRadio, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="400"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="1.2*" MinWidth="500"/>
                </Grid.ColumnDefinitions>

                <!-- 좌측: 파일 업로드 영역 -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 드롭존 -->
                    <Border Grid.Row="0"
                            Background="{StaticResource BackgroundBrush}"
                            BorderBrush="{StaticResource PrimaryBrush}"
                            BorderThickness="2"
                            CornerRadius="12"
                            AllowDrop="True"
                            Drop="ExcelDropZone_Drop"
                            DragOver="DropZone_DragOver">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
                        </Border.Effect>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="{StaticResource BlueGray300Brush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedExcelFile, Converter={StaticResource NullToBoolConverter}}" Value="False">
                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="Background" Value="{StaticResource BlueGray50Brush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>

                        <Grid>
                            <!-- 파일 미선택 상태 -->
                            <StackPanel HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Visibility="{Binding SelectedExcelFile, Converter={StaticResource NullToInverseVisibilityConverter}}">
                                <Border Background="{StaticResource BlueGray100Brush}"
                                        CornerRadius="50"
                                        Width="100" Height="100"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,24">
                                    <TextBlock Text="📁" 
                                               FontSize="{StaticResource FontSizeHero}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Excel 파일을 여기에 드래그" 
                                           Style="{StaticResource H3TextStyle}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="또는" 
                                           Style="{StaticResource BodyTextStyle}"
                                           Opacity="0.5"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"/>
                                <Button Content="📂 파일 선택하기" 
                                        Command="{Binding SelectExcelFileCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="24,12"
                                        FontSize="{StaticResource FontSizeH4}"/>
                                <TextBlock Text="지원 형식: .xlsx, .xls" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Opacity="0.5"
                                           HorizontalAlignment="Center"
                                           Margin="0,16,0,0"/>
                            </StackPanel>

                            <!-- 파일 선택됨 상태 -->
                            <Grid Margin="24"
                                  Visibility="{Binding SelectedExcelFile, Converter={StaticResource NullToVisibilityConverter}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 파일 정보 헤더 -->
                                <Border Grid.Row="0"
                                        Background="{StaticResource SuccessBrush}"
                                        CornerRadius="8"
                                        Padding="16"
                                        Margin="0,0,0,16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="✓" 
                                                   FontSize="{StaticResource FontSizeLarge}"
                                                   Foreground="White"
                                                   FontWeight="Bold"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="파일이 선택되었습니다" 
                                                       Foreground="White"
                                                       FontWeight="SemiBold"
                                                       FontSize="{StaticResource FontSizeH4}"/>
                                            <TextBlock Text="{Binding SelectedExcelFile}" 
                                                       Foreground="White"
                                                       Opacity="0.9"
                                                       FontSize="12"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                        <Button Grid.Column="2"
                                                Content="✕" 
                                                Command="{Binding CancelExcelFileCommand}"
                                                Background="Transparent"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Padding="8,4"
                                                FontSize="16"
                                                FontWeight="Bold"
                                                Cursor="Hand"
                                                ToolTip="파일 선택 취소"/>
                                    </Grid>
                                </Border>

                                <!-- 시트 선택 (Multi-Sheet) -->
                                <Border Grid.Row="1"
                                        Background="{StaticResource BlueGray50Brush}"
                                        CornerRadius="8"
                                        Padding="16"
                                        Margin="0,0,0,16"
                                        Visibility="{Binding ShowSheetSelection, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <TextBlock Text="📋" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="업로드할 시트 선택" 
                                                       Style="{StaticResource H4TextStyle}" 
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        
                                        <ScrollViewer MaxHeight="250" 
                                                    VerticalScrollBarVisibility="Auto"
                                                    HorizontalScrollBarVisibility="Disabled">
                                            <ItemsControl ItemsSource="{Binding AvailableSheets}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="White"
                                                            CornerRadius="6"
                                                            Padding="12,10"
                                                            Margin="0,0,0,8"
                                                            Cursor="Hand">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            
                                                            <CheckBox Grid.Column="0"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      Margin="0,0,12,0"/>
                                                            
                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding Name}" 
                                                                           FontWeight="SemiBold"
                                                                           FontSize="13"/>
                                                                <TextBlock FontSize="11" Opacity="0.7">
                                                                    <Run Text="{Binding SheetTypeDisplay, Mode=OneWay}"/>
                                                                    <Run Text=" · "/>
                                                                    <Run Text="{Binding DataRowCount, Mode=OneWay}"/>
                                                                    <Run Text="행"/>
                                                                </TextBlock>
                                                            </StackPanel>
                                                            
                                                            <Border Grid.Column="2"
                                                                    Background="{StaticResource PrimaryBrush}"
                                                                    CornerRadius="4"
                                                                    Padding="8,4"
                                                                    VerticalAlignment="Center"
                                                                    Opacity="0.8">
                                                                <TextBlock Text="{Binding SheetTypeDisplay}" 
                                                                           Foreground="White"
                                                                           FontSize="10"
                                                                           FontWeight="SemiBold"/>
                                                            </Border>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                        
                                        <!-- 업로드 버튼 -->
                                        <Button Content="📤 선택한 시트 업로드"
                                                Command="{Binding UploadSelectedSheetsCommand}"
                                                Style="{StaticResource PrimaryButton}"
                                                Padding="16,10"
                                                Margin="0,8,0,0"
                                                HorizontalAlignment="Stretch"
                                                IsEnabled="{Binding HasSelectedSheets}"/>
                                        
                                        <!-- 업로드 진행률 -->
                                        <StackPanel Visibility="{Binding IsExcelUploading, Converter={StaticResource BoolToVisibilityConverter}}"
                                                    Margin="0,12,0,0">
                                            <TextBlock Text="{Binding UploadStatusMessage}" 
                                                       FontSize="12" 
                                                       Opacity="0.8"
                                                       Margin="0,0,0,8"/>
                                            <ProgressBar Value="{Binding ExcelUploadProgress}"
                                                         Maximum="100"
                                                         Height="8"
                                                         Foreground="{StaticResource PrimaryBrush}"/>
                                            <TextBlock FontSize="11" Opacity="0.6" Margin="0,4,0,0">
                                                <Run Text="{Binding ProcessedSheets, Mode=OneWay}"/>
                                                <Run Text=" / "/>
                                                <Run Text="{Binding TotalSheetsToProcess, Mode=OneWay}"/>
                                                <Run Text=" 시트 완료"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- 데이터 요약 (단일 시트 모드) -->
                                <Border Grid.Row="2"
                                        Background="{StaticResource BlueGray50Brush}"
                                        CornerRadius="8"
                                        Padding="20"
                                        Visibility="{Binding ShowSheetSelection, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                    <StackPanel>
                                        <TextBlock Text="데이터 요약" 
                                                   Style="{StaticResource H4TextStyle}"
                                                   Margin="0,0,0,16"/>
                                        
                                        <Grid Margin="0,0,0,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="총 데이터 행" Style="{StaticResource BodyTextStyle}" Opacity="0.7"/>
                                            <TextBlock Grid.Column="1" FontWeight="Bold" FontSize="16">
                                                <Run Text="{Binding TotalRows}"/>
                                                <Run Text="행" FontWeight="Normal" FontSize="12"/>
                                            </TextBlock>
                                        </Grid>

                                        <Grid Margin="0,0,0,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="인식된 컬럼" Style="{StaticResource BodyTextStyle}" Opacity="0.7"/>
                                            <TextBlock Grid.Column="1" FontWeight="Bold" FontSize="16">
                                                <Run Text="{Binding ExcelColumns.Count, Mode=OneWay}"/>
                                                <Run Text="개" FontWeight="Normal" FontSize="12"/>
                                            </TextBlock>
                                        </Grid>

                                        <Separator Margin="0,8" Background="{StaticResource DividerBrush}"/>

                                        <TextBlock Text="컬럼 목록" 
                                                   Style="{StaticResource CaptionTextStyle}"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,8"/>
                                        <ItemsControl ItemsSource="{Binding ExcelColumns}" MaxHeight="150">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="{StaticResource BlueGray200Brush}"
                                                            CornerRadius="4"
                                                            Padding="8,4"
                                                            Margin="0,0,6,6">
                                                        <TextBlock Text="{Binding}" FontSize="11"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- 다른 파일 선택 버튼 -->
                                <Button Grid.Row="3"
                                        Content="📂 다른 파일 선택"
                                        Command="{Binding SelectExcelFileCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="16,10"
                                        Margin="0,16,0,0"
                                        HorizontalAlignment="Center"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- 템플릿 다운로드 버튼 -->
                    <Button Grid.Row="1"
                            Content="📥 표준 템플릿 다운로드"
                            Command="{Binding DownloadExcelTemplateCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Padding="16,10"
                            Margin="0,16,0,0"
                            HorizontalAlignment="Stretch"/>
                </Grid>

                <!-- 우측: 매핑 & 미리보기 또는 가이드 -->
                <Border Grid.Column="2"
                        Background="{StaticResource BackgroundBrush}"
                        CornerRadius="12">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
                    </Border.Effect>

                    <Grid>
                        <!-- 파일 미선택: 사용 가이드 -->
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      Visibility="{Binding SelectedExcelFile, Converter={StaticResource NullToInverseVisibilityConverter}}">
                            <StackPanel Margin="24">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                    <TextBlock Text="📘" FontSize="{StaticResource FontSizeH1}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    <TextBlock Text="Excel 업로드 가이드" Style="{StaticResource H3TextStyle}" VerticalAlignment="Center"/>
                                </StackPanel>

                                <!-- Step 1 -->
                                <Border Background="{StaticResource BlueGray50Brush}"
                                        CornerRadius="8"
                                        Padding="16"
                                        Margin="0,0,0,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Background="{StaticResource PrimaryBrush}"
                                                CornerRadius="16"
                                                Width="32" Height="32"
                                                Margin="0,0,16,0"
                                                VerticalAlignment="Top">
                                            <TextBlock Text="1" 
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      FontWeight="Bold"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="템플릿 다운로드" 
                                                      Style="{StaticResource BodyTextStyle}" 
                                                      FontWeight="SemiBold"
                                                      Margin="0,0,0,4"/>
                                            <TextBlock Text="좌측 하단의 '표준 템플릿 다운로드' 버튼을 클릭하여 양식을 받으세요."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      TextWrapping="Wrap"
                                                      Opacity="0.8"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Step 2 -->
                                <Border Background="{StaticResource BlueGray50Brush}"
                                        CornerRadius="8"
                                        Padding="16"
                                        Margin="0,0,0,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Background="{StaticResource PrimaryBrush}"
                                                CornerRadius="16"
                                                Width="32" Height="32"
                                                Margin="0,0,16,0"
                                                VerticalAlignment="Top">
                                            <TextBlock Text="2" 
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      FontWeight="Bold"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="데이터 입력" 
                                                      Style="{StaticResource BodyTextStyle}" 
                                                      FontWeight="SemiBold"
                                                      Margin="0,0,0,4"/>
                                            <TextBlock Text="템플릿을 열어 물건 정보를 입력하세요. 첫 행(헤더)은 수정하지 마세요."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      TextWrapping="Wrap"
                                                      Opacity="0.8"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Step 3 -->
                                <Border Background="{StaticResource BlueGray50Brush}"
                                        CornerRadius="8"
                                        Padding="16"
                                        Margin="0,0,0,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Background="{StaticResource PrimaryBrush}"
                                                CornerRadius="16"
                                                Width="32" Height="32"
                                                Margin="0,0,16,0"
                                                VerticalAlignment="Top">
                                            <TextBlock Text="3" 
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      FontWeight="Bold"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="파일 업로드" 
                                                      Style="{StaticResource BodyTextStyle}" 
                                                      FontWeight="SemiBold"
                                                      Margin="0,0,0,4"/>
                                            <TextBlock Text="좌측 영역에 파일을 드래그 앤 드롭하거나 버튼을 클릭하여 선택하세요."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      TextWrapping="Wrap"
                                                      Opacity="0.8"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Step 4 -->
                                <Border Background="{StaticResource BlueGray50Brush}"
                                        CornerRadius="8"
                                        Padding="16"
                                        Margin="0,0,0,20">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Background="{StaticResource PrimaryBrush}"
                                                CornerRadius="16"
                                                Width="32" Height="32"
                                                Margin="0,0,16,0"
                                                VerticalAlignment="Top">
                                            <TextBlock Text="4" 
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      FontWeight="Bold"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="매핑 확인 및 업로드" 
                                                      Style="{StaticResource BodyTextStyle}" 
                                                      FontWeight="SemiBold"
                                                      Margin="0,0,0,4"/>
                                            <TextBlock Text="컬럼 매핑을 확인하고 '업로드 시작' 버튼을 클릭하세요."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      TextWrapping="Wrap"
                                                      Opacity="0.8"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- 필수 컬럼 안내 -->
                                <Border Background="{StaticResource InfoBgBrush}"
                                        BorderBrush="{StaticResource InfoBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="16">
                                    <StackPanel>
                                        <TextBlock Text="📋 필수 컬럼 (10개)" 
                                                  FontWeight="SemiBold"
                                                  Margin="0,0,0,8"/>
                                        <WrapPanel>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="프로젝트ID" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="물건번호" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="물건유형" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="전체주소" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="도로명주소" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="지번주소" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="토지면적" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="건물면적" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="감정가" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource BlueGray200Brush}" CornerRadius="4" Padding="6,3" Margin="0,0,6,6">
                                                <TextBlock Text="최저입찰가" FontSize="11"/>
                                            </Border>
                                        </WrapPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- 파일 선택됨: 컬럼 매핑 & 미리보기 -->
                        <Grid Visibility="{Binding ShowMappingSection, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 컬럼 매핑 -->
                            <Border Grid.Row="0" Padding="24,24,24,0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0" Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="컬럼 매핑" Style="{StaticResource H4TextStyle}"/>
                                        <Button Grid.Column="1"
                                                Content="🔄 자동 매핑"
                                                Command="{Binding AutoMapColumnsCommand}"
                                                Style="{StaticResource SecondaryButton}"
                                                Padding="12,6"
                                                FontSize="12"/>
                                    </Grid>

                                    <ScrollViewer Grid.Row="1" 
                                                  VerticalScrollBarVisibility="Auto"
                                                  MaxHeight="180">
                                        <ItemsControl ItemsSource="{Binding ColumnMappings}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,0,0,8">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="32"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>

                                                        <Border Grid.Column="0" 
                                                                Background="{StaticResource BlueGray50Brush}"
                                                                CornerRadius="4"
                                                                Padding="10,8">
                                                            <TextBlock Text="{Binding TargetColumn}" 
                                                                       FontSize="12"
                                                                       VerticalAlignment="Center"
                                                                       TextTrimming="CharacterEllipsis"/>
                                                        </Border>
                                                        
                                                        <TextBlock Grid.Column="1" 
                                                                   Text="←" 
                                                                   VerticalAlignment="Center"
                                                                   HorizontalAlignment="Center"
                                                                   Opacity="0.4"
                                                                   FontSize="{StaticResource FontSizeH4}"/>
                                                        
                                                        <ComboBox Grid.Column="2"
                                                                  ItemsSource="{Binding DataContext.ExcelColumns, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                  SelectedItem="{Binding SourceColumn}"
                                                                  Padding="10,8"
                                                                  FontSize="12"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </Border>

                            <!-- 데이터 미리보기 -->
                            <Border Grid.Row="1" Padding="24,16">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                               Text="데이터 미리보기 (상위 10행)" 
                                               Style="{StaticResource H4TextStyle}"
                                               Margin="0,0,0,12"/>

                                    <DataGrid Grid.Row="1"
                                              ItemsSource="{Binding PreviewData}"
                                              AutoGenerateColumns="True"
                                              IsReadOnly="True"
                                              CanUserAddRows="False"
                                              HeadersVisibility="Column"
                                              HorizontalScrollBarVisibility="Auto"
                                              VerticalScrollBarVisibility="Auto"
                                              Background="{StaticResource SurfaceBrush}"
                                              BorderBrush="{StaticResource TableBorderBrush}"
                                              BorderThickness="1"
                                              GridLinesVisibility="All"
                                              HorizontalGridLinesBrush="{StaticResource BlueGray100Brush}"
                                              VerticalGridLinesBrush="{StaticResource BlueGray100Brush}">
                                        <DataGrid.ColumnHeaderStyle>
                                            <Style TargetType="DataGridColumnHeader">
                                                <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                                                <Setter Property="Foreground" Value="{StaticResource DeepNavy700Brush}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="FontSize" Value="11"/>
                                                <Setter Property="Padding" Value="8,6"/>
                                            </Style>
                                        </DataGrid.ColumnHeaderStyle>
                                        <DataGrid.CellStyle>
                                            <Style TargetType="DataGridCell">
                                                <Setter Property="FontSize" Value="11"/>
                                                <Setter Property="Padding" Value="6,4"/>
                                            </Style>
                                        </DataGrid.CellStyle>
                                    </DataGrid>
                                </Grid>
                            </Border>

                            <!-- 액션 버튼 -->
                            <Border Grid.Row="2" 
                                    Background="{StaticResource BlueGray50Brush}"
                                    Padding="24,16">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- 에러 메시지 -->
                                    <Border Grid.Row="0"
                                            Background="{StaticResource ErrorBrush}"
                                            CornerRadius="6"
                                            Padding="12"
                                            Margin="0,0,0,12"
                                            Visibility="{Binding ExcelErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
                                        <TextBlock Text="{Binding ExcelErrorMessage}" 
                                                   Foreground="White"
                                                   TextWrapping="Wrap"
                                                   FontSize="12"/>
                                    </Border>

                                    <!-- 프로그레스 & 버튼 -->
                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 프로그레스바 -->
                                        <StackPanel Grid.Column="0" 
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,16,0"
                                                    Visibility="{Binding IsExcelUploading, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <ProgressBar Value="{Binding ExcelUploadProgress}" 
                                                         Maximum="100"
                                                         Height="8"
                                                         Foreground="{StaticResource PrimaryBrush}"
                                                         Margin="0,0,0,6"/>
                                            <TextBlock Style="{StaticResource CaptionTextStyle}" Opacity="0.7">
                                                <Run Text="{Binding ExcelUploadProgress, StringFormat=N0}"/>
                                                <Run Text="% 완료 ("/>
                                                <Run Text="{Binding ProcessedRows}"/>
                                                <Run Text="/"/>
                                                <Run Text="{Binding TotalRows}"/>
                                                <Run Text="행)"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <!-- 버튼 -->
                                        <Button Grid.Column="1"
                                                Content="🚀 업로드 시작" 
                                                Command="{Binding UploadExcelCommand}"
                                                Style="{StaticResource PrimaryButton}"
                                                Padding="32,12"
                                                FontSize="{StaticResource FontSizeH4}"
                                                FontWeight="SemiBold"
                                                IsEnabled="{Binding IsExcelUploading, Converter={StaticResource InverseBooleanConverter}}"/>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <!-- PDF 업로드 패널 -->
            <Grid Visibility="{Binding IsChecked, ElementName=PdfTabRadio, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="400"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="1.2*" MinWidth="500"/>
                </Grid.ColumnDefinitions>

                <!-- 좌측: 파일 업로드 영역 -->
                <Border Grid.Column="0"
                        Background="{StaticResource BackgroundBrush}"
                        BorderBrush="{StaticResource BlueGray300Brush}"
                        BorderThickness="2"
                        CornerRadius="12"
                        AllowDrop="True"
                        Drop="PdfDropZone_Drop"
                        DragOver="DropZone_DragOver">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
                    </Border.Effect>

                    <Grid>
                        <!-- 파일 없음 상태 -->
                        <StackPanel HorizontalAlignment="Center" 
                                    VerticalAlignment="Center"
                                    Visibility="{Binding HasPdfFiles, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <Border Background="{StaticResource BlueGray100Brush}"
                                    CornerRadius="50"
                                    Width="100" Height="100"
                                    HorizontalAlignment="Center"
                                    Margin="0,0,0,24">
                                <TextBlock Text="📄" 
                                           FontSize="{StaticResource FontSizeHero}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="PDF 파일을 여기에 드래그" 
                                       Style="{StaticResource H3TextStyle}"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,8"/>
                            <TextBlock Text="다중 파일 선택 가능" 
                                       Style="{StaticResource CaptionTextStyle}"
                                       Opacity="0.5"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,16"/>
                            <Button Content="📂 파일 선택하기" 
                                    Command="{Binding SelectPdfFilesCommand}"
                                    Style="{StaticResource PrimaryButton}"
                                    Padding="24,12"
                                    FontSize="{StaticResource FontSizeH4}"/>
                            <TextBlock Text="지원 형식: .pdf" 
                                       Style="{StaticResource CaptionTextStyle}"
                                       Opacity="0.5"
                                       HorizontalAlignment="Center"
                                       Margin="0,16,0,0"/>
                        </StackPanel>

                        <!-- 파일 선택됨 상태 -->
                        <Grid Margin="20"
                              Visibility="{Binding HasPdfFiles, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 헤더 -->
                            <Grid Grid.Row="0" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="선택된 파일" Style="{StaticResource H4TextStyle}" VerticalAlignment="Center"/>
                                    <Border Background="{StaticResource PrimaryBrush}"
                                            CornerRadius="10"
                                            Padding="8,2"
                                            Margin="8,0,0,0">
                                        <TextBlock Text="{Binding PdfFiles.Count, Mode=OneWay}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                    </Border>
                                </StackPanel>
                                <Button Grid.Column="1"
                                        Content="✕ 전체 취소"
                                        Command="{Binding CancelAllPdfFilesCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="10,6"
                                        FontSize="11"/>
                            </Grid>

                            <!-- 파일 목록 -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding PdfFiles}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource BlueGray50Brush}"
                                                    CornerRadius="6"
                                                    Padding="12"
                                                    Margin="0,0,0,8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Text="📄" FontSize="{StaticResource FontSizeLarge}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                    
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding FileName}" 
                                                                   FontWeight="SemiBold"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding FileSizeFormatted}" 
                                                                   Style="{StaticResource CaptionTextStyle}"
                                                                   Opacity="0.6"/>
                                                    </StackPanel>

                                                    <Border Grid.Column="2"
                                                            CornerRadius="4"
                                                            Padding="8,4"
                                                            Margin="8,0"
                                                            VerticalAlignment="Center">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="{StaticResource BlueGray200Brush}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Status}" Value="완료">
                                                                        <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Status}" Value="업로드중">
                                                                        <Setter Property="Background" Value="{StaticResource InfoBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding Status}" FontSize="11">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Status}" Value="완료">
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Status}" Value="업로드중">
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Border>

                                                    <Button Grid.Column="3"
                                                            Content="✕"
                                                            Command="{Binding DataContext.RemovePdfFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            Foreground="{StaticResource TextSecondaryBrush}"
                                                            Padding="8,4"
                                                            FontSize="{StaticResource FontSizeH4}"
                                                            Cursor="Hand"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <!-- 추가 버튼 -->
                            <Button Grid.Row="2"
                                    Content="+ 파일 추가"
                                    Command="{Binding SelectPdfFilesCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="16,10"
                                    Margin="0,12,0,0"
                                    HorizontalAlignment="Stretch"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- 우측: 업로드 상태 -->
                <Border Grid.Column="2"
                        Background="{StaticResource BackgroundBrush}"
                        CornerRadius="12">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
                    </Border.Effect>

                    <Grid>
                        <!-- 업로드 전: 안내 -->
                        <StackPanel Margin="24"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding HasPdfFiles, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <TextBlock Text="📄" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                            <TextBlock Text="PDF 파일 업로드" 
                                       Style="{StaticResource H3TextStyle}"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,12"/>
                            <TextBlock Text="PDF 파일은 Supabase Storage에 안전하게 저장됩니다."
                                       Style="{StaticResource BodyTextStyle}"
                                       Opacity="0.7"
                                       HorizontalAlignment="Center"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       MaxWidth="300"/>
                        </StackPanel>

                        <!-- 업로드 가능 상태 -->
                        <Grid Visibility="{Binding HasPdfFiles, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 상태 표시 -->
                            <StackPanel Grid.Row="0" 
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Margin="24">
                                <Border Background="{StaticResource BlueGray50Brush}"
                                        CornerRadius="60"
                                        Width="120" Height="120"
                                        Margin="0,0,0,24">
                                    <TextBlock FontSize="{StaticResource FontSizeHero}" 
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="📤"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPdfUploading}" Value="True">
                                                        <Setter Property="Text" Value="⏳"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                                
                                <TextBlock HorizontalAlignment="Center"
                                           Margin="0,0,0,8">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource H3TextStyle}">
                                            <Setter Property="Text">
                                                <Setter.Value>
                                                    <Binding Path="PdfFiles.Count" Mode="OneWay" StringFormat="{}{0}개 파일 준비됨"/>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsPdfUploading}" Value="True">
                                                    <Setter Property="Text" Value="업로드 중..."/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- 업로드 진행률 -->
                                <StackPanel Visibility="{Binding IsPdfUploading, Converter={StaticResource BoolToVisibilityConverter}}"
                                            Margin="0,8,0,0"
                                            Width="200">
                                    <ProgressBar Value="{Binding PdfUploadProgress}"
                                                 Maximum="100"
                                                 Height="8"
                                                 Foreground="{StaticResource PrimaryBrush}"
                                                 Margin="0,0,0,8"/>
                                    <TextBlock Style="{StaticResource BodyTextStyle}"
                                               Opacity="0.7"
                                               HorizontalAlignment="Center">
                                        <Run Text="{Binding CompletedPdfCount}"/>
                                        <Run Text="/"/>
                                        <Run Text="{Binding TotalPdfCount}"/>
                                        <Run Text="완료"/>
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>

                            <!-- 업로드 버튼 -->
                            <Border Grid.Row="1"
                                    Background="{StaticResource BlueGray50Brush}"
                                    Padding="24,16">
                                <Button Content="🚀 업로드 시작" 
                                        Command="{Binding UploadPdfFilesCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="32,12"
                                        FontSize="{StaticResource FontSizeH4}"
                                        FontWeight="SemiBold"
                                        HorizontalAlignment="Center"
                                        IsEnabled="{Binding IsPdfUploading, Converter={StaticResource InverseBooleanConverter}}"/>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
