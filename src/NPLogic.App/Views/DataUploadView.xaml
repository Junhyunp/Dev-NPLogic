<UserControl x:Class="NPLogic.Views.DataUploadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:DataUploadViewModel}"
             AllowDrop="True"
             Drop="DataUploadView_Drop"
             DragOver="DataUploadView_DragOver">
    
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 영역 -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="데이터 업로드" 
                           Style="{StaticResource H2TextStyle}"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Excel 파일 또는 PDF 파일을 업로드하여 물건 데이터를 일괄 등록하세요" 
                           Style="{StaticResource BodyTextStyle}"
                           Opacity="0.7"/>
            </StackPanel>
        </Grid>

        <!-- 메인 컨텐츠 -->
        <Grid Grid.Row="1">
            <TabControl>
                <!-- Excel 업로드 탭 -->
                <TabItem Header="Excel 업로드">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                HorizontalScrollBarVisibility="Disabled">
                    <Grid Margin="24">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 파일 선택 영역 -->
                        <Border Grid.Row="0"
                                Background="{StaticResource BlueGray50Brush}"
                                BorderBrush="{StaticResource PrimaryBrush}"
                                BorderThickness="2"
                                CornerRadius="8"
                                Padding="40"
                                Margin="0,0,0,24"
                                AllowDrop="True"
                                Drop="ExcelDropZone_Drop"
                                DragOver="DropZone_DragOver">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="📁" 
                                           FontSize="64"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,16"/>
                                <TextBlock Text="Excel 파일을 여기에 드래그하거나 클릭하여 선택하세요" 
                                           Style="{StaticResource H4TextStyle}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="지원 형식: .xlsx, .xls" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Opacity="0.6"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,16"/>
                                <Button Content="파일 선택" 
                                        Command="{Binding SelectExcelFileCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="24,12"/>
                                
                                <!-- 선택된 파일 표시 -->
                                <StackPanel Orientation="Horizontal"
                                           HorizontalAlignment="Center"
                                           Margin="0,16,0,0"
                                           Visibility="{Binding SelectedExcelFile, Converter={StaticResource NullToVisibilityConverter}}">
                                    <TextBlock Text="{Binding SelectedExcelFile}" 
                                               Style="{StaticResource BodyTextStyle}"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               Margin="0,0,12,0"/>
                                    <Button Content="✕ 취소" 
                                            Command="{Binding CancelExcelFileCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,6"
                                            FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- 사용 가이드 패널 (접기/펴기 가능) -->
                        <Border Grid.Row="1"
                                Background="{StaticResource InfoBgBrush}"
                                BorderBrush="{StaticResource InfoBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Margin="0,0,0,16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 헤더 (항상 표시) -->
                                <Grid Grid.Row="0" Background="Transparent">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0"
                                            Command="{Binding ToggleGuideCommand}"
                                            HorizontalContentAlignment="Left"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Padding="16,12"
                                            Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#10000000"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📘" 
                                                      FontSize="18" 
                                                      VerticalAlignment="Center" 
                                                      Margin="0,0,8,0"/>
                                            <TextBlock Text="Excel 업로드 사용 가이드" 
                                                      Style="{StaticResource H4TextStyle}"
                                                      VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding GuideExpandIcon}" 
                                                      FontSize="14"
                                                      VerticalAlignment="Center"
                                                      Margin="8,0,0,0"/>
                                        </StackPanel>
                                    </Button>

                                    <Button Grid.Column="1"
                                            Content="📥 템플릿 다운로드"
                                            Command="{Binding DownloadExcelTemplateCommand}"
                                            Style="{StaticResource PrimaryButton}"
                                            Padding="16,8"
                                            Margin="0,8,16,8"/>
                                </Grid>

                                <!-- 가이드 내용 (펼쳤을 때만 표시) -->
                                <Border Grid.Row="1"
                                        Padding="16,0,16,16"
                                        Visibility="{Binding IsGuideExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel>
                                    
                                    <!-- Step 1 -->
                                    <Grid Margin="0,12,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Grid.Column="0"
                                                Background="{StaticResource PrimaryBrush}"
                                                CornerRadius="12"
                                                Width="24" Height="24"
                                                Margin="0,0,12,0">
                                            <TextBlock Text="1" 
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      FontWeight="Bold"
                                                      FontSize="12"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Style="{StaticResource BodyTextStyle}" 
                                                      FontWeight="SemiBold"
                                                      Margin="0,0,0,4">
                                                <Run Text="📥"/>
                                                <Run Text="템플릿 다운로드"/>
                                            </TextBlock>
                                            <TextBlock Text="우측 상단의 '템플릿 다운로드' 버튼을 클릭하여 표준 양식을 다운로드하세요."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      TextWrapping="Wrap"
                                                      Opacity="0.8"/>
                                            <TextBlock Text="파일명: 물건목록_템플릿.xlsx"
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      Opacity="0.7"
                                                      FontStyle="Italic"
                                                      Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>

                                    <Separator Margin="0,12" Background="{StaticResource DividerBrush}"/>

                                    <!-- Step 2 -->
                                    <Grid Margin="0,0,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Grid.Column="0"
                                                Background="{StaticResource PrimaryBrush}"
                                                CornerRadius="12"
                                                Width="24" Height="24"
                                                Margin="0,0,12,0">
                                            <TextBlock Text="2" 
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      FontWeight="Bold"
                                                      FontSize="12"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Style="{StaticResource BodyTextStyle}" 
                                                      FontWeight="SemiBold"
                                                      Margin="0,0,0,4">
                                                <Run Text="✏️"/>
                                                <Run Text="데이터 입력"/>
                                            </TextBlock>
                                            <TextBlock Text="다운로드한 템플릿 파일을 열어 물건 정보를 입력하세요."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      TextWrapping="Wrap"
                                                      Opacity="0.8"/>
                                            <TextBlock Style="{StaticResource CaptionTextStyle}"
                                                      Opacity="0.7"
                                                      Margin="0,4,0,0"
                                                      TextWrapping="Wrap">
                                                <Run Text="필수 컬럼:"/>
                                                <Run Text="프로젝트ID, 물건번호, 물건유형, 주소, 면적, 금액 등 10개"/>
                                            </TextBlock>
                                            <TextBlock Text="⚠️ 첫 행(헤더)은 수정하지 마세요!"
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      Foreground="{StaticResource WarningBrush}"
                                                      FontWeight="SemiBold"
                                                      Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Grid>

                                    <Separator Margin="0,12" Background="{StaticResource DividerBrush}"/>

                                    <!-- Step 3 -->
                                    <Grid Margin="0,0,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Grid.Column="0"
                                                Background="{StaticResource PrimaryBrush}"
                                                CornerRadius="12"
                                                Width="24" Height="24"
                                                Margin="0,0,12,0">
                                            <TextBlock Text="3" 
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      FontWeight="Bold"
                                                      FontSize="12"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Style="{StaticResource BodyTextStyle}" 
                                                      FontWeight="SemiBold"
                                                      Margin="0,0,0,4">
                                                <Run Text="📤"/>
                                                <Run Text="파일 업로드"/>
                                            </TextBlock>
                                            <TextBlock Text="작성 완료된 파일을 아래 영역에 드래그 앤 드롭하거나 '파일 선택' 버튼을 클릭하세요."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      TextWrapping="Wrap"
                                                      Opacity="0.8"/>
                                            <TextBlock Text="자동으로 데이터 검증 및 미리보기가 제공됩니다."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      Opacity="0.7"
                                                      Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>

                                    <Separator Margin="0,12" Background="{StaticResource DividerBrush}"/>

                                    <!-- Step 4 -->
                                    <Grid Margin="0,0,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Grid.Column="0"
                                                Background="{StaticResource PrimaryBrush}"
                                                CornerRadius="12"
                                                Width="24" Height="24"
                                                Margin="0,0,12,0">
                                            <TextBlock Text="4" 
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      FontWeight="Bold"
                                                      FontSize="12"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Style="{StaticResource BodyTextStyle}" 
                                                      FontWeight="SemiBold"
                                                      Margin="0,0,0,4">
                                                <Run Text="✅"/>
                                                <Run Text="업로드 확인"/>
                                            </TextBlock>
                                            <TextBlock Text="미리보기에서 데이터를 확인한 후 '업로드 시작' 버튼을 클릭하세요."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      TextWrapping="Wrap"
                                                      Opacity="0.8"/>
                                            <TextBlock Text="모든 데이터가 데이터베이스에 자동으로 저장됩니다."
                                                      Style="{StaticResource CaptionTextStyle}"
                                                      Opacity="0.7"
                                                      Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- 추가 안내 -->
                                    <Border Background="{StaticResource WarningBgBrush}"
                                            BorderBrush="{StaticResource WarningBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="12"
                                            Margin="0,16,0,0">
                                        <TextBlock Style="{StaticResource CaptionTextStyle}"
                                                  Foreground="{StaticResource WarningTextBrush}"
                                                  TextWrapping="Wrap">
                                            <Run Text="💡 팁:" FontWeight="SemiBold"/>
                                            <Run Text="템플릿을 사용하면 컬럼 매핑 없이 바로 업로드할 수 있습니다. 기존 Excel 파일을 사용하는 경우 컬럼 매핑 화면에서 수동으로 연결하세요."/>
                                        </TextBlock>
                                    </Border>
                                </StackPanel>
                                </Border>
                            </Grid>
                        </Border>

                        <!-- 컬럼 매핑 및 미리보기 -->
                        <Border Grid.Row="2"
                                Background="{StaticResource BackgroundBrush}"
                                CornerRadius="8"
                                Visibility="{Binding ShowMappingSection, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                            </Border.Effect>

                            <Grid Margin="24">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- 컬럼 매핑 헤더 -->
                                <TextBlock Grid.Row="0"
                                           Text="컬럼 매핑" 
                                           Style="{StaticResource H3TextStyle}"
                                           Margin="0,0,0,16"/>

                                <!-- 매핑 그리드 -->
                                <ScrollViewer Grid.Row="1" 
                                            VerticalScrollBarVisibility="Auto"
                                            MaxHeight="200"
                                            Margin="0,0,0,16">
                                    <ItemsControl ItemsSource="{Binding ColumnMappings}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="200"/>
                                                        <ColumnDefinition Width="32"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0" 
                                                             Text="{Binding TargetColumn}" 
                                                             VerticalAlignment="Center"
                                                             FontWeight="SemiBold"/>
                                                    
                                                    <TextBlock Grid.Column="1" 
                                                             Text="←" 
                                                             VerticalAlignment="Center"
                                                             HorizontalAlignment="Center"
                                                             Opacity="0.5"/>
                                                    
                                                    <ComboBox Grid.Column="2"
                                                            ItemsSource="{Binding DataContext.ExcelColumns, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            SelectedItem="{Binding SourceColumn}"
                                                            Padding="12,8"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <!-- 데이터 미리보기 -->
                                <Grid Grid.Row="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                             Text="데이터 미리보기" 
                                             Style="{StaticResource H4TextStyle}"
                                             Margin="0,0,0,12"/>

                                    <DataGrid Grid.Row="1"
                                            ItemsSource="{Binding PreviewData}"
                                            AutoGenerateColumns="True"
                                            IsReadOnly="True"
                                            MaxHeight="200"/>
                                </Grid>
                            </Grid>
                        </Border>

                        <!-- 액션 버튼 -->
                        <Grid Grid.Row="3" Margin="0,16,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 에러 메시지 -->
                            <Border Grid.Row="0"
                                    Background="{StaticResource ErrorBrush}"
                                    CornerRadius="4"
                                    Padding="12"
                                    Margin="0,0,0,12"
                                    Visibility="{Binding ExcelErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
                                <TextBlock Text="{Binding ExcelErrorMessage}" 
                                          Foreground="White"
                                          TextWrapping="Wrap"/>
                            </Border>

                            <!-- 버튼 및 프로그레스 -->
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- 프로그레스바 -->
                                <StackPanel Grid.Column="0" 
                                          VerticalAlignment="Center"
                                          Visibility="{Binding IsExcelUploading, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ProgressBar Value="{Binding ExcelUploadProgress}" 
                                               Maximum="100"
                                               Height="8"
                                               Margin="0,0,0,8"/>
                                    <TextBlock Style="{StaticResource CaptionTextStyle}"
                                             Opacity="0.7">
                                        <Run Text="{Binding ExcelUploadProgress, StringFormat=N0}"/>
                                        <Run Text="% 완료"/>
                                        <Run Text=" | "/>
                                        <Run Text="{Binding ProcessedRows}"/>
                                        <Run Text="/"/>
                                        <Run Text="{Binding TotalRows}"/>
                                        <Run Text="행"/>
                                    </TextBlock>
                                </StackPanel>

                                <!-- 버튼들 -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="자동 매핑" 
                                            Command="{Binding AutoMapColumnsCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="20,10"
                                            Margin="0,0,8,0"
                                            Visibility="{Binding ShowMappingSection, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    <Button Content="업로드 시작" 
                                            Command="{Binding UploadExcelCommand}"
                                            Style="{StaticResource PrimaryButton}"
                                            Padding="24,10"
                                            IsEnabled="{Binding IsExcelUploading, Converter={StaticResource InverseBooleanConverter}}"
                                            Visibility="{Binding ShowMappingSection, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Grid>
                    </ScrollViewer>
                </TabItem>

                <!-- PDF 업로드 탭 -->
                <TabItem Header="PDF 업로드">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                HorizontalScrollBarVisibility="Disabled">
                    <Grid Margin="24">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 파일 선택 영역 -->
                        <Border Grid.Row="0"
                                Background="{StaticResource BlueGray50Brush}"
                                BorderBrush="{StaticResource PrimaryBrush}"
                                BorderThickness="2"
                                CornerRadius="8"
                                Padding="40"
                                Margin="0,0,0,24"
                                AllowDrop="True"
                                Drop="PdfDropZone_Drop"
                                DragOver="DropZone_DragOver">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="📄" 
                                           FontSize="64"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,16"/>
                                <TextBlock Text="PDF 파일을 여기에 드래그하거나 클릭하여 선택하세요" 
                                           Style="{StaticResource H4TextStyle}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="다중 파일 선택 가능 | 지원 형식: .pdf" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Opacity="0.6"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,16"/>
                                <Button Content="파일 선택" 
                                        Command="{Binding SelectPdfFilesCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="24,12"/>
                            </StackPanel>
                        </Border>

                        <!-- 선택된 파일 목록 -->
                        <Border Grid.Row="1"
                                Background="{StaticResource BackgroundBrush}"
                                CornerRadius="8"
                                Visibility="{Binding HasPdfFiles, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                            </Border.Effect>

                            <Grid Margin="24">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                                    <TextBlock Text="선택된 파일 목록" 
                                             Style="{StaticResource H3TextStyle}"
                                             VerticalAlignment="Center"/>
                                    <TextBlock Style="{StaticResource BodyTextStyle}"
                                             VerticalAlignment="Center"
                                             Margin="12,0,0,0">
                                        <Run Text="("/>
                                        <Run Text="{Binding PdfFiles.Count, Mode=OneWay}"/>
                                        <Run Text="개)"/>
                                    </TextBlock>
                                    <Button Content="✕ 전체 취소" 
                                            Command="{Binding CancelAllPdfFilesCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,6"
                                            FontSize="12"
                                            Margin="16,0,0,0"
                                            VerticalAlignment="Center"/>
                                </StackPanel>

                                <DataGrid Grid.Row="1"
                                        ItemsSource="{Binding PdfFiles}"
                                        AutoGenerateColumns="False"
                                        CanUserAddRows="False"
                                        IsReadOnly="True">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="파일명" Binding="{Binding FileName}" Width="*"/>
                                        <DataGridTextColumn Header="크기" Binding="{Binding FileSizeFormatted}" Width="100"/>
                                        <DataGridTextColumn Header="상태" Binding="{Binding Status}" Width="100"/>
                                        <DataGridTemplateColumn Header="진행률" Width="200">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ProgressBar Value="{Binding Progress}" 
                                                               Maximum="100"
                                                               Height="20"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Header="작업" Width="80">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="삭제" 
                                                           Command="{Binding DataContext.RemovePdfFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                           CommandParameter="{Binding}"
                                                           Style="{StaticResource DangerButton}"
                                                           Padding="8,4"
                                                           FontSize="12"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <!-- 업로드 버튼 -->
                        <Grid Grid.Row="2" Margin="0,16,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 전체 프로그레스 -->
                            <StackPanel Grid.Column="0" 
                                      VerticalAlignment="Center"
                                      Visibility="{Binding IsPdfUploading, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Style="{StaticResource BodyTextStyle}"
                                         FontWeight="SemiBold"
                                         Margin="0,0,0,8">
                                    <Run Text="전체 업로드 진행:"/>
                                    <Run Text="{Binding CompletedPdfCount}"/>
                                    <Run Text="/"/>
                                    <Run Text="{Binding TotalPdfCount}"/>
                                    <Run Text="파일"/>
                                </TextBlock>
                                <ProgressBar Value="{Binding PdfUploadProgress}" 
                                           Maximum="100"
                                           Height="8"/>
                            </StackPanel>

                            <!-- 버튼 -->
                            <Button Grid.Column="1"
                                    Content="업로드 시작" 
                                    Command="{Binding UploadPdfFilesCommand}"
                                    Style="{StaticResource PrimaryButton}"
                                    Padding="24,10"
                                    IsEnabled="{Binding IsPdfUploading, Converter={StaticResource InverseBooleanConverter}}"
                                    Visibility="{Binding HasPdfFiles, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </Grid>
                    </Grid>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>

