<UserControl x:Class="NPLogic.Views.EvaluationTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1400"
             d:DataContext="{d:DesignInstance Type=vm:EvaluationTabViewModel}">

    <UserControl.Resources>
        <!-- 피드백 반영: 공통 스타일 참조로 통일 -->
        <!-- SectionHeader, LabelText, ValueText, AmountText, CardBorder 스타일은 Typography.xaml에서 참조 -->
        
        <!-- 로컬 별칭 (하위 호환성) -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource SectionHeader}">
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock" BasedOn="{StaticResource LabelText}"/>
        <Style x:Key="ValueStyle" TargetType="TextBlock" BasedOn="{StaticResource ValueText}"/>
        <Style x:Key="AmountStyle" TargetType="TextBlock" BasedOn="{StaticResource AmountText}"/>
        <Style x:Key="CardStyle" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Setter Property="Padding" Value="16"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 로딩 오버레이 -->
            <Border Grid.RowSpan="2" 
                    Background="{StaticResource OverlaySemiLightBrush}" 
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                    Panel.ZIndex="100">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                    <TextBlock Text="로딩 중..." Margin="0,12,0,0" 
                               HorizontalAlignment="Center"
                               Style="{StaticResource BodyTextStyle}"/>
                </StackPanel>
            </Border>

            <!-- 헤더: 평가 유형 선택 -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="📊 평가 유형 선택" 
                                   Style="{StaticResource SectionHeaderStyle}"
                                   Margin="0"/>
                    </StackPanel>

                    <!-- 평가 유형 라디오 버튼 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" 
                                Margin="32,0,0,0"
                                VerticalAlignment="Center">
                        <RadioButton Content="1. 아파트" 
                                     IsChecked="{Binding IsApartmentType}"
                                     GroupName="EvaluationType"
                                     Margin="0,0,24,0"
                                     FontSize="{StaticResource FontSizeBody}"/>
                        <RadioButton Content="2. 연립다세대" 
                                     IsChecked="{Binding IsMultiFamilyType}"
                                     GroupName="EvaluationType"
                                     Margin="0,0,24,0"
                                     FontSize="{StaticResource FontSizeBody}"/>
                        <RadioButton Content="3. 공장/창고" 
                                     IsChecked="{Binding IsFactoryType}"
                                     GroupName="EvaluationType"
                                     Margin="0,0,24,0"
                                     FontSize="{StaticResource FontSizeBody}"/>
                        <RadioButton Content="4. 상가/아파트형공장" 
                                     IsChecked="{Binding IsCommercialType}"
                                     GroupName="EvaluationType"
                                     Margin="0,0,24,0"
                                     FontSize="{StaticResource FontSizeBody}"/>
                        <RadioButton Content="5. 주택/근린시설/토지/기타" 
                                     IsChecked="{Binding IsHouseLandType}"
                                     GroupName="EvaluationType"
                                     FontSize="{StaticResource FontSizeBody}"/>
                    </StackPanel>

                    <!-- 피드백 반영: 탭 내부 저장/새로고침 버튼 제거 → 상단 헤더에서만 저장 -->
                    <!-- 저장은 PropertyDetailView 헤더의 버튼 사용 -->
                </Grid>
            </Border>

            <!-- 메인 콘텐츠 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 좌측: 사례지도 + 사례평가 -->
                <StackPanel Grid.Column="0">
                    
                    <!-- 사례지도 영역 -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="🗺️ 사례지도" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
                                <Button Content="가져오기" 
                                        Command="{Binding LoadCaseMapCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="12,4"
                                        FontSize="{StaticResource FontSizeBody}"
                                        HorizontalAlignment="Right"/>
                            </Grid>
                            
                            <!-- 지도 플레이스홀더 (E-001: 클릭 시 팝업 확대) -->
                            <Border Background="{StaticResource BlueGray50Brush}" 
                                    CornerRadius="4"
                                    Height="200"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="MapPlaceholder_MouseLeftButtonUp"
                                    ToolTip="클릭하여 확대">
                                <Grid>
                                    <TextBlock Text="소재지 기준으로 위치, 본건기준거래 자동 산정 및 표시"
                                               Style="{StaticResource BodyTextStyle}"
                                               Opacity="0.6"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Text="🔍 클릭하여 확대"
                                               Style="{StaticResource BodyTextStyle}"
                                               FontSize="{StaticResource FontSizeSmall}"
                                               Opacity="0.5"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Bottom"
                                               Margin="0,0,8,8"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- 유사물건 추천 영역 -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <!-- 제목 -->
                            <TextBlock Text="🎯 유사물건 추천" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,12"/>
                            
                            <!-- 필터 + 버튼 행 -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <!-- 규칙 순위 선택 -->
                                <TextBlock Text="규칙:" Style="{StaticResource LabelStyle}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <ComboBox SelectedIndex="{Binding SelectedRuleIndex, Mode=TwoWay}"
                                          Width="70" FontSize="{StaticResource FontSizeBody}" Padding="4,2" Margin="0,0,16,0">
                                    <ComboBoxItem Content="1순위" Tag="1"/>
                                    <ComboBoxItem Content="2순위" Tag="2"/>
                                    <ComboBoxItem Content="3순위" Tag="3"/>
                                    <ComboBoxItem Content="전체"/>
                                </ComboBox>
                                <!-- 지역 범위 선택 -->
                                <TextBlock Text="범위:" Style="{StaticResource LabelStyle}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <ComboBox Width="80" FontSize="{StaticResource FontSizeBody}" Padding="4,2" Margin="0,0,16,0"
                                          SelectedValue="{Binding SelectedRegionScope, Mode=TwoWay}"
                                          SelectedValuePath="Tag">
                                    <ComboBoxItem Content="시/도" Tag="big"/>
                                    <ComboBoxItem Content="시/군/구" Tag="mid"/>
                                </ComboBox>
                                <Button Content="추천 검색" 
                                        Command="{Binding RecommendSimilarCasesCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="12,4"
                                        FontSize="{StaticResource FontSizeBody}"
                                        Margin="0,0,8,0"/>
                                <Button Content="사례 적용" 
                                        Command="{Binding ApplyRecommendedCaseCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="12,4"
                                        FontSize="{StaticResource FontSizeBody}"
                                        Margin="0,0,16,0"/>
                                <!-- 로딩 표시 -->
                                <ProgressBar IsIndeterminate="True" 
                                             Width="80" Height="4" 
                                             VerticalAlignment="Center"
                                             Visibility="{Binding IsRecommendLoading, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}"/>
                            </StackPanel>
                            
                            <!-- 상태 메시지 -->
                            <TextBlock Text="{Binding RecommendStatusMessage}" 
                                       Style="{StaticResource LabelStyle}"
                                       Margin="0,0,0,8"
                                       Visibility="{Binding RecommendStatusMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
                            
                            <!-- 추천 결과 테이블 -->
                            <DataGrid ItemsSource="{Binding RecommendedCases}"
                                      SelectedItem="{Binding SelectedRecommendCase, Mode=TwoWay}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="All"
                                      BorderThickness="1"
                                      BorderBrush="{StaticResource BlueGray200Brush}"
                                      MaxHeight="350"
                                      MinHeight="200"
                                      FontSize="{StaticResource FontSizeSmall}"
                                      SelectionMode="Single">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="사건번호" Binding="{Binding CaseNo}" Width="110" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="낙찰일" Binding="{Binding AuctionDate, StringFormat=yyyy-MM-dd}" Width="85" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="용도" Binding="{Binding Usage}" Width="70" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="감정가" Binding="{Binding AppraisalPrice, StringFormat=N0}" Width="100" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="낙찰가" Binding="{Binding WinningPrice, StringFormat=N0}" Width="100" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="낙찰율" Binding="{Binding WinningRateDisplay}" Width="60" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="건물면적" Binding="{Binding BuildingArea, StringFormat=N1}" Width="70" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="규칙" Binding="{Binding RuleName}" Width="55" IsReadOnly="True"/>
                                    <!-- E-005: 거리 컬럼 추가 -->
                                    <DataGridTextColumn Header="거리" Binding="{Binding DistanceDisplay}" Width="60" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <!-- 상세보기 버튼 -->
                                    <DataGridTemplateColumn Header="상세" Width="50">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="📋"
                                                        Command="{Binding DataContext.ShowCaseDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource SecondaryButton}"
                                                        Padding="4,2"
                                                        FontSize="{StaticResource FontSizeBody}"
                                                        ToolTip="상세보기"
                                                        Cursor="Hand"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <!-- 사례평가 그리드 -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📋 사례평가" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
                                    <TextBlock Text="(직접 입력 가능)" 
                                               Style="{StaticResource LabelStyle}"
                                               FontSize="{StaticResource FontSizeSmall}"
                                               Opacity="0.7"
                                               Margin="8,0,0,0"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="초기화" 
                                            Command="{Binding ClearCaseEvaluationCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"
                                            Margin="0,0,8,0"/>
                                    <Button Content="경매사건검색" 
                                            Command="{Binding SearchAuctionCaseCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- 사례평가 테이블 (사용자 편집 가능) -->
                            <DataGrid ItemsSource="{Binding CaseItems}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="All"
                                      BorderThickness="1"
                                      BorderBrush="{StaticResource BlueGray200Brush}"
                                      MaxHeight="400"
                                      FontSize="{StaticResource FontSizeBody}"
                                      SelectionUnit="Cell">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="구분" Binding="{Binding Label}" Width="100" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Background" Value="{StaticResource BlueGray50Brush}"/>
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="본건 감정가" Binding="{Binding BaseValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="사례1" Binding="{Binding Case1Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="사례2" Binding="{Binding Case2Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="사례3" Binding="{Binding Case3Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="사례4" Binding="{Binding Case4Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- 우측: 실거래가 + 낙찰통계 + 평가결과 -->
                <StackPanel Grid.Column="2">
                    
                    <!-- 실거래가 영역 -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="💰 실거래가" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="조회" 
                                            Command="{Binding FetchRealTransactionCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"
                                            Margin="0,0,8,0"/>
                                    <!-- E-004: 시세 차트 대안 - 외부 링크 -->
                                    <Button Content="📈 시세보기" 
                                            Command="{Binding OpenPriceTrendSiteCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"
                                            ToolTip="KB부동산 시세 조회"
                                            Margin="0,0,8,0"/>
                                    <Button Content="🔗 rt.molit.go.kr" 
                                            Command="{Binding OpenRealTransactionSiteCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- 실거래가 테이블 (E-003: 테이블 크기 축소 150→120) -->
                            <DataGrid ItemsSource="{Binding RealTransactions}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="All"
                                      BorderThickness="1"
                                      BorderBrush="{StaticResource BlueGray200Brush}"
                                      MaxHeight="120"
                                      FontSize="{StaticResource FontSizeBody}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="거래면적" Binding="{Binding Area, StringFormat=N2}" Width="80"/>
                                    <DataGridTextColumn Header="거래일자" Binding="{Binding TransactionDate, StringFormat=yyyy-MM-dd}" Width="90"/>
                                    <DataGridTextColumn Header="거래금액" Binding="{Binding Amount, StringFormat=N0}" Width="100"/>
                                    <DataGridTextColumn Header="층수" Binding="{Binding Floor}" Width="50"/>
                                    <DataGridTextColumn Header="등기여부" Binding="{Binding IsRegistered}" Width="60"/>
                                    <DataGridCheckBoxColumn Header="적용" Binding="{Binding IsApplied}" Width="50"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <!-- 낙찰통계 영역 - 피드백 반영: 시/군구/동 3×3 매트릭스 -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="📈 낙찰통계" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
                                <!-- 적용낙찰가율 헤더 - 피드백 반영: 가시성 개선 -->
                                <TextBlock Text="적용낙찰가율" 
                                           HorizontalAlignment="Right"
                                           FontSize="{StaticResource FontSizeH4}"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryBrush}"/>
                            </Grid>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 헤더 행 -->
                                <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                    <TextBlock Text="지역통계" Style="{StaticResource LabelStyle}" FontWeight="SemiBold"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                    <TextBlock Text="{Binding RegionName1}" Style="{StaticResource ValueStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="2" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                    <TextBlock Text="{Binding RegionName2}" Style="{StaticResource ValueStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="3" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,0,1">
                                    <TextBlock Text="{Binding RegionName3}" Style="{StaticResource ValueStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>

                                <!-- 1년 평균 -->
                                <Border Grid.Row="1" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                    <TextBlock Text="1년 평균" Style="{StaticResource LabelStyle}"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats1Year_Rate1, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats1Year_Count1, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats1Year_Rate2, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats1Year_Count2, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats1Year_Rate3, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats1Year_Count3, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>

                                <!-- 6개월 평균 -->
                                <Border Grid.Row="2" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                    <TextBlock Text="6개월 평균" Style="{StaticResource LabelStyle}"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats6Month_Rate1, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats6Month_Count1, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats6Month_Rate2, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats6Month_Count2, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats6Month_Rate3, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats6Month_Count3, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>

                                <!-- 3개월 평균 -->
                                <Border Grid.Row="3" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                    <TextBlock Text="3개월 평균" Style="{StaticResource LabelStyle}"/>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats3Month_Rate1, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats3Month_Count1, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats3Month_Rate2, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats3Month_Count2, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats3Month_Rate3, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats3Month_Count3, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>

                                <!-- 적용 낙찰가율 - 피드백 반영: 가시성 대폭 개선 -->
                                <Border Grid.Row="0" Grid.RowSpan="4" Grid.Column="4"
                                        Background="{StaticResource PrimaryBrush}"
                                        CornerRadius="8"
                                        Padding="20,12"
                                        Margin="12,0,0,0"
                                        VerticalAlignment="Center">
                                    <StackPanel>
                                        <TextBlock Text="{Binding AppliedBidRate, StringFormat={}{0:P1}}" 
                                                   FontSize="{StaticResource FontSizeXLarge}"
                                                   FontWeight="Bold"
                                                   Foreground="White"
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding AppliedBidRateDescription}" 
                                                   FontSize="{StaticResource FontSizeSmall}"
                                                   Foreground="White"
                                                   Opacity="0.9"
                                                   HorizontalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   MaxWidth="100"
                                                   TextAlignment="Center"
                                                   Margin="0,6,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 평가결과 영역 -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="✅ 평가결과" Style="{StaticResource SectionHeaderStyle}"/>
                            
                            <!-- 시나리오 1 (하한) - 피드백 반영 -->
                            <Border Background="{StaticResource BlueGray50Brush}" 
                                    CornerRadius="4" 
                                    Padding="16"
                                    Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.ColumnSpan="4" 
                                               Text="시나리오 1 (하한)" 
                                               FontWeight="Bold" FontSize="{StaticResource FontSizeH4}"
                                               Foreground="{StaticResource PrimaryBrush}"
                                               Margin="0,0,0,12"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="낙찰가액" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" 
                                               Text="{Binding Scenario1_Amount, StringFormat={}{0:N0}원}" 
                                               Style="{StaticResource AmountStyle}"
                                               Margin="8,0,24,0"/>
                                    <TextBlock Grid.Row="1" Grid.Column="2" Text="낙찰가율" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Grid.Row="1" Grid.Column="3" 
                                               Text="{Binding Scenario1_Rate, StringFormat={}{0:P1}}" 
                                               Style="{StaticResource ValueStyle}"
                                               Margin="8,0,0,0"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="평가 사유" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"
                                             Text="{Binding Scenario1_Reason, UpdateSourceTrigger=PropertyChanged}"
                                             Margin="8,8,0,0"
                                             Padding="8,4"
                                             FontSize="{StaticResource FontSizeBody}"/>
                                </Grid>
                            </Border>

                            <!-- 시나리오 2 (상한) - 피드백 반영 -->
                            <Border Background="{StaticResource BlueGray50Brush}" 
                                    CornerRadius="4" 
                                    Padding="16"
                                    Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.ColumnSpan="4" 
                                               Text="시나리오 2 (상한)" 
                                               FontWeight="Bold" FontSize="{StaticResource FontSizeH4}"
                                               Foreground="{StaticResource SecondaryBrush}"
                                               Margin="0,0,0,12"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="낙찰가액" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" 
                                               Text="{Binding Scenario2_Amount, StringFormat={}{0:N0}원}" 
                                               Style="{StaticResource AmountStyle}"
                                               Foreground="{StaticResource SecondaryBrush}"
                                               Margin="8,0,24,0"/>
                                    <TextBlock Grid.Row="1" Grid.Column="2" Text="낙찰가율" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Grid.Row="1" Grid.Column="3" 
                                               Text="{Binding Scenario2_Rate, StringFormat={}{0:P1}}" 
                                               Style="{StaticResource ValueStyle}"
                                               Margin="8,0,0,0"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="평가 사유" Style="{StaticResource LabelStyle}" Margin="0,8,0,0"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"
                                             Text="{Binding Scenario2_Reason, UpdateSourceTrigger=PropertyChanged}"
                                             Margin="8,8,0,0"
                                             Padding="8,4"
                                             FontSize="{StaticResource FontSizeBody}"/>
                                </Grid>
                            </Border>

                            <!-- 피드백 반영: 시나리오 3 제거됨 -->
                        </StackPanel>
                    </Border>

                    <!-- 차주별 배당 요약 (피드백 반영: 시나리오 3 컬럼 제거) -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="📊 시나리오별 배당 요약" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,12"/>
                            
                            <DataGrid ItemsSource="{Binding ScenarioSummaryItems}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="All"
                                      BorderThickness="1"
                                      BorderBrush="{StaticResource BlueGray200Brush}"
                                      FontSize="{StaticResource FontSizeBody}"
                                      IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="구분" Binding="{Binding Label}" Width="80">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Background" Value="{StaticResource BlueGray50Brush}"/>
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="하한" Binding="{Binding Scenario1Display}" Width="120">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="상한" Binding="{Binding Scenario2Display}" Width="120">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="합계" Binding="{Binding TotalDisplay}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <!-- QA 관리 섹션 (평가) -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="❓ QA 관리 (평가)" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,12"/>
                            
                            <!-- QA 입력 -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" 
                                         Text="{Binding NewEvalQuestion, UpdateSourceTrigger=PropertyChanged}" 
                                         Padding="10,8"
                                         Margin="0,0,8,0"/>
                                <Button Grid.Column="1" 
                                        Content="QA 등록" 
                                        Command="{Binding AddEvalQACommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="16,8"/>
                            </Grid>

                            <!-- QA 목록 -->
                            <ItemsControl ItemsSource="{Binding EvalQaList}" MaxHeight="150">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource BlueGray50Brush}"
                                                CornerRadius="4"
                                                Padding="12"
                                                Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Question}" 
                                                               TextWrapping="Wrap"
                                                               FontSize="13"/>
                                                    <TextBlock Text="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" 
                                                               FontSize="11"
                                                               Opacity="0.6"
                                                               Margin="0,4,0,0"/>
                                                </StackPanel>
                                                
                                                <Border Grid.Column="1"
                                                        CornerRadius="4"
                                                        Padding="8,4"
                                                        VerticalAlignment="Top">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="#FFC107"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsAnswered}" Value="True">
                                                                    <Setter Property="Background" Value="#4CAF50"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Foreground="White" FontSize="11">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="대기"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsAnswered}" Value="True">
                                                                        <Setter Property="Text" Value="완료"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <!-- 빈 상태 -->
                            <TextBlock Text="등록된 QA가 없습니다"
                                       FontSize="12"
                                       Opacity="0.5"
                                       HorizontalAlignment="Center"
                                       Visibility="{Binding HasNoEvalQA, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Visible}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- 메시지 표시 -->
            <Border Grid.Row="1" 
                    Background="{StaticResource MaterialGreenBgBrush}" 
                    CornerRadius="4" 
                    Padding="12"
                    Margin="0,16,0,0"
                    VerticalAlignment="Bottom"
                    Visibility="{Binding SuccessMessage, Converter={StaticResource NullToVisibilityConverter}}">
                <TextBlock Text="{Binding SuccessMessage}" 
                           Foreground="{StaticResource MaterialGreenDarkBrush}"
                           FontSize="{StaticResource FontSizeBody}"/>
            </Border>

            <Border Grid.Row="1" 
                    Background="{StaticResource MaterialRedBgBrush}" 
                    CornerRadius="4" 
                    Padding="12"
                    Margin="0,16,0,0"
                    VerticalAlignment="Bottom"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="{StaticResource MaterialRedDarkBrush}"
                           FontSize="{StaticResource FontSizeBody}"/>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>

