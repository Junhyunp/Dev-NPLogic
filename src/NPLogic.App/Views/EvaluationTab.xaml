<UserControl x:Class="NPLogic.Views.EvaluationTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:evalViews="clr-namespace:NPLogic.Views.Evaluation"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1400"
             d:DataContext="{d:DesignInstance Type=vm:EvaluationTabViewModel}">

    <UserControl.Resources>
        <!-- 피드백 반영: 공통 스타일 참조로 통일 -->
        <!-- SectionHeader, LabelText, ValueText, AmountText, CardBorder 스타일은 Typography.xaml에서 참조 -->
        
        <!-- 로컬 별칭 (하위 호환성) -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource SectionHeader}">
            <Setter Property="FontSize" Value="{StaticResource FontSizeH4}"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock" BasedOn="{StaticResource LabelText}"/>
        <Style x:Key="ValueStyle" TargetType="TextBlock" BasedOn="{StaticResource ValueText}"/>
        <Style x:Key="AmountStyle" TargetType="TextBlock" BasedOn="{StaticResource AmountText}"/>
        <Style x:Key="CardStyle" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Setter Property="Padding" Value="16"/>
        </Style>

        <!-- DataGrid 공통 스타일 (SeniorRightsView와 동일) -->
        <Style x:Key="SectionDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>
        <Style x:Key="SectionDataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
                <Trigger Property="IsEditing" Value="True">
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SectionDataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
        <Style x:Key="SectionDataGridTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SectionDataGridTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="12,8,12,12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 로딩 오버레이 -->
            <Border Grid.RowSpan="2" 
                    Background="{StaticResource OverlaySemiLightBrush}" 
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                    Panel.ZIndex="100">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                    <TextBlock Text="로딩 중..." Margin="0,12,0,0" 
                               HorizontalAlignment="Center"
                               Style="{StaticResource BodyTextStyle}"/>
                </StackPanel>
            </Border>

            <!-- 헤더: 평가 유형 선택 -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="평가 유형 선택" 
                                   Style="{StaticResource SectionHeaderStyle}"
                                   Margin="0"/>
                    </StackPanel>

                    <!-- 평가 유형 라디오 버튼 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" 
                                Margin="32,0,0,0"
                                VerticalAlignment="Center">
                        <RadioButton Content="1. 아파트" 
                                     IsChecked="{Binding IsApartmentType}"
                                     GroupName="EvaluationType"
                                     Margin="0,0,24,0"
                                     FontSize="{StaticResource FontSizeBody}"/>
                        <RadioButton Content="2. 연립다세대" 
                                     IsChecked="{Binding IsMultiFamilyType}"
                                     GroupName="EvaluationType"
                                     Margin="0,0,24,0"
                                     FontSize="{StaticResource FontSizeBody}"/>
                        <RadioButton Content="3. 공장/창고" 
                                     IsChecked="{Binding IsFactoryType}"
                                     GroupName="EvaluationType"
                                     Margin="0,0,24,0"
                                     FontSize="{StaticResource FontSizeBody}"/>
                        <RadioButton Content="4. 상가/아파트형공장" 
                                     IsChecked="{Binding IsCommercialType}"
                                     GroupName="EvaluationType"
                                     Margin="0,0,24,0"
                                     FontSize="{StaticResource FontSizeBody}"/>
                        <RadioButton Content="5. 주택/근린시설/토지/기타" 
                                     IsChecked="{Binding IsHouseLandType}"
                                     GroupName="EvaluationType"
                                     FontSize="{StaticResource FontSizeBody}"/>
                    </StackPanel>

                    <Button Grid.Column="2" Content="저장"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Padding="12,6"/>
                </Grid>
            </Border>

            <!-- 메인 콘텐츠 -->
            <StackPanel Grid.Row="1">
                    
                    <!-- 사례지도 영역 -->
                    <Border Background="{StaticResource SurfaceColor}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Margin="0,0,0,16">
                        <StackPanel>
                            <!-- 섹션 헤더 -->
                            <Border Background="{StaticResource PrimaryBrush}"
                                    CornerRadius="8,8,0,0"
                                    Padding="16,10">
                                <Grid>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="MapMarkerRadius" Width="18" Height="18"
                                                                 Margin="0,0,6,0" VerticalAlignment="Center" Foreground="White"/>
                                        <TextBlock Text="사례지도"
                                                   Foreground="White"
                                                   FontWeight="SemiBold"
                                                   FontSize="{StaticResource FontSizeH4}"/>
                                    </StackPanel>
                                    <Button Content="가져오기"
                                            Command="{Binding LoadCaseMapCommand}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <!-- 지도 플레이스홀더 (클릭 시 팝업 확대) -->
                            <Border Background="{StaticResource BlueGray50Brush}"
                                    Height="200"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="MapPlaceholder_MouseLeftButtonUp"
                                    ToolTip="클릭하여 확대">
                                <TextBlock Text="소재지 기준으로 위치, 본건기준거래 자동 산정 및 표시"
                                           Style="{StaticResource BodyTextStyle}"
                                           Opacity="0.6"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- 실거래가 영역 -->
                    <Border Background="{StaticResource SurfaceColor}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Margin="0,0,0,16">
                        <StackPanel>
                            <!-- 섹션 헤더 -->
                            <Border Background="{StaticResource PrimaryBrush}"
                                    CornerRadius="8,8,0,0"
                                    Padding="16,10">
                                <Grid>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="CurrencyKrw" Width="18" Height="18"
                                                                 Margin="0,0,6,0" VerticalAlignment="Center" Foreground="White"/>
                                        <TextBlock Text="실거래가"
                                                   Foreground="White"
                                                   FontWeight="SemiBold"
                                                   FontSize="{StaticResource FontSizeH4}"/>
                                    </StackPanel>
                                    <!-- 실거래가는 물건 로드 시 자동 조회 -->
                                </Grid>
                            </Border>

                            <!-- 실거래가 테이블 -->
                            <DataGrid x:Name="RealTransactionGrid"
                                      ItemsSource="{Binding RealTransactions}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      CanUserSortColumns="False"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="All"
                                      BorderThickness="0"
                                      SelectionUnit="Cell"
                                      SelectionMode="Single"
                                      HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                                      VerticalGridLinesBrush="{StaticResource BorderBrush}"
                                      RowHeight="32"
                                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                      MinHeight="60">
                                <DataGrid.ColumnHeaderStyle>
                                    <StaticResource ResourceKey="SectionDataGridColumnHeaderStyle"/>
                                </DataGrid.ColumnHeaderStyle>
                                <DataGrid.CellStyle>
                                    <StaticResource ResourceKey="SectionDataGridCellStyle"/>
                                </DataGrid.CellStyle>
                                <DataGrid.RowStyle>
                                    <StaticResource ResourceKey="SectionDataGridRowStyle"/>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="거래면적" Binding="{Binding Area, StringFormat=N2}" Width="*" IsReadOnly="True"
                                                        ElementStyle="{StaticResource SectionDataGridTextBlockStyle}"/>
                                    <DataGridTextColumn Header="거래일자" Binding="{Binding TransactionDate, StringFormat=yyyy-MM-dd}" Width="*" IsReadOnly="True"
                                                        ElementStyle="{StaticResource SectionDataGridTextBlockStyle}"/>
                                    <DataGridTextColumn Header="거래금액" Binding="{Binding Amount, StringFormat=N0}" Width="*" IsReadOnly="True"
                                                        ElementStyle="{StaticResource SectionDataGridTextBlockStyle}"/>
                                    <DataGridTextColumn Header="층수" Binding="{Binding Floor}" Width="80" IsReadOnly="True"
                                                        ElementStyle="{StaticResource SectionDataGridTextBlockStyle}"/>
                                    <DataGridTextColumn Header="등기여부" Binding="{Binding IsRegistered}" Width="80" IsReadOnly="True"
                                                        ElementStyle="{StaticResource SectionDataGridTextBlockStyle}"/>
                                    <DataGridTemplateColumn Header="적용" Width="60">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsApplied, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                                          IsHitTestVisible="True"
                                                          Focusable="True"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <!-- 유사물건 추천 영역 -->
                    <Border Background="{StaticResource SurfaceColor}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Margin="0,0,0,16">
                        <StackPanel>
                            <!-- 섹션 헤더 -->
                            <Border Background="{StaticResource PrimaryBrush}"
                                    CornerRadius="8,8,0,0"
                                    Padding="16,10">
                                <TextBlock Text="유사물건 추천"
                                           Foreground="White"
                                           FontWeight="SemiBold"
                                           FontSize="{StaticResource FontSizeH4}"/>
                            </Border>

                            <StackPanel Margin="16,12,16,16">
                                <!-- 버튼 행 -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <Button Content="추천 검색"
                                            Command="{Binding RecommendSimilarCasesCommand}"
                                            Style="{StaticResource PrimaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"
                                            Margin="0,0,8,0"/>
                                    <Button Content="사례 적용"
                                            Command="{Binding ApplyRecommendedCaseCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"
                                            Margin="0,0,16,0"/>
                                    <!-- 로딩 표시 -->
                                    <ProgressBar IsIndeterminate="True"
                                                 Width="80" Height="4"
                                                 VerticalAlignment="Center"
                                                 Visibility="{Binding IsRecommendLoading, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}"/>
                                </StackPanel>

                                <!-- 상태 메시지 -->
                                <TextBlock Text="{Binding RecommendStatusMessage}"
                                           Style="{StaticResource LabelStyle}"
                                           Margin="0,0,0,8"
                                           Visibility="{Binding RecommendStatusMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

                                <!-- 추천 결과 테이블 -->
                                <DataGrid ItemsSource="{Binding RecommendedCases}"
                                          SelectedItem="{Binding SelectedRecommendCase, Mode=TwoWay}"
                                          AutoGenerateColumns="False"
                                          CanUserAddRows="False"
                                          CanUserDeleteRows="False"
                                          HeadersVisibility="Column"
                                          GridLinesVisibility="All"
                                          BorderThickness="1"
                                          BorderBrush="{StaticResource BlueGray200Brush}"
                                          MaxHeight="350"
                                          MinHeight="200"
                                          FontSize="{StaticResource FontSizeSmall}"
                                          SelectionMode="Single">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="사건번호" Binding="{Binding CaseNo}" Width="110" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="낙찰일" Binding="{Binding AuctionDate, StringFormat=yyyy-MM-dd}" Width="85" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="용도" Binding="{Binding Usage}" Width="70" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="감정가" Binding="{Binding AppraisalPrice, StringFormat=N0}" Width="100" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="낙찰가" Binding="{Binding WinningPrice, StringFormat=N0}" Width="100" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="낙찰율" Binding="{Binding WinningRateDisplay}" Width="60" IsReadOnly="True">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                    <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="건물면적" Binding="{Binding BuildingArea, StringFormat=N1}" Width="70" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="규칙" Binding="{Binding RuleName}" Width="55" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="거리" Binding="{Binding DistanceDisplay}" Width="60" IsReadOnly="True">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTemplateColumn Header="상세" Width="50">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="상세"
                                                            Command="{Binding DataContext.ShowCaseDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{StaticResource SecondaryButton}"
                                                            Padding="4,2"
                                                            FontSize="{StaticResource FontSizeSmall}"
                                                            ToolTip="상세보기"
                                                            Cursor="Hand"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <!-- 페이지네이션 -->
                                <Grid Margin="0,8,0,0">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <TextBlock Text="표시: " Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding DisplayedCasesCount}" Style="{StaticResource ValueStyle}" VerticalAlignment="Center"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding TotalCasesCount}" Style="{StaticResource ValueStyle}" VerticalAlignment="Center"/>
                                        <TextBlock Text=" 건" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <Button Content="더보기 (+5)"
                                            Command="{Binding LoadMoreCasesCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"
                                            HorizontalAlignment="Right"
                                            Visibility="{Binding HasMoreCases, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 사례평가 그리드 -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="사례평가" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
                                    <TextBlock Text="(직접 입력 가능)" 
                                               Style="{StaticResource LabelStyle}"
                                               FontSize="{StaticResource FontSizeSmall}"
                                               Opacity="0.7"
                                               Margin="8,0,0,0"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="초기화" 
                                            Command="{Binding ClearCaseEvaluationCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"
                                            Margin="0,0,8,0"/>
                                    <Button Content="경매사건검색" 
                                            Command="{Binding SearchAuctionCaseCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="{StaticResource FontSizeBody}"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- 사례평가 테이블 (피드백 반영: 엑셀 기준 구조, 정렬 맞추기) -->
                            <DataGrid ItemsSource="{Binding CaseItems}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="All"
                                      BorderThickness="1"
                                      BorderBrush="{StaticResource BlueGray200Brush}"
                                      MaxHeight="500"
                                      FontSize="{StaticResource FontSizeSmall}"
                                      SelectionUnit="Cell"
                                      HorizontalScrollBarVisibility="Auto">
                                <DataGrid.ColumnHeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                                        <Setter Property="Padding" Value="8,6"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource BlueGray200Brush}"/>
                                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                    </Style>
                                </DataGrid.ColumnHeaderStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="구분" Binding="{Binding Label}" Width="120" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Background" Value="{StaticResource BlueGray50Brush}"/>
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="본건 감정가" Binding="{Binding BaseValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="6,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="사례1" Binding="{Binding Case1Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="6,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Background" Value="#F3E8FF"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="Background" Value="#F3E8FF"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="사례2" Binding="{Binding Case2Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="6,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Background" Value="#F3E8FF"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="Background" Value="#F3E8FF"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="사례3" Binding="{Binding Case3Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="6,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Background" Value="#F3E8FF"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="Background" Value="#F3E8FF"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="사례4" Binding="{Binding Case4Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="6,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Background" Value="#F3E8FF"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Padding" Value="4,2"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="Background" Value="#F3E8FF"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                <!-- 낙찰통계 + 평가결과 -->

                    <!-- 낙찰통계 영역 - 피드백 반영: 시/군구/동 3×3 매트릭스 -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="낙찰통계" Style="{StaticResource SectionHeaderStyle}" Margin="0"/>
                                <!-- 적용낙찰가율 헤더 - 피드백 반영: 가시성 개선 -->
                                <TextBlock Text="적용낙찰가율" 
                                           HorizontalAlignment="Right"
                                           FontSize="{StaticResource FontSizeH4}"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryBrush}"/>
                            </Grid>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 헤더 행 -->
                                <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                    <TextBlock Text="지역통계" Style="{StaticResource LabelStyle}" FontWeight="SemiBold"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                    <TextBlock Text="{Binding RegionName1}" Style="{StaticResource ValueStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="2" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                    <TextBlock Text="{Binding RegionName2}" Style="{StaticResource ValueStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="3" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,0,1">
                                    <TextBlock Text="{Binding RegionName3}" Style="{StaticResource ValueStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>

                                <!-- 1년 평균 -->
                                <Border Grid.Row="1" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                    <TextBlock Text="1년 평균" Style="{StaticResource LabelStyle}"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats1Year_Rate1, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats1Year_Count1, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats1Year_Rate2, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats1Year_Count2, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats1Year_Rate3, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats1Year_Count3, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>

                                <!-- 6개월 평균 -->
                                <Border Grid.Row="2" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                    <TextBlock Text="6개월 평균" Style="{StaticResource LabelStyle}"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats6Month_Rate1, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats6Month_Count1, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats6Month_Rate2, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats6Month_Count2, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats6Month_Rate3, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats6Month_Count3, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>

                                <!-- 3개월 평균 -->
                                <Border Grid.Row="3" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                    <TextBlock Text="3개월 평균" Style="{StaticResource LabelStyle}"/>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats3Month_Rate1, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats3Month_Count1, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats3Month_Rate2, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats3Month_Count2, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Stats3Month_Rate3, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}"/>
                                        <TextBlock Text=" / " Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding Stats3Month_Count3, StringFormat={}{0}건}" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}"/>
                                    </StackPanel>
                                </Border>

                                <!-- 적용 낙찰가율 - 피드백 반영: 가시성 대폭 개선 -->
                                <Border Grid.Row="0" Grid.RowSpan="4" Grid.Column="4"
                                        Background="{StaticResource PrimaryBrush}"
                                        CornerRadius="8"
                                        Padding="20,12"
                                        Margin="12,0,0,0"
                                        VerticalAlignment="Center">
                                    <StackPanel>
                                        <TextBlock Text="{Binding AppliedBidRate, StringFormat={}{0:P1}}" 
                                                   FontSize="{StaticResource FontSizeXLarge}"
                                                   FontWeight="Bold"
                                                   Foreground="White"
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding AppliedBidRateDescription}" 
                                                   FontSize="{StaticResource FontSizeSmall}"
                                                   Foreground="White"
                                                   Opacity="0.9"
                                                   HorizontalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   MaxWidth="100"
                                                   TextAlignment="Center"
                                                   Margin="0,6,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 평가결과 영역 (피드백 반영: 엑셀 구조, 토지/건물평당낙찰가 추가) -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="평가결과" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,12"/>
                            
                            <!-- 평가결과 테이블 (엑셀 스타일) -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 헤더 행 -->
                                <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource BlueGray100Brush}" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,1,0,1" Padding="6,4">
                                    <TextBlock Text="구분" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource BlueGray100Brush}" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,1,0,1" Padding="6,4">
                                    <TextBlock Text="낙찰가액" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="2" Background="{StaticResource BlueGray100Brush}" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,1,0,1" Padding="6,4">
                                    <TextBlock Text="낙찰가율" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="3" Background="{StaticResource BlueGray100Brush}" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,1,0,1" Padding="6,4">
                                    <TextBlock Text="토지평당" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="4" Background="{StaticResource BlueGray100Brush}" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,1,0,1" Padding="6,4">
                                    <TextBlock Text="건물평당" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="5" Background="{StaticResource BlueGray100Brush}" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,1,1,1" Padding="6,4">
                                    <TextBlock Text="평가 사유" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                </Border>

                                <!-- 시나리오 1 행 -->
                                <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource BlueGray50Brush}" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="시나리오 1" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="1" Background="#FFFDE7" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="{Binding Scenario1_Amount, StringFormat={}{0:N0}}" Style="{StaticResource AmountStyle}" Foreground="#D50000" HorizontalAlignment="Right"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="{Binding Scenario1_Rate, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="{Binding Scenario1_LandPerPyung, StringFormat={}{0:N0}}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="4" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="{Binding Scenario1_BuildingPerPyung, StringFormat={}{0:N0}}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="5" Background="#FFFDE7" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="4,4">
                                    <TextBox Text="{Binding Scenario1_Reason, UpdateSourceTrigger=PropertyChanged}" 
                                             BorderThickness="0" Background="Transparent" 
                                             FontSize="{StaticResource FontSizeSmall}"
                                             Foreground="#D50000" FontWeight="Bold"/>
                                </Border>

                                <!-- 시나리오 2 행 -->
                                <Border Grid.Row="2" Grid.Column="0" Background="{StaticResource BlueGray50Brush}" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="시나리오 2" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" Foreground="{StaticResource SecondaryBrush}" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="1" Background="#FFFDE7" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="{Binding Scenario2_Amount, StringFormat={}{0:N0}}" Style="{StaticResource AmountStyle}" Foreground="#D50000" HorizontalAlignment="Right"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="{Binding Scenario2_Rate, StringFormat={}{0:P1}}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="{Binding Scenario2_LandPerPyung, StringFormat={}{0:N0}}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="4" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,0,1" Padding="6,6">
                                    <TextBlock Text="{Binding Scenario2_BuildingPerPyung, StringFormat={}{0:N0}}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="5" Background="#FFFDE7" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="4,4">
                                    <TextBox Text="{Binding Scenario2_Reason, UpdateSourceTrigger=PropertyChanged}" 
                                             BorderThickness="0" Background="Transparent" 
                                             FontSize="{StaticResource FontSizeSmall}"
                                             Foreground="#D50000" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 회수 전략 요약 (신규 추가) -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="회수 전략 요약" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,12"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 시나리오 1 회수 전략 -->
                                <Border Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1" CornerRadius="4">
                                    <StackPanel>
                                        <Border Background="{StaticResource PrimaryBrush}" Padding="8,6">
                                            <TextBlock Text="시나리오 1 회수 전략" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                        </Border>
                                        <Grid Margin="8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            
                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="CF in date" Style="{StaticResource LabelStyle}" Margin="0,0,8,4"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Recovery1_CFDate, StringFormat=yyyy-MM-dd, TargetNullValue=-}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                                            
                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="담보 배당 회수" Style="{StaticResource LabelStyle}" Margin="0,0,8,4"/>
                                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Recovery1_CollateralAmount, StringFormat={}{0:N0}원, TargetNullValue=-}" Style="{StaticResource AmountStyle}" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                                            
                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="경매비용" Style="{StaticResource LabelStyle}" Margin="0,0,8,4"/>
                                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Recovery1_AuctionCost, StringFormat={}{0:N0}원, TargetNullValue=-}" Style="{StaticResource ValueStyle}" Foreground="#D50000" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                                            
                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="신용보증서 회수" Style="{StaticResource LabelStyle}" Margin="0,0,8,4"/>
                                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Recovery1_GuaranteeAmount, StringFormat={}{0:N0}원, TargetNullValue=-}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                                            
                                            <TextBlock Grid.Row="4" Grid.Column="0" Text="회수율 (Ratio)" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" Margin="0,4,8,0"/>
                                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Recovery1_Ratio, StringFormat={}{0:P1}, TargetNullValue=-}" Style="{StaticResource AmountStyle}" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                                
                                <!-- 시나리오 2 회수 전략 -->
                                <Border Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1" CornerRadius="4">
                                    <StackPanel>
                                        <Border Background="{StaticResource SecondaryBrush}" Padding="8,6">
                                            <TextBlock Text="시나리오 2 회수 전략" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                        </Border>
                                        <Grid Margin="8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            
                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="CF in date" Style="{StaticResource LabelStyle}" Margin="0,0,8,4"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Recovery2_CFDate, StringFormat=yyyy-MM-dd, TargetNullValue=-}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                                            
                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="담보 배당 회수" Style="{StaticResource LabelStyle}" Margin="0,0,8,4"/>
                                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Recovery2_CollateralAmount, StringFormat={}{0:N0}원, TargetNullValue=-}" Style="{StaticResource AmountStyle}" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                                            
                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="경매비용" Style="{StaticResource LabelStyle}" Margin="0,0,8,4"/>
                                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Recovery2_AuctionCost, StringFormat={}{0:N0}원, TargetNullValue=-}" Style="{StaticResource ValueStyle}" Foreground="#D50000" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                                            
                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="신용보증서 회수" Style="{StaticResource LabelStyle}" Margin="0,0,8,4"/>
                                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Recovery2_GuaranteeAmount, StringFormat={}{0:N0}원, TargetNullValue=-}" Style="{StaticResource ValueStyle}" HorizontalAlignment="Right" Margin="0,0,0,4"/>
                                            
                                            <TextBlock Grid.Row="4" Grid.Column="0" Text="회수율 (Ratio)" Style="{StaticResource LabelStyle}" FontWeight="SemiBold" Margin="0,4,8,0"/>
                                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Recovery2_Ratio, StringFormat={}{0:P1}, TargetNullValue=-}" Style="{StaticResource AmountStyle}" Foreground="{StaticResource SecondaryBrush}" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 차주별 배당 요약 (피드백 반영: 시나리오 3 컬럼 제거) -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="시나리오별 배당 요약" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,12"/>
                            
                            <DataGrid ItemsSource="{Binding ScenarioSummaryItems}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="All"
                                      BorderThickness="1"
                                      BorderBrush="{StaticResource BlueGray200Brush}"
                                      FontSize="{StaticResource FontSizeBody}"
                                      IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="구분" Binding="{Binding Label}" Width="80">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Background" Value="{StaticResource BlueGray50Brush}"/>
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="시나리오 1" Binding="{Binding Scenario1Display}" Width="120">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="시나리오 2" Binding="{Binding Scenario2Display}" Width="120">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="합계" Binding="{Binding TotalDisplay}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <!-- 인터림 상계/회수 (차주당 첫 번째 물건에만 표시) -->
                    <Border Style="{StaticResource CardStyle}" 
                            Visibility="{Binding IsFirstPropertyInBorrower, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <TextBlock Text="인터림 상계/회수" Style="{StaticResource SectionHeaderStyle}"/>
                                <TextBlock Text="(차주별 합산)" 
                                           Style="{StaticResource LabelStyle}" 
                                           FontSize="{StaticResource FontSizeSmall}" 
                                           Opacity="0.6" 
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"/>
                            </StackPanel>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 회수 정보 -->
                                <Border Grid.Column="0" Background="{StaticResource SuccessLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,8,0">
                                    <StackPanel>
                                        <TextBlock Text="회수 총액" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding InterimTotalRecovery, StringFormat=N0}" 
                                                   Style="{StaticResource AmountStyle}" 
                                                   FontSize="{StaticResource FontSizeH2}"
                                                   Foreground="{StaticResource SuccessBrush}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <StackPanel Margin="0,0,16,0">
                                                <TextBlock Text="원금회수" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeTiny}"/>
                                                <TextBlock Text="{Binding InterimPrincipalRecovery, StringFormat=N0}" Style="{StaticResource ValueStyle}" FontSize="{StaticResource FontSizeBody}"/>
                                            </StackPanel>
                                            <StackPanel>
                                                <TextBlock Text="이자회수" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeTiny}"/>
                                                <TextBlock Text="{Binding InterimInterestRecovery, StringFormat=N0}" Style="{StaticResource ValueStyle}" FontSize="{StaticResource FontSizeBody}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                
                                <!-- 지출 정보 -->
                                <Border Grid.Column="1" Background="{StaticResource WarningLightBrush}" CornerRadius="4" Padding="12" Margin="0,0,8,0">
                                    <StackPanel>
                                        <TextBlock Text="지출 총액" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding InterimTotalAdvance, StringFormat=N0}" 
                                                   Style="{StaticResource AmountStyle}" 
                                                   FontSize="{StaticResource FontSizeH2}"
                                                   Foreground="{StaticResource WarningBrush}"/>
                                        <StackPanel Margin="0,8,0,0">
                                            <TextBlock Text="경매비용" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeTiny}"/>
                                            <TextBlock Text="{Binding InterimAuctionCost, StringFormat=N0}" Style="{StaticResource ValueStyle}" FontSize="{StaticResource FontSizeBody}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                
                                <!-- 순수익 -->
                                <Border Grid.Column="2" Background="{StaticResource PrimaryLightBrush}" CornerRadius="4" Padding="12">
                                    <StackPanel>
                                        <TextBlock Text="순수익" Style="{StaticResource LabelStyle}" FontSize="{StaticResource FontSizeSmall}" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding InterimNetAmount, StringFormat=N0}" 
                                                   Style="{StaticResource AmountStyle}" 
                                                   FontSize="{StaticResource FontSizeH2}"
                                                   FontWeight="Bold"
                                                   Foreground="{StaticResource PrimaryBrush}"/>
                                        <TextBlock Text="회수 - 지출" 
                                                   Style="{StaticResource LabelStyle}" 
                                                   FontSize="{StaticResource FontSizeTiny}" 
                                                   Opacity="0.6"
                                                   Margin="0,8,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

            </StackPanel>

            <!-- 메시지 표시 -->
            <Border Grid.Row="1" 
                    Background="{StaticResource MaterialGreenBgBrush}" 
                    CornerRadius="4" 
                    Padding="12"
                    Margin="0,16,0,0"
                    VerticalAlignment="Bottom"
                    Visibility="{Binding SuccessMessage, Converter={StaticResource NullToVisibilityConverter}}">
                <TextBlock Text="{Binding SuccessMessage}" 
                           Foreground="{StaticResource MaterialGreenDarkBrush}"
                           FontSize="{StaticResource FontSizeBody}"/>
            </Border>

            <Border Grid.Row="1" 
                    Background="{StaticResource MaterialRedBgBrush}" 
                    CornerRadius="4" 
                    Padding="12"
                    Margin="0,16,0,0"
                    VerticalAlignment="Bottom"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="{StaticResource MaterialRedDarkBrush}"
                           FontSize="{StaticResource FontSizeBody}"/>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>

