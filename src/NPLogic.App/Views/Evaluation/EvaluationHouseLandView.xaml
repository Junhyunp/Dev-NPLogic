<UserControl x:Class="NPLogic.Views.Evaluation.EvaluationHouseLandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 좌측: 탐문결과 + 탐문수익가치 -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 사례지도 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="🗺️ 사례지도" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <Button Content="가져오기" 
                                    Command="{Binding LoadCaseMapCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="8,0"
                                    Height="20" MinHeight="0" MinWidth="0"
                                    HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <Border Background="{StaticResource BlueGray50Brush}" 
                                CornerRadius="4"
                                Height="180"
                                Cursor="Hand">
                            <TextBlock Text="주택/근린시설/토지 거래 사례 표시"
                                       Style="{StaticResource BodyTextStyle}"
                                       Opacity="0.6"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Tool Box: 탐문결과 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="📞 탐문결과" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 헤더 -->
                            <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="구분" Style="{StaticResource LabelText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="토지" Style="{StaticResource LabelText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="2" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="건물" Style="{StaticResource LabelText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="3" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,0,1">
                                <TextBlock Text="기계" Style="{StaticResource LabelText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>

                            <!-- 탐문단가 -->
                            <Border Grid.Row="1" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="탐문단가" Style="{StaticResource LabelText}"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="4">
                                <TextBox Text="{Binding InquiryLandUnitPrice, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" 
                                         HorizontalContentAlignment="Right"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="4">
                                <TextBox Text="{Binding InquiryBuildingUnitPrice, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" 
                                         HorizontalContentAlignment="Right"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="4">
                                <TextBox Text="{Binding InquiryMachineryUnitPrice, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" 
                                         HorizontalContentAlignment="Right"/>
                            </Border>

                            <!-- 면적 -->
                            <Border Grid.Row="2" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="면적(평)" Style="{StaticResource LabelText}"/>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding LandAreaPyeong, StringFormat=N2}" Style="{StaticResource ValueText}" HorizontalAlignment="Right"/>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding BuildingAreaPyeong, StringFormat=N2}" Style="{StaticResource ValueText}" HorizontalAlignment="Right"/>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="-" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>

                            <!-- 평가액 -->
                            <Border Grid.Row="3" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="평가액" Style="{StaticResource LabelText}" FontWeight="Bold"/>
                            </Border>
                            <Border Grid.Row="3" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding InquiryLandValue, StringFormat=N0}" Style="{StaticResource ValueText}" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </Border>
                            <Border Grid.Row="3" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding InquiryBuildingValue, StringFormat=N0}" Style="{StaticResource ValueText}" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </Border>
                            <Border Grid.Row="3" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding InquiryMachineryValue, StringFormat=N0}" Style="{StaticResource ValueText}" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </Border>
                        </Grid>

                        <!-- 합계 -->
                        <Border Background="{StaticResource PrimaryBrush}" CornerRadius="4" Padding="12" Margin="0,12,0,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="탐문결과 합계:" Foreground="White" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding TotalInquiryValue, StringFormat=\{0:N0\}}" 
                                           Foreground="White" FontWeight="Bold" FontSize="{StaticResource FontSizeH3}" Margin="8,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Tool Box: 탐문수익가치 (민감도 분석) -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="📊 탐문수익가치 (민감도 분석)" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="기본할인율" Style="{StaticResource LabelText}" Margin="0,0,0,8" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="0" Grid.Column="1" 
                                     Text="{Binding InquiryDiscountRate, StringFormat={}{0:P1}, UpdateSourceTrigger=PropertyChanged}" 
                                     Margin="0,0,0,8" Width="100" HorizontalAlignment="Left"/>
                        </Grid>

                        <!-- 민감도 분석 결과 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="5%" Style="{StaticResource LabelText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="6%" Style="{StaticResource LabelText}" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="2" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,0,1">
                                <TextBlock Text="7%" Style="{StaticResource LabelText}" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding InquiryIncomeValue5, StringFormat=N0}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource PrimaryBrush}" Padding="8,6" Margin="0,0,1,0">
                                <TextBlock Text="{Binding InquiryIncomeValue6, StringFormat=N0}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding InquiryIncomeValue7, StringFormat=N0}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Tool Box: 수익가치 (임대차 정보) -->
                <Border Style="{StaticResource CardBorder}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="💰 수익가치 (임대차 정보)" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>
                        
                        <DataGrid ItemsSource="{Binding LeaseInfoList}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="All"
                                  BorderThickness="1"
                                  BorderBrush="{StaticResource BlueGray200Brush}"
                                  MaxHeight="180"
                                  FontSize="{StaticResource FontSizeSmall}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="임차인" Binding="{Binding TenantName}" Width="80"/>
                                <DataGridTextColumn Header="보증금" Binding="{Binding Deposit, StringFormat=N0}" Width="90"/>
                                <DataGridTextColumn Header="월임대료" Binding="{Binding MonthlyRent, StringFormat=N0}" Width="90"/>
                                <DataGridTextColumn Header="임차개시일" Binding="{Binding LeaseStartDate, StringFormat=yyyy-MM-dd}" Width="90"/>
                                <DataGridTextColumn Header="임차종료일" Binding="{Binding LeaseEndDate, StringFormat=yyyy-MM-dd}" Width="90"/>
                                <DataGridTextColumn Header="확정신고일" Binding="{Binding ConfirmationDate, StringFormat=yyyy-MM-dd}" Width="90"/>
                                <DataGridTextColumn Header="연임대료" Binding="{Binding AnnualRent, StringFormat=N0}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Background="{StaticResource BlueGray50Brush}" CornerRadius="4" Padding="12" Margin="0,12,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="연임대료 합계" Style="{StaticResource LabelText}" FontSize="{StaticResource FontSizeSmall}"/>
                                    <TextBlock Text="{Binding TotalAnnualRent, StringFormat=\{0:N0\}}" 
                                               Style="{StaticResource ValueText}" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="수익가치" Style="{StaticResource LabelText}" FontSize="{StaticResource FontSizeSmall}"/>
                                    <TextBlock Text="{Binding LeaseIncomeValue, StringFormat=\{0:N0\}}" 
                                               Style="{StaticResource ValueText}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 우측: 유사물건 추천 + 낙찰통계 + 사례평가 -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 유사물건 추천 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="🎯 유사물건 추천" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="규칙:" Style="{StaticResource LabelText}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <ComboBox SelectedIndex="{Binding SelectedRuleIndex, Mode=TwoWay}"
                                      Width="70" FontSize="{StaticResource FontSizeBody}" Padding="4,2" Margin="0,0,16,0">
                                <ComboBoxItem Content="1순위"/>
                                <ComboBoxItem Content="2순위"/>
                                <ComboBoxItem Content="3순위"/>
                                <ComboBoxItem Content="전체"/>
                            </ComboBox>
                            <Button Content="추천 검색" 
                                    Command="{Binding RecommendSimilarCasesCommand}"
                                    Style="{StaticResource PrimaryButton}"
                                    Padding="12,4"
                                    Margin="0,0,8,0"/>
                            <Button Content="사례 적용" 
                                    Command="{Binding ApplyRecommendedCaseCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="12,4"/>
                        </StackPanel>
                        
                        <DataGrid ItemsSource="{Binding RecommendedCases}"
                                  SelectedItem="{Binding SelectedRecommendCase, Mode=TwoWay}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="All"
                                  BorderThickness="1"
                                  BorderBrush="{StaticResource BlueGray200Brush}"
                                  MaxHeight="150"
                                  FontSize="{StaticResource FontSizeSmall}"
                                  SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="사건번호" Binding="{Binding CaseNo}" Width="100" IsReadOnly="True"/>
                                <DataGridTextColumn Header="낙찰일" Binding="{Binding AuctionDate, StringFormat=yyyy-MM-dd}" Width="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="용도" Binding="{Binding Usage}" Width="60" IsReadOnly="True"/>
                                <DataGridTextColumn Header="감정가" Binding="{Binding AppraisalPrice, StringFormat=N0}" Width="90" IsReadOnly="True"/>
                                <DataGridTextColumn Header="낙찰가" Binding="{Binding WinningPrice, StringFormat=N0}" Width="90" IsReadOnly="True"/>
                                <DataGridTextColumn Header="낙찰율" Binding="{Binding WinningRateDisplay}" Width="55" IsReadOnly="True"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- 낙찰통계 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="📈 낙찰통계" Style="{StaticResource SectionHeader}" Margin="0"/>
                        </Grid>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="지역통계" Style="{StaticResource LabelText}" FontWeight="SemiBold"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="{Binding RegionName1}" Style="{StaticResource ValueText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="2" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="{Binding RegionName2}" Style="{StaticResource ValueText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="3" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,0,1">
                                <TextBlock Text="{Binding RegionName3}" Style="{StaticResource ValueText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="1년 평균" Style="{StaticResource LabelText}"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats1Year_Display1}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats1Year_Display2}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats1Year_Display3}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="2" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="6개월 평균" Style="{StaticResource LabelText}"/>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats6Month_Display1}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats6Month_Display2}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats6Month_Display3}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="3" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="3개월 평균" Style="{StaticResource LabelText}"/>
                            </Border>
                            <Border Grid.Row="3" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats3Month_Display1}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="3" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats3Month_Display2}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="3" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats3Month_Display3}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="0" Grid.RowSpan="4" Grid.Column="4"
                                    Background="{StaticResource PrimaryBrush}"
                                    CornerRadius="8"
                                    Padding="16,12"
                                    Margin="12,0,0,0"
                                    VerticalAlignment="Center">
                                <StackPanel>
                                    <TextBlock Text="{Binding AppliedBidRate, StringFormat={}{0:P1}}" 
                                               FontSize="{StaticResource FontSizeH2}"
                                               FontWeight="Bold"
                                               Foreground="White"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 사례평가 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="📋 사례평가" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <Button Content="경매사건검색" 
                                    Command="{Binding SearchAuctionCaseCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="12,4"
                                    HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <DataGrid ItemsSource="{Binding CaseItems}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="All"
                                  BorderThickness="1"
                                  BorderBrush="{StaticResource BlueGray200Brush}"
                                  MaxHeight="250"
                                  FontSize="{StaticResource FontSizeSmall}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="구분" Binding="{Binding Label}" Width="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="본건 감정가" Binding="{Binding BaseValue, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="사례1" Binding="{Binding Case1Value, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="사례2" Binding="{Binding Case2Value, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="사례3" Binding="{Binding Case3Value, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="사례4" Binding="{Binding Case4Value, Mode=TwoWay}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- 부동산 탐문내역 -->
                <Border Style="{StaticResource CardBorder}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="📞 부동산 탐문내역" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="부동산명" Style="{StaticResource LabelText}" Margin="0,0,0,8"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding InquiryRealEstateName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="연락처" Style="{StaticResource LabelText}"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding InquiryPhone, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>
                        <TextBox Text="{Binding InquiryContent, UpdateSourceTrigger=PropertyChanged}" 
                                 Margin="0,8,0,0"
                                 Height="60"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
