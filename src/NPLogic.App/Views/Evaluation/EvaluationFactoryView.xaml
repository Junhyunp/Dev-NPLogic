<UserControl x:Class="NPLogic.Views.Evaluation.EvaluationFactoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 좌측: 공시지가 + 지번별 평가 + 기계기구 목록 -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 공시지가 연동 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="📋 공시지가" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <Button Content="조회" 
                                    Command="{Binding FetchOfficialLandPriceCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="12,4"
                                    HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="2023년" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding OfficialLandPrice2023, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" 
                                         IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                <TextBlock Text="2024년" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding OfficialLandPrice2024, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" 
                                         IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="2025년" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding OfficialLandPrice2025, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" 
                                         IsReadOnly="True"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 지번별 평가 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="📍 지번별 평가" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="+ 토지 추가" 
                                        Command="{Binding AddLandParcelCommand}"
                                        CommandParameter="토지"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="12,4"
                                        Margin="0,0,8,0"/>
                                <Button Content="+ 건물 추가" 
                                        Command="{Binding AddLandParcelCommand}"
                                        CommandParameter="건물"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="12,4"/>
                            </StackPanel>
                        </Grid>

                        <!-- 토지 평가 테이블 -->
                        <TextBlock Text="토지" Style="{StaticResource LabelText}" FontWeight="Bold" Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding LandParcels}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="All"
                                  BorderThickness="1"
                                  BorderBrush="{StaticResource BlueGray200Brush}"
                                  MaxHeight="180"
                                  FontSize="{StaticResource FontSizeSmall}"
                                  Margin="0,0,0,16">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="지번번호" Binding="{Binding ParcelNumber}" Width="80"/>
                                <DataGridTextColumn Header="지번주소" Binding="{Binding Address}" Width="*"/>
                                <DataGridTextColumn Header="면적(평)" Binding="{Binding AreaPyeong, StringFormat=N2}" Width="70"/>
                                <DataGridTextColumn Header="평당감정가" Binding="{Binding UnitAppraisal, StringFormat=N0}" Width="80"/>
                                <DataGridTextColumn Header="감정가" Binding="{Binding AppraisalValue, StringFormat=N0}" Width="100"/>
                                <DataGridTextColumn Header="1안 평단가" Binding="{Binding Scenario1UnitPrice, StringFormat=N0}" Width="80"/>
                                <DataGridTextColumn Header="1안 평가액" Binding="{Binding Scenario1Value, StringFormat=N0}" Width="100"/>
                                <DataGridTextColumn Header="2안 평단가" Binding="{Binding Scenario2UnitPrice, StringFormat=N0}" Width="80"/>
                                <DataGridTextColumn Header="2안 평가액" Binding="{Binding Scenario2Value, StringFormat=N0}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- 건물 평가 테이블 -->
                        <TextBlock Text="건물" Style="{StaticResource LabelText}" FontWeight="Bold" Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding BuildingParcels}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="All"
                                  BorderThickness="1"
                                  BorderBrush="{StaticResource BlueGray200Brush}"
                                  MaxHeight="180"
                                  FontSize="{StaticResource FontSizeSmall}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="지번번호" Binding="{Binding ParcelNumber}" Width="80"/>
                                <DataGridTextColumn Header="지번주소" Binding="{Binding Address}" Width="*"/>
                                <DataGridTextColumn Header="면적(평)" Binding="{Binding AreaPyeong, StringFormat=N2}" Width="70"/>
                                <DataGridTextColumn Header="평당감정가" Binding="{Binding UnitAppraisal, StringFormat=N0}" Width="80"/>
                                <DataGridTextColumn Header="감정가" Binding="{Binding AppraisalValue, StringFormat=N0}" Width="100"/>
                                <DataGridTextColumn Header="1안 평단가" Binding="{Binding Scenario1UnitPrice, StringFormat=N0}" Width="80"/>
                                <DataGridTextColumn Header="1안 평가액" Binding="{Binding Scenario1Value, StringFormat=N0}" Width="100"/>
                                <DataGridTextColumn Header="2안 평단가" Binding="{Binding Scenario2UnitPrice, StringFormat=N0}" Width="80"/>
                                <DataGridTextColumn Header="2안 평가액" Binding="{Binding Scenario2Value, StringFormat=N0}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- 지번별 평가 합계 -->
                        <Border Background="{StaticResource BlueGray50Brush}" CornerRadius="4" Padding="12" Margin="0,12,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="토지+건물 감정가 합계" Style="{StaticResource LabelText}" FontSize="{StaticResource FontSizeSmall}"/>
                                    <TextBlock Text="{Binding TotalLandBuildingAppraisal, StringFormat=\{0:N0\}}"
                                               Style="{StaticResource ValueText}" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="1안 평가액 합계" Style="{StaticResource LabelText}" FontSize="{StaticResource FontSizeSmall}"/>
                                    <TextBlock Text="{Binding TotalScenario1Value, StringFormat=\{0:N0\}}"
                                               Style="{StaticResource ValueText}" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="2안 평가액 합계" Style="{StaticResource LabelText}" FontSize="{StaticResource FontSizeSmall}"/>
                                    <TextBlock Text="{Binding TotalScenario2Value, StringFormat=\{0:N0\}}"
                                               Style="{StaticResource ValueText}" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- 기계기구 목록 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️ 기계기구 목록" Style="{StaticResource SectionHeader}" Margin="0"/>
                                <Border Background="{StaticResource WarningBrush}" 
                                        CornerRadius="4" 
                                        Padding="8,2" 
                                        Margin="12,0,0,0"
                                        VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="기계인정율:" Foreground="White" FontSize="{StaticResource FontSizeSmall}"/>
                                        <TextBlock Text="{Binding MachineryRecognitionRate, StringFormat={}{0:P0}}"
                                                   Foreground="White" FontSize="{StaticResource FontSizeSmall}" FontWeight="Bold" Margin="4,0,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="+ 추가" 
                                        Command="{Binding AddMachineryCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="12,4"
                                        Margin="0,0,8,0"/>
                                <Button Content="삭제" 
                                        Command="{Binding DeleteMachineryCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="12,4"/>
                            </StackPanel>
                        </Grid>

                        <DataGrid ItemsSource="{Binding MachineryList}"
                                  SelectedItem="{Binding SelectedMachinery, Mode=TwoWay}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="All"
                                  BorderThickness="1"
                                  BorderBrush="{StaticResource BlueGray200Brush}"
                                  MaxHeight="250"
                                  FontSize="{StaticResource FontSizeSmall}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="번호" Binding="{Binding ItemNumber}" Width="50" IsReadOnly="True"/>
                                <DataGridTextColumn Header="기계기구명" Binding="{Binding MachineryName}" Width="*"/>
                                <DataGridTextColumn Header="제조사" Binding="{Binding Manufacturer}" Width="100"/>
                                <DataGridTextColumn Header="제작일자" Binding="{Binding ManufactureDate, StringFormat=yyyy-MM-dd}" Width="80"/>
                                <DataGridTextColumn Header="수량" Binding="{Binding Quantity}" Width="50"/>
                                <DataGridTextColumn Header="단가" Binding="{Binding UnitPrice, StringFormat=N0}" Width="100"/>
                                <DataGridTextColumn Header="감정가" Binding="{Binding AppraisalValue, StringFormat=N0}" Width="100"/>
                                <DataGridCheckBoxColumn Header="공장1" Binding="{Binding FactoryMortgage1}" Width="50"/>
                                <DataGridCheckBoxColumn Header="공장2" Binding="{Binding FactoryMortgage2}" Width="50"/>
                                <DataGridCheckBoxColumn Header="공장3" Binding="{Binding FactoryMortgage3}" Width="50"/>
                                <DataGridCheckBoxColumn Header="공장4" Binding="{Binding FactoryMortgage4}" Width="50"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- 기계기구 합계 -->
                        <Border Background="{StaticResource BlueGray50Brush}" CornerRadius="4" Padding="12" Margin="0,12,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="기계 감정가 합계" Style="{StaticResource LabelText}" FontSize="{StaticResource FontSizeSmall}"/>
                                    <TextBlock Text="{Binding TotalMachineryAppraisal, StringFormat=\{0:N0\}}"
                                               Style="{StaticResource ValueText}" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="인정 기계 감정가" Style="{StaticResource LabelText}" FontSize="{StaticResource FontSizeSmall}"/>
                                    <TextBlock Text="{Binding RecognizedMachineryValue, StringFormat=\{0:N0\}}"
                                               Style="{StaticResource ValueText}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="기계 평가액 (인정율 적용)" Style="{StaticResource LabelText}" FontSize="{StaticResource FontSizeSmall}"/>
                                    <TextBlock Text="{Binding MachineryEvaluationValue, StringFormat=\{0:N0\}}"
                                               Style="{StaticResource ValueText}" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 우측: 사례지도 + 낙찰통계 + 사례평가 -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 사례지도 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="🗺️ 사례지도" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <Button Content="가져오기" 
                                    Command="{Binding LoadCaseMapCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="8,0"
                                    Height="20" MinHeight="0" MinWidth="0"
                                    HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <Border Background="{StaticResource BlueGray50Brush}" 
                                CornerRadius="4"
                                Height="180"
                                Cursor="Hand">
                            <TextBlock Text="공장/창고 거래 사례 표시"
                                       Style="{StaticResource BodyTextStyle}"
                                       Opacity="0.6"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- 낙찰통계 -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="📈 낙찰통계" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <TextBlock Text="적용낙찰가율"
                                       HorizontalAlignment="Right"
                                       FontSize="{StaticResource FontSizeH4}"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource PrimaryBrush}"/>
                        </Grid>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="지역통계" Style="{StaticResource LabelText}" FontWeight="SemiBold"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="{Binding RegionName1}" Style="{StaticResource ValueText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="2" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,1,1">
                                <TextBlock Text="{Binding RegionName2}" Style="{StaticResource ValueText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="3" Background="{StaticResource BlueGray100Brush}" Padding="8,6" Margin="0,0,0,1">
                                <TextBlock Text="{Binding RegionName3}" Style="{StaticResource ValueText}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="1년 평균" Style="{StaticResource LabelText}"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats1Year_Display1}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats1Year_Display2}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats1Year_Display3}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="2" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="6개월 평균" Style="{StaticResource LabelText}"/>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats6Month_Display1}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats6Month_Display2}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="2" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats6Month_Display3}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="3" Grid.Column="0" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="1,0,1,1" Padding="8,6">
                                <TextBlock Text="3개월 평균" Style="{StaticResource LabelText}"/>
                            </Border>
                            <Border Grid.Row="3" Grid.Column="1" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats3Month_Display1}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="3" Grid.Column="2" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats3Month_Display2}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>
                            <Border Grid.Row="3" Grid.Column="3" BorderBrush="{StaticResource BlueGray200Brush}" BorderThickness="0,0,1,1" Padding="8,6">
                                <TextBlock Text="{Binding Stats3Month_Display3}" Style="{StaticResource ValueText}" HorizontalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="0" Grid.RowSpan="4" Grid.Column="4"
                                    Background="{StaticResource PrimaryBrush}"
                                    CornerRadius="8"
                                    Padding="20,12"
                                    Margin="12,0,0,0"
                                    VerticalAlignment="Center">
                                <StackPanel>
                                    <TextBlock Text="{Binding AppliedBidRate, StringFormat={}{0:P1}}"
                                               FontSize="{StaticResource FontSizeLarge}"
                                               FontWeight="Bold"
                                               Foreground="White"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding AppliedBidRateDescription}"
                                               FontSize="{StaticResource FontSizeSmall}"
                                               Foreground="White"
                                               Opacity="0.9"
                                               HorizontalAlignment="Center"
                                               TextWrapping="Wrap"
                                               MaxWidth="100"
                                               TextAlignment="Center"
                                               Margin="0,6,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 사례평가 (공장/창고용) -->
                <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="📋 사례평가" Style="{StaticResource SectionHeader}" Margin="0"/>
                            <Button Content="경매사건검색" 
                                    Command="{Binding SearchAuctionCaseCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="12,4"
                                    HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <DataGrid ItemsSource="{Binding CaseItems}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="All"
                                  BorderThickness="1"
                                  BorderBrush="{StaticResource BlueGray200Brush}"
                                  MaxHeight="300"
                                  FontSize="{StaticResource FontSizeSmall}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="구분" Binding="{Binding Label}" Width="100" IsReadOnly="True"/>
                                <DataGridTextColumn Header="본건 감정가" Binding="{Binding BaseValue, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="사례1" Binding="{Binding Case1Value, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="사례2" Binding="{Binding Case2Value, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="사례3" Binding="{Binding Case3Value, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="사례4" Binding="{Binding Case4Value, Mode=TwoWay}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- 부동산 탐문내역 -->
                <Border Style="{StaticResource CardBorder}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="📞 부동산 탐문내역" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="부동산명" Style="{StaticResource LabelText}" Margin="0,0,0,8"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding InquiryRealEstateName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="연락처" Style="{StaticResource LabelText}"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding InquiryPhone, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>
                        <TextBox Text="{Binding InquiryContent, UpdateSourceTrigger=PropertyChanged}" 
                                 Margin="0,8,0,0"
                                 Height="60"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
