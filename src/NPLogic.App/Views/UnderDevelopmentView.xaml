<UserControl x:Class="NPLogic.Views.UnderDevelopmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource BackgroundBrush}">
    
    <Grid>
        <StackPanel VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    MaxWidth="500">
            
            <!-- 아이콘 -->
            <Border Background="{StaticResource BlueGray50Brush}"
                    Width="120" Height="120"
                    CornerRadius="60"
                    Margin="0,0,0,32">
                <materialDesign:PackIcon Kind="HammerWrench" 
                                       Width="64" Height="64"
                                       Foreground="{StaticResource PrimaryBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
            </Border>
            
            <!-- 제목 -->
            <TextBlock Text="개발중입니다" 
                       Style="{StaticResource H2TextStyle}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16"/>
            
            <!-- 설명 -->
            <TextBlock Text="이 기능은 현재 개발 중입니다." 
                       Style="{StaticResource BodyTextStyle}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Opacity="0.7"
                       Margin="0,0,0,8"/>
            
            <TextBlock Text="빠른 시일 내에 제공될 예정입니다." 
                       Style="{StaticResource BodyTextStyle}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Opacity="0.7"
                       Margin="0,0,0,32"/>
            
            <!-- 정보 카드 -->
            <Border Background="{StaticResource InfoBrush}"
                    CornerRadius="8"
                    Padding="16"
                    Opacity="0.1">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Information" 
                                           Width="20" Height="20"
                                           Foreground="{StaticResource InfoBrush}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>
                    <TextBlock Text="다른 메뉴를 이용해 주세요." 
                               Style="{StaticResource BodyTextStyle}"
                               Foreground="{StaticResource InfoBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

