<UserControl x:Class="NPLogic.Views.Controls.CaseMapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="600">
    
    <Border Style="{StaticResource CardBorder}" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 헤더 -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🗺️ 사례지도" Style="{StaticResource SectionHeader}" Margin="0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnLoadMap" 
                            Content="가져오기" 
                            Click="BtnLoadMap_Click"
                            Style="{StaticResource SecondaryButton}"
                            Padding="12,4"
                            Margin="0,0,8,0"/>
                    <Button x:Name="BtnExpandMap" 
                            Content="🔍 확대"
                            Click="BtnExpandMap_Click"
                            Style="{StaticResource SecondaryButton}"
                            Padding="12,4"/>
                </StackPanel>
            </Grid>
            
            <!-- 지도 영역 -->
            <Border Grid.Row="1" 
                    x:Name="MapContainer"
                    Background="{StaticResource BlueGray50Brush}" 
                    CornerRadius="4"
                    MinHeight="180"
                    Cursor="Hand"
                    MouseLeftButtonUp="MapContainer_MouseLeftButtonUp">
                <Grid>
                    <!-- 지도 플레이스홀더 (WebView2가 로드되기 전) -->
                    <StackPanel x:Name="MapPlaceholder" 
                                VerticalAlignment="Center" 
                                HorizontalAlignment="Center">
                        <TextBlock Text="소재지 기준으로 위치, 본건기준거래 자동 산정 및 표시"
                                   Style="{StaticResource BodyTextStyle}"
                                   Opacity="0.6"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="📍 본건 + 사례1~4 마커 표시"
                                   Style="{StaticResource BodyTextStyle}"
                                   FontSize="11"
                                   Opacity="0.4"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                    
                    <!-- 지도 마커 범례 -->
                    <Border Background="White" 
                            CornerRadius="4" 
                            Padding="8" 
                            Opacity="0.9"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Margin="8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <Ellipse Width="10" Height="10" Fill="#E53935" Margin="0,0,6,0"/>
                                <TextBlock Text="본건" FontSize="10"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <Ellipse Width="10" Height="10" Fill="#1E88E5" Margin="0,0,6,0"/>
                                <TextBlock Text="사례 (낙찰)" FontSize="10"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" Fill="#43A047" Margin="0,0,6,0"/>
                                <TextBlock Text="사례 (매매)" FontSize="10"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- 확대 안내 -->
                    <TextBlock Text="🔍 클릭하여 확대"
                               Style="{StaticResource BodyTextStyle}"
                               FontSize="11"
                               Opacity="0.5"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Bottom"
                               Margin="0,0,8,8"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
