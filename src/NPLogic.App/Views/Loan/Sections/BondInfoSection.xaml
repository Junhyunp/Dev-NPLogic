<UserControl x:Class="NPLogic.Views.Loan.Sections.BondInfoSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="1600">

    <UserControl.Resources>
        <Style x:Key="HeaderCellStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="{StaticResource FontSizeTiny}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="4,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CellStyle" TargetType="DataGridCell">
            <Setter Property="FontSize" Value="{StaticResource FontSizeTiny}"/>
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
                <Trigger Property="IsEditing" Value="True">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AmountCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Padding" Value="4,0"/>
        </Style>

        <Style x:Key="CenterCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <!-- 합계 행 숨김 스타일 -->
        <Style x:Key="HideOnSummary" TargetType="TextBlock">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSummaryRow}" Value="True">
                    <Setter Property="Visibility" Value="Hidden"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AmountHideOnSummary" TargetType="TextBlock" BasedOn="{StaticResource AmountCellStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSummaryRow}" Value="True">
                    <Setter Property="Visibility" Value="Hidden"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CenterHideOnSummary" TargetType="TextBlock" BasedOn="{StaticResource CenterCellStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSummaryRow}" Value="True">
                    <Setter Property="Visibility" Value="Hidden"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CheckBoxHideOnSummary" TargetType="CheckBox">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSummaryRow}" Value="True">
                    <Setter Property="Visibility" Value="Hidden"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource SurfaceColor}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Margin="0,0,0,16">
        <StackPanel>
            <!-- 섹션 헤더 -->
            <Border Background="{StaticResource PrimaryBrush}"
                    CornerRadius="8,8,0,0"
                    Padding="16,10">
                <StackPanel>
                    <TextBlock Text="채권정보" FontWeight="SemiBold" FontSize="{StaticResource FontSizeH4}" Foreground="White"/>
                    <TextBlock Text="피담보채무범위" FontSize="{StaticResource FontSizeTiny}" Foreground="#B0C4DE"/>
                </StackPanel>
            </Border>

            <!-- DataGrid -->
            <DataGrid ItemsSource="{Binding LoansWithSummary}"
                      SelectedItem="{Binding SelectedLoan}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      RowHeaderWidth="0"
                      GridLinesVisibility="All"
                      HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                      VerticalGridLinesBrush="{StaticResource BorderBrush}"
                      Background="White"
                      BorderThickness="0"
                      MinHeight="60"
                      MaxHeight="250"
                      HorizontalScrollBarVisibility="Auto"
                      ColumnHeaderStyle="{StaticResource HeaderCellStyle}"
                      CellStyle="{StaticResource CellStyle}">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSummaryRow}" Value="True">
                                <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="계좌일련번호(A)" Binding="{Binding AccountSerial}" Width="90" IsReadOnly="True"/>
                    <DataGridTextColumn Header="대출과목(B)" Binding="{Binding LoanType}" Width="120" ElementStyle="{StaticResource HideOnSummary}"/>
                    <DataGridTextColumn Header="계좌번호(D)" Binding="{Binding AccountNumber}" Width="100" ElementStyle="{StaticResource HideOnSummary}"/>
                    <DataGridTextColumn Header="최초대출일(E)" Binding="{Binding InitialLoanDate, StringFormat=yyyy-MM-dd}" Width="85" ElementStyle="{StaticResource HideOnSummary}"/>
                    <DataGridTextColumn Header="최초대출원금(F)" Binding="{Binding InitialLoanAmount, StringFormat=N0}" Width="100" ElementStyle="{StaticResource AmountCellStyle}"/>
                    <DataGridTextColumn Header="대출원금잔액(G)" Binding="{Binding LoanPrincipalBalance, StringFormat=N0}" Width="100" ElementStyle="{StaticResource AmountCellStyle}"/>
                    <DataGridTextColumn Header="가지급금(H)" Binding="{Binding AdvancePayment, StringFormat=N0}" Width="85" ElementStyle="{StaticResource AmountHideOnSummary}"/>
                    <DataGridTextColumn Header="미수이자(I)" Binding="{Binding AccruedInterest, StringFormat=N0}" Width="85" ElementStyle="{StaticResource AmountHideOnSummary}"/>
                    <DataGridTextColumn Header="채권액합계(J)" Binding="{Binding TotalClaimAmount, StringFormat=N0}" Width="100" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Padding" Value="4,0"/>
                                <Setter Property="Background" Value="#C6EFCE"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSummaryRow}" Value="True">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="최종이수일(K)" Binding="{Binding LastInterestDate, StringFormat=yyyy-MM-dd}" Width="85" ElementStyle="{StaticResource HideOnSummary}"/>
                    <DataGridTextColumn Header="정상이자율(L)" Binding="{Binding NormalInterestRate, StringFormat=P1}" Width="75" ElementStyle="{StaticResource CenterHideOnSummary}"/>
                    <DataGridTextColumn Header="연체이자율(M)" Binding="{Binding OverdueInterestRate, StringFormat=P1}" Width="75" ElementStyle="{StaticResource CenterHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="약정서확인" Binding="{Binding HasAgreementDoc, UpdateSourceTrigger=PropertyChanged}" Width="55" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="경매신청서" Binding="{Binding HasAuctionApplication, UpdateSourceTrigger=PropertyChanged}" Width="55" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="경매비용" Binding="{Binding HasAuctionCost, UpdateSourceTrigger=PropertyChanged}" Width="50" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="대위등기비용" Binding="{Binding HasSubrogationRegistrationCost, UpdateSourceTrigger=PropertyChanged}" Width="60" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="1순위피담보(N)" Binding="{Binding HasCollateralPriority1, UpdateSourceTrigger=PropertyChanged}" Width="65" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="2순위피담보(N2)" Binding="{Binding HasCollateralPriority2, UpdateSourceTrigger=PropertyChanged}" Width="70" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="3순위피담보(N3)" Binding="{Binding HasCollateralPriority3, UpdateSourceTrigger=PropertyChanged}" Width="70" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="유효보증서여부(O)" Binding="{Binding HasValidGuarantee, UpdateSourceTrigger=PropertyChanged}" Width="75" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="기대위변제(P)" Binding="{Binding HasPriorSubrogation, UpdateSourceTrigger=PropertyChanged}" Width="65" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="해지부보증여부(Q)" Binding="{Binding IsTerminatedGuarantee, UpdateSourceTrigger=PropertyChanged}" Width="75" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridCheckBoxColumn Header="MCI보증" Binding="{Binding HasMciGuarantee, UpdateSourceTrigger=PropertyChanged}" Width="50" ElementStyle="{StaticResource CheckBoxHideOnSummary}"/>
                    <DataGridTextColumn Header="원금상계(R)" Binding="{Binding PrincipalOffset, StringFormat=N0}" Width="85" ElementStyle="{StaticResource AmountHideOnSummary}"/>
                    <DataGridTextColumn Header="원금회수(S)" Binding="{Binding PrincipalRecovery, StringFormat=N0}" Width="85" ElementStyle="{StaticResource AmountHideOnSummary}"/>
                    <DataGridTextColumn Header="이자상계(T)" Binding="{Binding InterestOffset, StringFormat=N0}" Width="85" ElementStyle="{StaticResource AmountHideOnSummary}"/>
                    <DataGridTextColumn Header="이자회수(U)" Binding="{Binding InterestRecovery, StringFormat=N0}" Width="*" ElementStyle="{StaticResource AmountHideOnSummary}"/>
                </DataGrid.Columns>
            </DataGrid>


        </StackPanel>
    </Border>
</UserControl>
