<UserControl x:Class="NPLogic.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200"
             Loaded="MapView_Loaded"
             Unloaded="MapView_Unloaded">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 상단 헤더 -->
        <Border Grid.Row="0"
                Background="{StaticResource BackgroundBrush}"
                BorderBrush="{StaticResource BlueGray200Brush}"
                BorderThickness="0,0,0,1"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           Text="📄 담보물건 Export"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BtnRefreshAll"
                            Content="🔄 새로고침"
                            Click="RefreshAll_Click"
                            Style="{StaticResource SecondaryButton}"
                            Padding="12,6"
                            Margin="0,0,8,0"/>
                    
                    <Button x:Name="BtnExportExcel"
                            Content="📊 Excel Export"
                            Click="ExportExcel_Click"
                            Style="{StaticResource PrimaryButton}"
                            Padding="12,6"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 4분할 미리보기 영역 (메인 컨텐츠) -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 1. 지적도 (자동 캡처) -->
            <Border Grid.Column="0"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BlueGray200Brush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0"
                               Text="지적도"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,12,0,8"/>
                    
                    <Border Grid.Row="1"
                            Margin="8,0,8,8"
                            Background="#F1F5F9"
                            CornerRadius="4">
                        <Grid>
                            <!-- 이미지 표시 영역 -->
                            <Image x:Name="PreviewMapImage"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            
                            <!-- 로딩 상태 -->
                            <StackPanel x:Name="MapLoadingState"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                <ProgressBar IsIndeterminate="True"
                                             Width="80"
                                             Height="3"
                                             Margin="0,0,0,8"/>
                                <TextBlock Text="로딩 중..."
                                           FontSize="11"
                                           Foreground="{StaticResource BlueGray400Brush}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                            
                            <!-- 빈 상태 (에러 시) -->
                            <StackPanel x:Name="MapEmptyState"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Visibility="Collapsed">
                                <TextBlock Text="🗺️"
                                           FontSize="28"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="지도를 불러올 수 없습니다"
                                           FontSize="11"
                                           Foreground="{StaticResource BlueGray400Brush}"
                                           HorizontalAlignment="Center"/>
                                <Button x:Name="BtnRetryMap"
                                        Content="다시 시도"
                                        Click="RetryMap_Click"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="16,4"
                                        FontSize="11"
                                        Margin="0,8,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
            
            <!-- 2. 로드뷰 (자동 캡처) -->
            <Border Grid.Column="1"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BlueGray200Brush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="4,0,4,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0"
                               Text="로드뷰"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,12,0,8"/>
                    
                    <Border Grid.Row="1"
                            Margin="8,0,8,8"
                            Background="#F1F5F9"
                            CornerRadius="4">
                        <Grid>
                            <!-- 이미지 표시 영역 -->
                            <Image x:Name="PreviewRoadViewImage"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            
                            <!-- 로딩 상태 -->
                            <StackPanel x:Name="RoadViewLoadingState"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                <ProgressBar IsIndeterminate="True"
                                             Width="80"
                                             Height="3"
                                             Margin="0,0,0,8"/>
                                <TextBlock Text="로딩 중..."
                                           FontSize="11"
                                           Foreground="{StaticResource BlueGray400Brush}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                            
                            <!-- 빈 상태 -->
                            <StackPanel x:Name="RoadViewEmptyState"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Visibility="Collapsed">
                                <TextBlock Text="🛣️"
                                           FontSize="28"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="로드뷰를 불러올 수 없습니다"
                                           FontSize="11"
                                           Foreground="{StaticResource BlueGray400Brush}"
                                           HorizontalAlignment="Center"/>
                                <Button x:Name="BtnRetryRoadView"
                                        Content="다시 시도"
                                        Click="RetryRoadView_Click"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="16,4"
                                        FontSize="11"
                                        Margin="0,8,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
            
            <!-- 3. 토지이용계획확인원 (자동 캡처) -->
            <Border Grid.Column="2"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BlueGray200Brush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="4,0,4,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0"
                               Text="토지이용계획확인원"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,12,0,8"/>
                    
                    <Border Grid.Row="1"
                            Margin="8,0,8,8"
                            Background="#F1F5F9"
                            CornerRadius="4">
                        <Grid>
                            <!-- 이미지 표시 영역 -->
                            <Image x:Name="PreviewLandUsePlanImage"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            
                            <!-- 로딩 상태 -->
                            <StackPanel x:Name="LandUsePlanLoadingState"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                <ProgressBar IsIndeterminate="True"
                                             Width="80"
                                             Height="3"
                                             Margin="0,0,0,8"/>
                                <TextBlock Text="토지이음 조회 중..."
                                           FontSize="11"
                                           Foreground="{StaticResource BlueGray400Brush}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                            
                            <!-- 빈 상태 (에러/수동 업로드) -->
                            <StackPanel x:Name="LandUsePlanEmptyState"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Visibility="Collapsed">
                                <TextBlock Text="📋"
                                           FontSize="28"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4"/>
                                <TextBlock x:Name="LandUsePlanStatusText"
                                           Text="주소 정보가 필요합니다"
                                           FontSize="11"
                                           Foreground="{StaticResource BlueGray400Brush}"
                                           HorizontalAlignment="Center"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                                    <Button x:Name="BtnRetryLandUsePlan"
                                            Content="다시 시도"
                                            Click="RetryLandUsePlan_Click"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="11"
                                            Margin="0,0,4,0"/>
                                    <Button x:Name="BtnUploadLandUsePlan"
                                            Content="직접 업로드"
                                            Click="UploadLandUsePlan_Click"
                                            Style="{StaticResource SecondaryButton}"
                                            Padding="12,4"
                                            FontSize="11"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
            
            <!-- 4. 요약건축물대장 -->
            <Border Grid.Column="3"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BlueGray200Brush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0"
                               Text="요약건축물대장"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,12,0,8"/>
                    
                    <Border Grid.Row="1"
                            Margin="8,0,8,8"
                            Background="#F1F5F9"
                            CornerRadius="4">
                        <Grid>
                            <!-- 이미지 표시 영역 -->
                            <Image x:Name="PreviewBuildingRegisterImage"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            
                            <!-- 빈 상태 -->
                            <StackPanel x:Name="BuildingRegisterEmptyState"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                <TextBlock Text="🏢"
                                           FontSize="28"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="업로드 필요"
                                           FontSize="11"
                                           Foreground="{StaticResource BlueGray400Brush}"
                                           HorizontalAlignment="Center"/>
                                <Button x:Name="BtnUploadBuildingRegister"
                                        Content="업로드"
                                        Click="UploadBuildingRegister_Click"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="16,4"
                                        FontSize="11"
                                        Margin="0,8,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
        
        <!-- 숨겨진 WebView2 (캡처용) -->
        <Grid Visibility="Collapsed">
            <wv2:WebView2 x:Name="MapWebView"
                          Width="800" Height="600"
                          DefaultBackgroundColor="Transparent"/>
            <wv2:WebView2 x:Name="RoadViewWebView"
                          Width="800" Height="600"
                          DefaultBackgroundColor="Transparent"/>
            <wv2:WebView2 x:Name="LandUsePlanWebView"
                          Width="1200" Height="1800"
                          DefaultBackgroundColor="White"/>
        </Grid>
    </Grid>
</UserControl>
