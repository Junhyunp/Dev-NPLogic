<UserControl x:Class="NPLogic.Views.LoanDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             xmlns:controls="clr-namespace:NPLogic.Controls;assembly=NPLogic.UI"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1400"
             d:DataContext="{d:DesignInstance Type=vm:LoanDetailViewModel}"
             Loaded="LoanDetailView_Loaded">
    
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 영역 -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Loan 상세" 
                           Style="{StaticResource H2TextStyle}"
                           Margin="0,0,0,8"/>
                <TextBlock Style="{StaticResource BodyTextStyle}" Opacity="0.7">
                    <Run Text="채권정보 관리 및 Loan Cap 계산"/>
                </TextBlock>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Excel 내보내기" 
                        Command="{Binding ExportToExcelCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Padding="20,10"
                        Margin="0,0,8,0"/>
                <Button Content="새로고침" 
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Padding="20,10"/>
            </StackPanel>
        </Grid>

        <!-- 통계 카드 -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="대출 건수" FontSize="12" Opacity="0.7"/>
                    <TextBlock FontSize="24" FontWeight="Bold" Margin="0,4,0,0">
                        <Run Text="{Binding Statistics.TotalCount, FallbackValue=0}"/>
                        <Run Text="건" FontSize="12" FontWeight="Normal"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="대출원금잔액 합계" FontSize="12" Opacity="0.7"/>
                    <TextBlock FontSize="20" FontWeight="Bold" Margin="0,4,0,0">
                        <Run Text="{Binding Statistics.TotalPrincipalBalance, StringFormat=N0, FallbackValue=0}"/>
                        <Run Text="원" FontSize="11" FontWeight="Normal"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="Loan Cap (1안)" FontSize="12" Opacity="0.7"/>
                    <TextBlock FontSize="20" FontWeight="Bold" Margin="0,4,0,0" Foreground="#64B5F6">
                        <Run Text="{Binding Statistics.TotalLoanCap1, StringFormat=N0, FallbackValue=0}"/>
                        <Run Text="원" FontSize="11" FontWeight="Normal"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Column="6" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="Loan Cap (2안)" FontSize="12" Opacity="0.7"/>
                    <TextBlock FontSize="20" FontWeight="Bold" Margin="0,4,0,0" Foreground="#81C784">
                        <Run Text="{Binding Statistics.TotalLoanCap2, StringFormat=N0, FallbackValue=0}"/>
                        <Run Text="원" FontSize="11" FontWeight="Normal"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 메인 컨텐츠 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="250"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 차주 및 대출 목록 (좌측) -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 검색 -->
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center"
                             Padding="12,10">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <TextBlock Text="계좌번호 검색..."
                              VerticalAlignment="Center"
                              Margin="16,0"
                              Opacity="0.5"
                              IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SearchText}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- 차주 목록 -->
                <Border Grid.Row="1" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="12" Margin="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="차주 선택" FontWeight="SemiBold" FontSize="13" Margin="4,0,0,8"/>

                        <ListBox Grid.Row="1"
                                 ItemsSource="{Binding Borrowers}"
                                 SelectedItem="{Binding SelectedBorrower}"
                                 Background="Transparent"
                                 BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="8,6">
                                        <StackPanel>
                                            <TextBlock Text="{Binding BorrowerName}" FontWeight="SemiBold" FontSize="13"/>
                                            <TextBlock Text="{Binding BorrowerNumber}" FontSize="11" Opacity="0.7"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- 대출 목록 -->
                <Border Grid.Row="2" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" FontWeight="SemiBold" FontSize="13" Margin="4,0,0,8">
                            <Run Text="대출 목록 ("/>
                            <Run Text="{Binding Loans.Count, Mode=OneWay, FallbackValue=0}"/>
                            <Run Text="건)"/>
                        </TextBlock>

                        <ListBox Grid.Row="1"
                                 ItemsSource="{Binding Loans}"
                                 SelectedItem="{Binding SelectedLoan}"
                                 Background="Transparent"
                                 BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="8,6" Margin="0,2">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0" Text="{Binding AccountSerial}" FontWeight="SemiBold" FontSize="13"/>
                                            <TextBlock Grid.Row="1" Text="{Binding LoanType}" FontSize="11" Opacity="0.7"/>
                                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,2,0,0">
                                                <TextBlock Text="잔액: " FontSize="11" Opacity="0.7"/>
                                                <TextBlock Text="{Binding LoanPrincipalBalance, StringFormat=N0}" FontSize="11"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <Button Grid.Row="2" Content="+ 대출 추가" 
                                Command="{Binding AddLoanCommand}"
                                Style="{StaticResource SecondaryButton}"
                                Padding="12,8"
                                Margin="0,8,0,0"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- 대출 상세 (우측) -->
            <Border Grid.Column="2" Background="{StaticResource SurfaceColor}" CornerRadius="12" Padding="24">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <!-- 선택된 대출이 없을 때 -->
                        <TextBlock Text="좌측에서 대출을 선택해주세요"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Opacity="0.5">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedLoan}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- 대출 상세 정보 -->
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedLoan}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <!-- 채권정보 섹션 -->
                            <controls:NPExpandableSection Header="채권정보" IsExpanded="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="12"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Grid.Column="0">
                                        <TextBlock Text="계좌일련번호 (A)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.AccountSerial, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="0" Grid.Column="2">
                                        <TextBlock Text="대출과목 (B)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.LoanType, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="0" Grid.Column="4">
                                        <TextBlock Text="대출종류 (C)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.LoanCategory, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="0" Grid.Column="6">
                                        <TextBlock Text="계좌번호 (D)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.AccountNumber, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Grid.Column="0">
                                        <TextBlock Text="최초대출일 (E)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <DatePicker SelectedDate="{Binding SelectedLoan.InitialLoanDate}" Padding="10,6"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Grid.Column="2">
                                        <TextBlock Text="최종이수일 (K)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <DatePicker SelectedDate="{Binding SelectedLoan.LastInterestDate}" Padding="10,6"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Grid.Column="4">
                                        <TextBlock Text="정상이자율 (L)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.NormalInterestRate, StringFormat=P2, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Grid.Column="6">
                                        <TextBlock Text="연체이자율 (M)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.OverdueInterestRate, StringFormat=P2, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>
                                </Grid>
                            </controls:NPExpandableSection>

                            <!-- 금액 정보 섹션 -->
                            <controls:NPExpandableSection Header="금액 정보" IsExpanded="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="대출원금잔액 (G)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.LoanPrincipalBalance, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="가지급금 (H)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.AdvancePayment, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="4">
                                        <TextBlock Text="미수이자 (I)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.AccruedInterest, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="6">
                                        <TextBlock Text="채권액 합계 (J)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.TotalClaimAmount, StringFormat=N0}" Padding="10,8" IsReadOnly="True" Background="{StaticResource BackgroundColor}"/>
                                    </StackPanel>
                                </Grid>
                            </controls:NPExpandableSection>

                            <!-- 체크박스 섹션 -->
                            <controls:NPExpandableSection Header="상태 확인" IsExpanded="True">
                                <WrapPanel>
                                    <CheckBox Content="약정서 확인" IsChecked="{Binding SelectedLoan.HasAgreementDoc}" Margin="0,0,24,8"/>
                                    <CheckBox Content="유효보증서 (O)" IsChecked="{Binding SelectedLoan.HasValidGuarantee}" Margin="0,0,24,8"/>
                                    <CheckBox Content="기 대위변제 (P)" IsChecked="{Binding SelectedLoan.HasPriorSubrogation}" Margin="0,0,24,8"/>
                                    <CheckBox Content="해지부보증 (Q)" IsChecked="{Binding SelectedLoan.IsTerminatedGuarantee}" Margin="0,0,24,8"/>
                                    <CheckBox Content="MCI보증" IsChecked="{Binding SelectedLoan.HasMciGuarantee}" Margin="0,0,24,8"/>
                                </WrapPanel>
                            </controls:NPExpandableSection>

                            <!-- Interim 정보 -->
                            <controls:NPExpandableSection Header="Interim 정보" IsExpanded="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="원금상계 (R)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.PrincipalOffset, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="원금회수 (S)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.PrincipalRecovery, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="4">
                                        <TextBlock Text="이자상계 (T)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.InterestOffset, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="6">
                                        <TextBlock Text="이자회수 (U)" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedLoan.InterestRecovery, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                    </StackPanel>
                                </Grid>
                            </controls:NPExpandableSection>

                            <!-- Loan Cap 계산 섹션 -->
                            <controls:NPExpandableSection Header="Loan Cap 계산" IsExpanded="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 시나리오 1 -->
                                    <Border Grid.Column="0" Background="{StaticResource BackgroundColor}" CornerRadius="8" Padding="16">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="12"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="12"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="12"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Text="시나리오 1안" FontWeight="SemiBold" FontSize="14"/>

                                            <StackPanel Grid.Row="2">
                                                <TextBlock Text="예상배당일" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                                <DatePicker SelectedDate="{Binding Scenario1Date}" Padding="8,4"/>
                                            </StackPanel>

                                            <Grid Grid.Row="4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="연체이자" FontSize="11" Opacity="0.6"/>
                                                    <TextBlock Text="{Binding SelectedLoan.OverdueInterest1, StringFormat=N0, FallbackValue=-}" FontWeight="SemiBold" Margin="0,4,0,0"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Loan Cap" FontSize="11" Opacity="0.6"/>
                                                    <TextBlock Text="{Binding SelectedLoan.LoanCap1, StringFormat=N0, FallbackValue=-}" FontWeight="Bold" FontSize="16" Foreground="#64B5F6" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>
                                    </Border>

                                    <!-- 시나리오 2 -->
                                    <Border Grid.Column="2" Background="{StaticResource BackgroundColor}" CornerRadius="8" Padding="16">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="12"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="12"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="12"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Text="시나리오 2안" FontWeight="SemiBold" FontSize="14"/>

                                            <StackPanel Grid.Row="2">
                                                <TextBlock Text="예상배당일" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                                <DatePicker SelectedDate="{Binding Scenario2Date}" Padding="8,4"/>
                                            </StackPanel>

                                            <Grid Grid.Row="4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="연체이자" FontSize="11" Opacity="0.6"/>
                                                    <TextBlock Text="{Binding SelectedLoan.OverdueInterest2, StringFormat=N0, FallbackValue=-}" FontWeight="SemiBold" Margin="0,4,0,0"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Loan Cap" FontSize="11" Opacity="0.6"/>
                                                    <TextBlock Text="{Binding SelectedLoan.LoanCap2, StringFormat=N0, FallbackValue=-}" FontWeight="Bold" FontSize="16" Foreground="#81C784" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </controls:NPExpandableSection>

                            <!-- 버튼 영역 -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Loan Cap 재계산" 
                                        Command="{Binding RecalculateLoanCapCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="20,10"
                                        Margin="0,0,8,0"/>
                                <Button Content="전체 일괄 계산" 
                                        Command="{Binding RecalculateAllLoanCapsCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="20,10"
                                        Margin="0,0,8,0"/>
                                <Button Content="저장" 
                                        Command="{Binding SaveLoanCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="32,10"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- 로딩 오버레이 -->
        <Border Grid.RowSpan="3" Background="#80000000" CornerRadius="8"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200"/>
                <TextBlock Text="로딩 중..." Foreground="White" Margin="0,16,0,0"/>
            </StackPanel>
        </Border>

        <!-- 에러 메시지 -->
        <Border Grid.Row="2" Background="#E57373" CornerRadius="8" Padding="16" Margin="0,16,0,0"
                VerticalAlignment="Bottom"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="White" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</UserControl>

