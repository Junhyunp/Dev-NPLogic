<UserControl x:Class="NPLogic.Views.PropertyDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1400"
             d:DataContext="{d:DesignInstance Type=vm:PropertyDetailViewModel}"
             Loaded="PropertyDetailView_Loaded">
    
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 영역 -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 뒤로가기 버튼 -->
            <Button Grid.Column="0"
                    Content="← 목록으로"
                    Command="{Binding GoBackCommand}"
                    Style="{StaticResource SecondaryButton}"
                    Padding="12,6"
                    FontSize="{StaticResource FontSizeBody}"
                    Margin="0,0,16,0"/>

            <!-- N-002: 물건 탐색 UI -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                <Button Command="{Binding NavigatePreviousCommand}"
                        IsEnabled="{Binding CanNavigatePrevious}"
                        Style="{StaticResource SecondaryButton}"
                        Padding="8,4"
                        ToolTip="이전 물건 (Ctrl+←)">
                    <TextBlock Text="◀" FontSize="{StaticResource FontSizeTiny}"/>
                </Button>
                
                <Border Background="{StaticResource SurfaceColor}" 
                        CornerRadius="4" 
                        Padding="12,4" 
                        Margin="4,0"
                        ToolTip="현재 물건 위치 (Ctrl+↑: 첫번째, Ctrl+↓: 마지막)">
                    <TextBlock Text="{Binding NavigationInfo}" 
                               FontSize="{StaticResource FontSizeSmall}"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                </Border>
                
                <Button Command="{Binding NavigateNextCommand}"
                        IsEnabled="{Binding CanNavigateNext}"
                        Style="{StaticResource SecondaryButton}"
                        Padding="8,4"
                        ToolTip="다음 물건 (Ctrl+→)">
                    <TextBlock Text="▶" FontSize="{StaticResource FontSizeTiny}"/>
                </Button>
            </StackPanel>

            <!-- 제목 -->
            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                <TextBlock Text="물건 상세" 
                           Style="{StaticResource H2TextStyle}"
                           Margin="0,0,0,4"/>
                <TextBlock Style="{StaticResource BodyTextStyle}"
                           Opacity="0.7">
                    <Run Text="물건번호:"/>
                    <Run Text="{Binding Property.PropertyNumber}" FontWeight="SemiBold"/>
                    <Run Text=" | "/>
                    <Run Text="{Binding Property.AddressFull}"/>
                </TextBlock>
            </StackPanel>

            <!-- 액션 버튼들 - 피드백 반영: 크기 축소, 되돌리기 추가 -->
            <StackPanel Grid.Column="3" Orientation="Horizontal">
                <Button Command="{Binding SaveCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Padding="12,6"
                        Margin="0,0,8,0"
                        ToolTip="변경사항 저장 (Ctrl+S)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" FontSize="{StaticResource FontSizeBody}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="저장" VerticalAlignment="Center" FontSize="{StaticResource FontSizeBody}"/>
                    </StackPanel>
                </Button>
                <!-- 되돌리기 버튼 (피드백 반영: 자동저장 + 되돌리기) -->
                <Button Command="{Binding UndoCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Padding="12,6"
                        Margin="0,0,8,0"
                        IsEnabled="{Binding CanUndo}"
                        ToolTip="이전 상태로 되돌리기 (최대 3단계)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="↩" FontSize="{StaticResource FontSizeBody}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="되돌리기" VerticalAlignment="Center" FontSize="{StaticResource FontSizeBody}"/>
                        <TextBlock Text="{Binding UndoCount, StringFormat=({0})}" 
                                   VerticalAlignment="Center" 
                                   FontSize="{StaticResource FontSizeTiny}" 
                                   Margin="4,0,0,0"
                                   Opacity="0.7"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding RefreshCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Padding="12,6"
                        ToolTip="데이터를 서버에서 다시 불러옵니다 (Ctrl+R)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔄" FontSize="{StaticResource FontSizeBody}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="새로고침" VerticalAlignment="Center" FontSize="{StaticResource FontSizeBody}"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- 탭 컨트롤 -->
        <TabControl Grid.Row="1"
                    SelectedIndex="{Binding SelectedTabIndex}"
                    Background="Transparent"
                    BorderThickness="0"
                    ClipToBounds="True">
            
            <!-- 탭 스타일 -->
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" 
                                        Background="Transparent"
                                        BorderThickness="0,0,0,2"
                                        BorderBrush="Transparent"
                                        Padding="20,12"
                                        Margin="0,0,4,0">
                                    <ContentPresenter ContentSource="Header"
                                                     VerticalAlignment="Center"
                                                     HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource BlueGray50Brush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" Value="{StaticResource FontSizeH4}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                </Style>
            </TabControl.Resources>

            <!-- 피드백 반영: 탭 구조 변경 (S-002) -->
            <!-- 하단 탭: 차주개요, 론, 담보물건, 선순위, 평가, 경공매일정 -->
            <!-- 현금흐름은 상단 메뉴로 이동 예정 -->
            
            <!-- 1. 차주개요 탭 -->
            <TabItem Header="차주개요">
                <local:HomeTab DataContext="{Binding}"/>
            </TabItem>

            <!-- 2. 론 탭 -->
            <TabItem Header="론">
                <local:LoanDetailView DataContext="{Binding}"/>
            </TabItem>

            <!-- 3. 담보물건 탭 -->
            <TabItem Header="담보물건">
                <local:BasicDataTab DataContext="{Binding}"/>
            </TabItem>

            <!-- 4. 선순위 탭 -->
            <TabItem Header="선순위">
                <local:SeniorRightsView DataContext="{Binding SeniorRightsViewModel}"/>
            </TabItem>

            <!-- 5. 평가 탭 -->
            <TabItem Header="평가">
                <local:EvaluationTab DataContext="{Binding EvaluationViewModel}"/>
            </TabItem>

            <!-- 6. 경공매일정 탭 -->
            <TabItem Header="경공매일정">
                <local:AuctionScheduleDetailView DataContext="{Binding AuctionScheduleDetailViewModel}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>

