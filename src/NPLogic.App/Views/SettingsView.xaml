<UserControl x:Class="NPLogic.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
             Loaded="SettingsView_Loaded">
    
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 영역 -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="설정 관리" 
                           Style="{StaticResource H2TextStyle}"
                           Margin="0,0,0,8"/>
                <TextBlock Text="계산 수식, 데이터 매핑, 시스템 환경 설정" 
                           Style="{StaticResource BodyTextStyle}"
                           Opacity="0.7"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="새로고침" 
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Padding="20,10"/>
        </Grid>

        <!-- 탭 컨텐츠 -->
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
            
            <!-- 계산 수식 설정 탭 -->
            <TabItem Header="계산 수식">
                <Grid Margin="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="350"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 수식 목록 -->
                    <Border Grid.Column="0" Background="{StaticResource SurfaceColor}" CornerRadius="8" Padding="16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="수식 목록" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>

                            <ComboBox Grid.Row="1" 
                                      ItemsSource="{Binding FormulaCategories}" 
                                      SelectedItem="{Binding SelectedFormulaCategory}"
                                      Padding="12,10" Margin="0,0,0,12"/>

                            <ListBox Grid.Row="2" 
                                     ItemsSource="{Binding Formulas}" 
                                     SelectedItem="{Binding SelectedFormula}"
                                     Background="Transparent" 
                                     BorderThickness="0"
                                     VirtualizingPanel.ScrollUnit="Pixel">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="10,8" Margin="0,2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding FormulaName}" FontWeight="SemiBold"/>
                                                    <TextBlock Text="{Binding AppliesTo}" FontSize="11" Opacity="0.6"/>
                                                </StackPanel>
                                                <Border Grid.Column="1" 
                                                        Background="{Binding IsActive, Converter={StaticResource BooleanToColorConverter}}"
                                                        CornerRadius="4" Padding="6,2">
                                                    <TextBlock Text="{Binding IsActive, Converter={StaticResource BooleanToTextConverter}}" 
                                                               FontSize="10" Foreground="White"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Button Grid.Row="3" 
                                    Content="+ 새 수식 추가" 
                                    Command="{Binding NewFormulaCommand}"
                                    Style="{StaticResource SecondaryButton}" 
                                    Margin="0,12,0,0"/>
                        </Grid>
                    </Border>

                    <!-- 수식 편집 폼 -->
                    <Border Grid.Column="2" Background="{StaticResource SurfaceColor}" CornerRadius="8" Padding="20">
                        <Grid>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFormulaEditing}" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBlock Text="수식 설정" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,20"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="16"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="16"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="16"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Grid.Column="0">
                                        <TextBlock Text="수식명 *" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding EditFormulaName, UpdateSourceTrigger=PropertyChanged}" 
                                                 Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="0" Grid.Column="2">
                                        <TextBlock Text="적용 대상" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <ComboBox SelectedItem="{Binding EditFormulaAppliesTo}" 
                                                  IsEditable="True"
                                                  Text="{Binding EditFormulaAppliesTo, UpdateSourceTrigger=PropertyChanged}"
                                                  Padding="10,8">
                                            <ComboBoxItem Content="권리분석"/>
                                            <ComboBoxItem Content="평가"/>
                                            <ComboBoxItem Content="XNPV"/>
                                            <ComboBoxItem Content="경매"/>
                                            <ComboBoxItem Content="기타"/>
                                        </ComboBox>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Grid.ColumnSpan="3">
                                        <TextBlock Text="수식 표현식 *" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding EditFormulaExpression, UpdateSourceTrigger=PropertyChanged}" 
                                                 Padding="10,8" 
                                                 AcceptsReturn="True"
                                                 TextWrapping="Wrap"
                                                 Height="100"
                                                 VerticalScrollBarVisibility="Auto"
                                                 FontFamily="Consolas"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="4" Grid.ColumnSpan="3">
                                        <TextBlock Text="설명" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding EditFormulaDescription, UpdateSourceTrigger=PropertyChanged}" 
                                                 Padding="10,8"
                                                 AcceptsReturn="True"
                                                 TextWrapping="Wrap"
                                                 Height="80"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="6" Grid.Column="0" Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding EditFormulaIsActive}" 
                                                  Content="활성화"
                                                  VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>

                                <!-- 수식 도움말 -->
                                <Border Background="#1A2980B9" CornerRadius="6" Padding="12" Margin="0,20,0,0">
                                    <StackPanel>
                                        <TextBlock Text="💡 수식 작성 가이드" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <TextBlock Text="• 변수: {감정가}, {낙찰가}, {선순위합계}, {경매비용} 등" FontSize="12" Opacity="0.8"/>
                                        <TextBlock Text="• 연산자: +, -, *, /, (, )" FontSize="12" Opacity="0.8"/>
                                        <TextBlock Text="• 함수: MAX(), MIN(), IF(), ROUND()" FontSize="12" Opacity="0.8"/>
                                        <TextBlock Text="• 예시: ({낙찰가} - {경매비용} - {선순위합계}) * 0.9" FontSize="12" Opacity="0.8"/>
                                    </StackPanel>
                                </Border>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                                    <Button Content="삭제" 
                                            Command="{Binding DeleteFormulaCommand}"
                                            Style="{StaticResource SecondaryButton}" 
                                            Padding="20,10" 
                                            Margin="0,0,8,0"
                                            Visibility="{Binding SelectedFormula, Converter={StaticResource NullToVisibilityConverter}}"/>
                                    <Button Content="취소" 
                                            Command="{Binding CancelFormulaEditCommand}"
                                            Style="{StaticResource SecondaryButton}" 
                                            Padding="20,10" 
                                            Margin="0,0,8,0"/>
                                    <Button Content="저장" 
                                            Command="{Binding SaveFormulaCommand}"
                                            Style="{StaticResource PrimaryButton}" 
                                            Padding="32,10"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- 수식 미선택 시 -->
                        <TextBlock Text="좌측에서 수식을 선택하거나 새 수식을 추가하세요."
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Opacity="0.5"
                                   Visibility="{Binding IsFormulaEditing, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- 데이터 매핑 설정 탭 -->
            <TabItem Header="데이터 매핑">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                        <Button Content="+ 매핑 추가" 
                                Command="{Binding AddDataMappingCommand}"
                                Style="{StaticResource SecondaryButton}" 
                                Padding="16,8" 
                                Margin="0,0,16,0"/>
                        <TextBlock Text="Excel 컬럼과 DB 필드 간의 매핑 설정" 
                                   VerticalAlignment="Center" 
                                   Opacity="0.6"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding DataMappings}" 
                              SelectedItem="{Binding SelectedDataMapping}"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False"
                              Background="Transparent">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="매핑 키" 
                                                Binding="{Binding SettingKey}" 
                                                Width="200"/>
                            <DataGridTextColumn Header="매핑 값" 
                                                Binding="{Binding SettingValue}" 
                                                Width="300"/>
                            <DataGridTextColumn Header="설명" 
                                                Binding="{Binding Description}" 
                                                Width="*"/>
                            <DataGridTemplateColumn Header="액션" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="저장" 
                                                    Command="{Binding DataContext.SaveDataMappingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    Style="{StaticResource SecondaryButton}" 
                                                    Padding="8,4" 
                                                    Margin="0,0,4,0"/>
                                            <Button Content="삭제" 
                                                    Command="{Binding DataContext.DeleteDataMappingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource SecondaryButton}" 
                                                    Padding="8,4"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- 시스템 환경 설정 탭 -->
            <TabItem Header="시스템 환경">
                <Grid Margin="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 설정 목록 -->
                    <Border Grid.Column="0" Background="{StaticResource SurfaceColor}" CornerRadius="8" Padding="16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="시스템 설정 목록" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>

                            <ListBox Grid.Row="1" 
                                     ItemsSource="{Binding SystemSettings}" 
                                     SelectedItem="{Binding SelectedSystemSetting}"
                                     Background="Transparent" 
                                     BorderThickness="0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="10,8" Margin="0,2">
                                            <StackPanel>
                                                <TextBlock Text="{Binding SettingKey}" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Description}" FontSize="11" Opacity="0.6" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Button Grid.Row="2" 
                                    Content="+ 새 설정 추가" 
                                    Command="{Binding AddSystemSettingCommand}"
                                    Style="{StaticResource SecondaryButton}" 
                                    Margin="0,12,0,0"/>
                        </Grid>
                    </Border>

                    <!-- 설정 상세 -->
                    <Border Grid.Column="2" Background="{StaticResource SurfaceColor}" CornerRadius="8" Padding="20">
                        <Grid>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedSystemSetting}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBlock Text="설정 상세 정보" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,20"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="16"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="16"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Grid.Column="0">
                                        <TextBlock Text="설정 키" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedSystemSetting.SettingKey, UpdateSourceTrigger=PropertyChanged}" 
                                                 Padding="10,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="0" Grid.Column="2">
                                        <TextBlock Text="타입" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <ComboBox SelectedItem="{Binding SelectedSystemSetting.SettingType}" 
                                                  Padding="10,8">
                                            <ComboBoxItem Content="STRING"/>
                                            <ComboBoxItem Content="NUMBER"/>
                                            <ComboBoxItem Content="BOOLEAN"/>
                                            <ComboBoxItem Content="JSON"/>
                                        </ComboBox>
                                    </StackPanel>

                                    <StackPanel Grid.Row="2" Grid.ColumnSpan="3">
                                        <TextBlock Text="설정 값" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedSystemSetting.SettingValue, UpdateSourceTrigger=PropertyChanged}" 
                                                 Padding="10,8"
                                                 AcceptsReturn="True"
                                                 TextWrapping="Wrap"
                                                 Height="100"
                                                 FontFamily="Consolas"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="4" Grid.ColumnSpan="3">
                                        <TextBlock Text="설명" FontSize="11" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox Text="{Binding SelectedSystemSetting.Description, UpdateSourceTrigger=PropertyChanged}" 
                                                 Padding="10,8"
                                                 AcceptsReturn="True"
                                                 TextWrapping="Wrap"
                                                 Height="80"/>
                                    </StackPanel>
                                </Grid>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                                    <Button Content="삭제" 
                                            Command="{Binding DeleteSystemSettingCommand}"
                                            CommandParameter="{Binding SelectedSystemSetting}"
                                            Style="{StaticResource SecondaryButton}" 
                                            Padding="20,10" 
                                            Margin="0,0,8,0"/>
                                    <Button Content="저장" 
                                            Command="{Binding SaveSystemSettingCommand}"
                                            Style="{StaticResource PrimaryButton}" 
                                            Padding="32,10"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- 설정 미선택 시 -->
                        <TextBlock Text="좌측에서 설정을 선택하거나 새 설정을 추가하세요."
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Opacity="0.5">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedSystemSetting}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- 로딩 오버레이 -->
        <Border Grid.RowSpan="2" Background="#80000000" CornerRadius="8"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200"/>
                <TextBlock Text="로딩 중..." Foreground="White" Margin="0,16,0,0"/>
            </StackPanel>
        </Border>

        <!-- 에러 메시지 -->
        <Border Grid.Row="1" 
                Background="#FFEBEE" 
                CornerRadius="8" 
                Padding="16" 
                Margin="0,0,0,16"
                VerticalAlignment="Top"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#C62828"/>
        </Border>
    </Grid>
</UserControl>

