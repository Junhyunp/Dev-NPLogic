<Window x:Class="NPLogic.Views.ImagePopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="{Binding Title}"
        Width="1000"
        Height="700"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 헤더 -->
        <Border Grid.Row="0" 
                Background="{StaticResource PrimaryBrush}" 
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Title}" 
                               FontSize="{StaticResource FontSizeH3}" 
                               FontWeight="Bold" 
                               Foreground="White"/>
                    <TextBlock Text="{Binding SubTitle}" 
                               FontSize="{StaticResource FontSizeBody}" 
                               Foreground="{StaticResource TextWhite80Brush}"
                               Margin="0,4,0,0"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                
                <Button Grid.Column="1"
                        Content="✕"
                        Click="CloseButton_Click"
                        Style="{StaticResource IconButton}"
                        Foreground="White"
                        FontSize="{StaticResource FontSizeH2}"
                        Width="36" Height="36"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>
        
        <!-- 콘텐츠 영역 -->
        <Grid Grid.Row="1" Margin="16">
            <!-- 지도 타입 -->
            <Grid x:Name="MapContainer" Visibility="Collapsed">
                <wv2:WebView2 x:Name="PopupMapWebView"
                              DefaultBackgroundColor="Transparent"/>
            </Grid>
            
            <!-- 이미지 타입 -->
            <ScrollViewer x:Name="ImageContainer" 
                          Visibility="Collapsed"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                <Image x:Name="PopupImage" 
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
            </ScrollViewer>
            
            <!-- 로딩 오버레이 -->
            <Border x:Name="LoadingOverlay"
                    Background="{StaticResource CardAlt2Brush}"
                    Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="로딩 중..."
                               FontSize="{StaticResource FontSizeH3}"
                               Foreground="{StaticResource TextMutedBrush}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
