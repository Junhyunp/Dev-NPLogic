<UserControl x:Class="NPLogic.Views.ProgramManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPLogic.Views"
             xmlns:vm="clr-namespace:NPLogic.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:ProgramManagementViewModel}"
             Loaded="ProgramManagementView_Loaded">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <Border Grid.Row="0" 
                Background="{StaticResource PrimaryBrush}"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="← 뒤로"
                        Command="{Binding GoBackCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Foreground="White"
                        Background="Transparent"
                        BorderBrush="#80FFFFFF"
                        Padding="12,6"
                        Margin="0,0,16,0"/>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="프로그램 관리" 
                               Foreground="White"
                               FontSize="20"
                               FontWeight="Bold"/>
                    <TextBlock Text="프로그램 생성, 수정, PM 배정" 
                               Foreground="#B0FFFFFF"
                               FontSize="12"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="새 프로그램 등록"
                            Command="{Binding ShowAddFormCommand}"
                            Style="{StaticResource PrimaryButton}"
                            Background="White"
                            Foreground="{StaticResource PrimaryBrush}"
                            Padding="16,8"
                            Margin="0,0,8,0"/>
                    <Button Content="새로고침"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Foreground="White"
                            Background="Transparent"
                            BorderBrush="#80FFFFFF"
                            Padding="16,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 메인 콘텐츠 -->
        <Grid Grid.Row="1">
            <!-- 프로그램 목록 -->
            <Border Background="{StaticResource BackgroundBrush}"
                    CornerRadius="8">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="8" ShadowDepth="2"/>
                </Border.Effect>

                <DataGrid ItemsSource="{Binding Programs}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="All"
                          HorizontalGridLinesBrush="{StaticResource TableBorderBrush}"
                          VerticalGridLinesBrush="{StaticResource TableBorderBrush}"
                          HeadersVisibility="Column"
                          Background="Transparent"
                          BorderThickness="0"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="프로그램명" 
                                          Binding="{Binding ProgramName}" 
                                          Width="150"
                                          IsReadOnly="True"/>
                        
                        <DataGridTextColumn Header="담당팀" 
                                          Binding="{Binding Team}" 
                                          Width="100"
                                          IsReadOnly="True"/>
                        
                        <DataGridTextColumn Header="회계법인" 
                                          Binding="{Binding AccountingFirm}" 
                                          Width="120"
                                          IsReadOnly="True"/>
                        
                        <DataGridTextColumn Header="차주수" 
                                          Binding="{Binding BorrowerCount}" 
                                          Width="80"
                                          IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="물건수" 
                                          Binding="{Binding PropertyCount}" 
                                          Width="80"
                                          IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="Cut-off" 
                                          Binding="{Binding CutOffDate, StringFormat=yyyy-MM-dd}" 
                                          Width="100"
                                          IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTextColumn Header="입찰일" 
                                          Binding="{Binding BidDate, StringFormat=yyyy-MM-dd}" 
                                          Width="100"
                                          IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <DataGridTemplateColumn Header="상태" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="inactive">
                                                        <Setter Property="Background" Value="{StaticResource BlueGray300Brush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="completed">
                                                        <Setter Property="Background" Value="{StaticResource InfoBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Foreground="White" FontSize="11">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="활성"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="inactive">
                                                            <Setter Property="Text" Value="비활성"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="completed">
                                                            <Setter Property="Text" Value="완료"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <DataGridTemplateColumn Header="작업" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="수정"
                                                Command="{Binding DataContext.ShowEditFormCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource PrimaryButton}"
                                                Padding="8,4"
                                                FontSize="11"
                                                Margin="0,0,4,0"/>
                                        <Button Content="삭제"
                                                Command="{Binding DataContext.DeleteProgramCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource SecondaryButton}"
                                                Background="{StaticResource ErrorBrush}"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Padding="8,4"
                                                FontSize="11"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BlueGray50Brush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                            <Setter Property="Foreground" Value="{StaticResource DeepNavy700Brush}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BlueGray200Brush}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BlueGray100Brush}"/>
                            <Setter Property="Padding" Value="8"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{StaticResource DeepNavy700Brush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Border>

            <!-- 폼 오버레이 -->
            <Grid Visibility="{Binding IsFormVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                  Background="#80000000">
                <Border Background="White"
                        CornerRadius="8"
                        Padding="32"
                        MaxWidth="900"
                        MaxHeight="700"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="20" ShadowDepth="5"/>
                    </Border.Effect>
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 폼 헤더 -->
                        <Grid Grid.Row="0" Margin="0,0,0,24">
                            <TextBlock Text="{Binding FormTitle}"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource DeepNavy700Brush}"/>
                            <Button Content="✕"
                                    HorizontalAlignment="Right"
                                    Command="{Binding CloseFormCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="8,4"
                                    FontSize="16"/>
                        </Grid>

                        <!-- 메인 컨텐츠: 2열 레이아웃 -->
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="350"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- 좌측: 프로그램 정보 입력 -->
                            <StackPanel Grid.Column="0">
                                <!-- 프로그램명 -->
                                <TextBlock Text="프로그램명 *" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Margin="0,0,0,8"/>
                                <TextBox Text="{Binding FormProgramName, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource NPTextBoxStyle}"
                                         Margin="0,0,0,16"/>

                                <!-- 담당팀 -->
                                <TextBlock Text="담당팀" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Margin="0,0,0,8"/>
                                <TextBox Text="{Binding FormTeam, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource NPTextBoxStyle}"
                                         Margin="0,0,0,16"/>

                                <!-- 회계법인 -->
                                <TextBlock Text="담당 회계법인" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Margin="0,0,0,8"/>
                                <TextBox Text="{Binding FormAccountingFirm, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource NPTextBoxStyle}"
                                         Margin="0,0,0,16"/>

                                <!-- 날짜 -->
                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Cut-off 날짜" 
                                                   Style="{StaticResource CaptionTextStyle}"
                                                   Margin="0,0,0,8"/>
                                        <DatePicker SelectedDate="{Binding FormCutOffDate}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="입찰일" 
                                                   Style="{StaticResource CaptionTextStyle}"
                                                   Margin="0,0,0,8"/>
                                        <DatePicker SelectedDate="{Binding FormBidDate}"/>
                                    </StackPanel>
                                </Grid>

                                <!-- PM 배정 -->
                                <TextBlock Text="담당 PM" 
                                           Style="{StaticResource CaptionTextStyle}"
                                           Margin="0,0,0,8"/>
                                <ComboBox ItemsSource="{Binding PmUsers}"
                                          SelectedItem="{Binding FormSelectedPM}"
                                          DisplayMemberPath="Name"
                                          Style="{StaticResource NPComboBox}"
                                          Margin="0,0,0,16"/>
                            </StackPanel>

                            <!-- 구분선 -->
                            <Border Grid.Column="1" 
                                    Width="1" 
                                    Background="{StaticResource BlueGray200Brush}"
                                    HorizontalAlignment="Center"/>

                            <!-- 우측: 데이터디스크 업로드 (선택사항) -->
                            <Grid Grid.Column="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- 헤더 -->
                                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                                    <TextBlock Text="데이터디스크 업로드 (선택사항)" 
                                               Style="{StaticResource H4TextStyle}"
                                               Margin="0,0,0,4"/>
                                    <TextBlock Text="엑셀 파일을 업로드하여 물건 목록을 자동 생성합니다" 
                                               Style="{StaticResource CaptionTextStyle}"
                                               Opacity="0.7"/>
                                </StackPanel>

                                <!-- 업로드 영역 -->
                                <Border Grid.Row="1"
                                        Background="{StaticResource BlueGray50Brush}"
                                        BorderBrush="{StaticResource BlueGray300Brush}"
                                        BorderThickness="2"
                                        CornerRadius="8"
                                        AllowDrop="True"
                                        Drop="DataDiskDropZone_Drop"
                                        DragOver="DataDiskDropZone_DragOver">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasDataDiskFile}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
                                                    <Setter Property="Background" Value="#F0FDF4"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <Grid>
                                        <!-- 파일 미선택 상태 -->
                                        <StackPanel HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"
                                                    Visibility="{Binding HasDataDiskFile, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                            <TextBlock Text="📁" 
                                                       FontSize="36"
                                                       HorizontalAlignment="Center"
                                                       Margin="0,0,0,12"/>
                                            <TextBlock Text="Excel 파일을 드래그하거나" 
                                                       Style="{StaticResource BodyTextStyle}"
                                                       HorizontalAlignment="Center"
                                                       Margin="0,0,0,4"/>
                                            <Button Content="파일 선택" 
                                                    Command="{Binding SelectDataDiskFileCommand}"
                                                    Style="{StaticResource SecondaryButton}"
                                                    Padding="16,8"
                                                    Margin="0,8,0,0"/>
                                            <TextBlock Text=".xlsx, .xls 지원" 
                                                       Style="{StaticResource CaptionTextStyle}"
                                                       Opacity="0.5"
                                                       HorizontalAlignment="Center"
                                                       Margin="0,12,0,0"/>
                                        </StackPanel>

                                        <!-- 파일 선택됨 상태 -->
                                        <StackPanel Margin="16"
                                                    Visibility="{Binding HasDataDiskFile, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <!-- 파일 정보 -->
                                            <Border Background="{StaticResource SuccessBrush}"
                                                    CornerRadius="6"
                                                    Padding="12"
                                                    Margin="0,0,0,12">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Text="✓" 
                                                               FontSize="16"
                                                               Foreground="White"
                                                               FontWeight="Bold"
                                                               VerticalAlignment="Center"
                                                               Margin="0,0,10,0"/>
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="파일 선택됨" 
                                                                   Foreground="White"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="12"/>
                                                        <TextBlock Text="{Binding DataDiskFileName}" 
                                                                   Foreground="White"
                                                                   Opacity="0.9"
                                                                   FontSize="11"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                    <Button Grid.Column="2"
                                                            Content="✕" 
                                                            Command="{Binding CancelDataDiskFileCommand}"
                                                            Background="Transparent"
                                                            Foreground="White"
                                                            BorderThickness="0"
                                                            Padding="6,2"
                                                            FontSize="14"
                                                            FontWeight="Bold"
                                                            Cursor="Hand"
                                                            ToolTip="파일 선택 취소"/>
                                                </Grid>
                                            </Border>

                                            <!-- 시트 선택 모드 -->
                                            <StackPanel Visibility="{Binding ShowSheetSelection, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock Text="📋 업로드할 시트 선택" 
                                                           Style="{StaticResource CaptionTextStyle}"
                                                           FontWeight="SemiBold"
                                                           Margin="0,0,0,8"/>
                                                <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                                                    <ItemsControl ItemsSource="{Binding AvailableSheets}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <StackPanel/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Background="White"
                                                                        CornerRadius="4"
                                                                        Padding="8,6"
                                                                        Margin="0,0,0,4"
                                                                        Cursor="Hand"
                                                                        MouseLeftButtonUp="SheetItem_Click">
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="Auto"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                            <ColumnDefinition Width="Auto"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        
                                                                        <CheckBox IsChecked="{Binding IsSelected}" 
                                                                                  VerticalAlignment="Center"
                                                                                  Margin="0,0,8,0"/>
                                                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                            <TextBlock Text="{Binding Name}" 
                                                                                       FontWeight="SemiBold"
                                                                                       FontSize="12"/>
                                                                            <TextBlock FontSize="10" Opacity="0.7">
                                                                                <Run Text="{Binding RowCount}"/>
                                                                                <Run Text="행"/>
                                                                            </TextBlock>
                                                                        </StackPanel>
                                                                        <Border Grid.Column="2"
                                                                                Background="{StaticResource PrimaryBrush}"
                                                                                CornerRadius="3"
                                                                                Padding="6,2"
                                                                                VerticalAlignment="Center"
                                                                                Visibility="{Binding SheetTypeDisplay, Converter={StaticResource NullToVisibilityConverter}}">
                                                                            <TextBlock Text="{Binding SheetTypeDisplay}" 
                                                                                       Foreground="White"
                                                                                       FontSize="9"
                                                                                       FontWeight="SemiBold"/>
                                                                        </Border>
                                                                    </Grid>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </ScrollViewer>
                                            </StackPanel>

                                            <!-- 단일 시트 모드 -->
                                            <StackPanel Visibility="{Binding ShowSheetSelection, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                <!-- 데이터 요약 -->
                                                <Border Background="White"
                                                        CornerRadius="6"
                                                        Padding="12"
                                                        Margin="0,0,0,12">
                                                    <StackPanel>
                                                        <Grid Margin="0,0,0,8">
                                                            <TextBlock Text="총 데이터 행" Style="{StaticResource CaptionTextStyle}"/>
                                                            <TextBlock HorizontalAlignment="Right" FontWeight="SemiBold">
                                                                <Run Text="{Binding DataDiskTotalRows}"/>
                                                                <Run Text="행" FontWeight="Normal" FontSize="11"/>
                                                            </TextBlock>
                                                        </Grid>
                                                        <Grid>
                                                            <TextBlock Text="인식된 컬럼" Style="{StaticResource CaptionTextStyle}"/>
                                                            <TextBlock HorizontalAlignment="Right" FontWeight="SemiBold">
                                                                <Run Text="{Binding DataDiskColumns.Count, Mode=OneWay}"/>
                                                                <Run Text="개" FontWeight="Normal" FontSize="11"/>
                                                            </TextBlock>
                                                        </Grid>
                                                    </StackPanel>
                                                </Border>

                                                <!-- 컬럼 목록 -->
                                                <TextBlock Text="컬럼 목록" 
                                                           Style="{StaticResource CaptionTextStyle}"
                                                           FontWeight="SemiBold"
                                                           Margin="0,0,0,6"/>
                                                <ScrollViewer MaxHeight="80" VerticalScrollBarVisibility="Auto">
                                                    <ItemsControl ItemsSource="{Binding DataDiskColumns}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <WrapPanel/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border Background="{StaticResource BlueGray200Brush}"
                                                                        CornerRadius="3"
                                                                        Padding="6,2"
                                                                        Margin="0,0,4,4">
                                                                    <TextBlock Text="{Binding}" FontSize="10"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </ScrollViewer>
                                            </StackPanel>

                                            <!-- 다른 파일 선택 -->
                                            <Button Content="다른 파일 선택"
                                                    Command="{Binding SelectDataDiskFileCommand}"
                                                    Style="{StaticResource SecondaryButton}"
                                                    Padding="12,6"
                                                    FontSize="11"
                                                    Margin="0,12,0,0"
                                                    HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Grid>

                        <!-- 버튼 영역 -->
                        <Grid Grid.Row="2" Margin="0,24,0,0">
                            <!-- 에러 메시지 -->
                            <TextBlock Text="{Binding DataDiskErrorMessage}" 
                                       Foreground="{StaticResource ErrorBrush}"
                                       FontSize="12"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding DataDiskErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="취소"
                                        Command="{Binding CloseFormCommand}"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="16,8"
                                        Margin="0,0,8,0"/>
                                <Button Content="저장"
                                        Command="{Binding SaveProgramCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="24,8"
                                        Visibility="{Binding HasDataDiskFile, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                                <Button Content="저장 및 물건 생성"
                                        Command="{Binding SaveProgramWithDataCommand}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="24,8"
                                        Visibility="{Binding HasDataDiskFile, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <!-- 로딩 오버레이 -->
            <Grid Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                  Background="#80FFFFFF">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                    <TextBlock Text="{Binding ProgressText, FallbackValue='처리 중...'}" 
                               Style="{StaticResource BodyTextStyle}"
                               HorizontalAlignment="Center"
                               FontWeight="SemiBold"
                               Margin="0,16,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>

