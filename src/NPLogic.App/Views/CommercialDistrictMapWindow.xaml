<Window x:Class="NPLogic.Views.CommercialDistrictMapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="상권 분석 지도"
        Height="700" Width="1100"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded"
        Closing="Window_Closing">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- 지도 영역 -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 상단 컨트롤 패널 -->
            <Border Grid.Row="0"
                    Background="{StaticResource BackgroundBrush}"
                    BorderBrush="{StaticResource BlueGray200Brush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 물건 정보 -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="MapMarker" 
                                                 Width="20" Height="20"
                                                 Foreground="{StaticResource PrimaryBrush}"
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,8,0"/>
                        <TextBlock x:Name="TxtPropertyInfo"
                                   Text="물건 정보"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- 반경 선택 및 버튼 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="반경:"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"
                                   Foreground="#64748B"/>
                        <ComboBox x:Name="CmbRadius"
                                  Width="100"
                                  SelectedIndex="1"
                                  SelectionChanged="CmbRadius_SelectionChanged">
                            <ComboBoxItem Content="200m"/>
                            <ComboBoxItem Content="500m"/>
                            <ComboBoxItem Content="1km"/>
                            <ComboBoxItem Content="2km"/>
                        </ComboBox>

                        <Button Content="새로고침"
                                Style="{StaticResource SecondaryButton}"
                                Padding="12,6"
                                Margin="16,0,0,0"
                                Click="BtnRefresh_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 지도 WebView -->
            <Grid Grid.Row="1">
                <wv2:WebView2 x:Name="MapWebView"
                              DefaultBackgroundColor="Transparent"/>

                <!-- 로딩 오버레이 -->
                <Border x:Name="LoadingOverlay"
                        Background="#F0F5F9"
                        Visibility="Visible">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True"
                                     Width="200"
                                     Height="4"
                                     Margin="0,0,0,16"/>
                        <TextBlock Text="지도 로딩 중..."
                                   FontSize="14"
                                   Foreground="#64748B"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- API 키 없음 오버레이 -->
                <Border x:Name="NoApiKeyOverlay"
                        Background="#1A2744"
                        Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="🗺️"
                                   FontSize="48"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,16"/>
                        <TextBlock Text="API 키가 필요합니다"
                                   FontSize="18"
                                   Foreground="White"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="카카오맵 API 키를 appsettings.json에 설정해주세요."
                                   FontSize="12"
                                   Foreground="#94A3B8"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"
                                   MaxWidth="300"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- 우측 상권 정보 패널 -->
        <Border Grid.Column="1"
                Background="{StaticResource SurfaceColor}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 패널 헤더 -->
                <Border Grid.Row="0"
                        Background="{StaticResource BlueGray50Brush}"
                        Padding="16,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="📊 상권 분석 정보"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   VerticalAlignment="Center"/>

                        <Button Grid.Column="1"
                                Content="Excel 내보내기"
                                Style="{StaticResource SecondaryButton}"
                                Padding="8,4"
                                FontSize="11"
                                Click="BtnExportExcel_Click"/>
                    </Grid>
                </Border>

                <!-- 상권 정보 스크롤 영역 -->
                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              Padding="16">
                    <StackPanel>
                        <!-- 상권 요약 정보 -->
                        <Border Background="White"
                                CornerRadius="8"
                                Padding="16"
                                Margin="0,0,0,16">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                            </Border.Effect>

                            <StackPanel>
                                <TextBlock Text="상권 요약"
                                           FontWeight="SemiBold"
                                           FontSize="13"
                                           Margin="0,0,0,12"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- 총 점포 수 -->
                                    <Border Grid.Row="0" Grid.Column="0"
                                            Background="{StaticResource PrimaryLightBrush}"
                                            CornerRadius="6"
                                            Padding="12"
                                            Margin="0,0,6,6">
                                        <StackPanel>
                                            <TextBlock Text="총 점포 수"
                                                       FontSize="11"
                                                       Foreground="#64748B"/>
                                            <TextBlock x:Name="TxtTotalStores"
                                                       Text="-"
                                                       FontSize="24"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource PrimaryBrush}"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- 업종 수 -->
                                    <Border Grid.Row="0" Grid.Column="1"
                                            Background="#FEF3C7"
                                            CornerRadius="6"
                                            Padding="12"
                                            Margin="6,0,0,6">
                                        <StackPanel>
                                            <TextBlock Text="업종 수"
                                                       FontSize="11"
                                                       Foreground="#64748B"/>
                                            <TextBlock x:Name="TxtIndustryCount"
                                                       Text="-"
                                                       FontSize="24"
                                                       FontWeight="Bold"
                                                       Foreground="#D97706"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- 상권명 -->
                                    <Border Grid.Row="1" Grid.ColumnSpan="2"
                                            Background="#F0FDF4"
                                            CornerRadius="6"
                                            Padding="12"
                                            Margin="0,6,0,0">
                                        <StackPanel>
                                            <TextBlock Text="상권명"
                                                       FontSize="11"
                                                       Foreground="#64748B"/>
                                            <TextBlock x:Name="TxtDistrictName"
                                                       Text="-"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="#059669"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- 업종별 현황 -->
                        <Border Background="White"
                                CornerRadius="8"
                                Padding="16"
                                Margin="0,0,0,16">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                            </Border.Effect>

                            <StackPanel>
                                <TextBlock Text="업종별 점포 현황"
                                           FontWeight="SemiBold"
                                           FontSize="13"
                                           Margin="0,0,0,12"/>

                                <ItemsControl x:Name="IndustryStatsList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="60"/>
                                                    <ColumnDefinition Width="60"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding IndustryName}"
                                                           FontSize="12"
                                                           VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding StoreCount, StringFormat={}{0}개}"
                                                           FontSize="12"
                                                           FontWeight="SemiBold"
                                                           HorizontalAlignment="Right"
                                                           VerticalAlignment="Center"/>
                                                <ProgressBar Grid.Column="2"
                                                             Value="{Binding Percentage}"
                                                             Maximum="100"
                                                             Height="6"
                                                             Margin="8,0,0,0"
                                                             Foreground="{StaticResource PrimaryBrush}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- 데이터 없음 메시지 -->
                                <TextBlock x:Name="TxtNoData"
                                           Text="상권 데이터를 불러오는 중..."
                                           FontSize="12"
                                           Foreground="#94A3B8"
                                           HorizontalAlignment="Center"
                                           Margin="0,16"/>
                            </StackPanel>
                        </Border>

                        <!-- 주변 점포 목록 -->
                        <Border Background="White"
                                CornerRadius="8"
                                Padding="16">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                            </Border.Effect>

                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="주변 점포 목록"
                                               FontWeight="SemiBold"
                                               FontSize="13"/>
                                    <TextBlock Grid.Column="1"
                                               x:Name="TxtStoreListCount"
                                               Text=""
                                               FontSize="11"
                                               Foreground="#64748B"
                                               VerticalAlignment="Center"/>
                                </Grid>

                                <DataGrid x:Name="StoreListGrid"
                                          AutoGenerateColumns="False"
                                          IsReadOnly="True"
                                          MaxHeight="300"
                                          HeadersVisibility="Column"
                                          GridLinesVisibility="Horizontal"
                                          BorderThickness="0"
                                          Background="Transparent"
                                          RowBackground="Transparent"
                                          AlternatingRowBackground="#F8FAFC">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="점포명" Binding="{Binding StoreName}" Width="*"/>
                                        <DataGridTextColumn Header="업종" Binding="{Binding IndustryName}" Width="80"/>
                                        <DataGridTextColumn Header="세부업종" Binding="{Binding IndustryDetail}" Width="100"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>

                        <!-- API 안내 메시지 -->
                        <Border x:Name="ApiGuidePanel"
                                Background="#FEF3C7"
                                CornerRadius="8"
                                Padding="16"
                                Margin="0,16,0,0"
                                Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="💡 소상공인 상권정보 API"
                                           FontWeight="SemiBold"
                                           FontSize="12"
                                           Foreground="#92400E"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="상권 데이터를 사용하려면 data.go.kr에서 소상공인 상권정보 API 키를 발급받아 appsettings.json에 설정해주세요."
                                           FontSize="11"
                                           Foreground="#92400E"
                                           TextWrapping="Wrap"
                                           LineHeight="18"/>
                                <Button Content="API 신청 페이지 열기"
                                        Style="{StaticResource SecondaryButton}"
                                        Padding="8,4"
                                        Margin="0,12,0,0"
                                        HorizontalAlignment="Left"
                                        Click="BtnOpenApiPage_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>










