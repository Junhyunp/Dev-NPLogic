---
phase: 02-datagrid-style
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NPLogic.App/Views/EvaluationTab.xaml
autonomous: false
requirements: [GRID-01, GRID-02, GRID-03]
must_haves:
  truths:
    - "15 DataGrid all have Background White RowBackground White AlternatingRowBackground FAFAFA"
    - "15 DataGrid all have BorderThickness 0"
    - "15 DataGrid all have BorderBrush StaticResource BorderBrush"
    - "MaxHeight only on RealTransactionGrid 280"
    - "FontSize unified via SectionDataGridTextBlockStyle FontSizeBody 12pt"
    - "RowHeight 32 unified across all"
  artifacts:
    - path: "src/NPLogic.App/Views/EvaluationTab.xaml"
      provides: "15 DataGrid style unification"
      contains: "AlternatingRowBackground"
  key_links: []
---

<objective>
EvaluationTab.xaml 내 15개 DataGrid의 시각적 속성을 표준 기준으로 통일한다.

Purpose: 어떤 평가 유형을 열어도 DataGrid의 배경색, 줄무늬, 테두리, 폰트가 동일하게 보이도록 일관성 확보
Output: 단일 파일(EvaluationTab.xaml)의 XAML 속성 수정
</objective>

<execution_context>
@C:/Users/kim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-datagrid-style/02-CONTEXT.md
@.planning/phases/02-datagrid-style/02-RESEARCH.md

<interfaces>
<!-- EvaluationTab.xaml 로컬 스타일 (lines 15-93) -->
SectionDataGridColumnHeaderStyle: BlueGray100Brush 배경, SemiBold, 12pt, Center, BorderBrush 테두리
SectionDataGridCellStyle: Foreground=Black, Background=White, BorderBrush 테두리, Padding 4,2
SectionDataGridRowStyle: VerticalContentAlignment=Center, Foreground=Black
SectionDataGridTextBlockStyle: FontSizeBody, Center 정렬
SectionDataGridTextBoxStyle: FontSizeBody, Center 정렬

<!-- 표준 DataGrid 속성 세트 (목표 상태) -->
```xml
AutoGenerateColumns="False"
CanUserAddRows="False" CanUserDeleteRows="False"
CanUserSortColumns="False"
HeadersVisibility="Column"
RowHeight="32"
GridLinesVisibility="All"
HorizontalGridLinesBrush="{StaticResource BorderBrush}"
VerticalGridLinesBrush="{StaticResource BorderBrush}"
BorderBrush="{StaticResource BorderBrush}" BorderThickness="0"
Background="White"
RowBackground="White"
AlternatingRowBackground="#FAFAFA"
MinHeight="60"
PreviewMouseWheel="DataGrid_PreviewMouseWheel"
```

<!-- 의도적 예외 (건드리지 않을 것) -->
RealTransactionGrid: MaxHeight="280", MinHeight="280", ScrollViewer.VerticalScrollBarVisibility="Auto", SelectionUnit="Cell", SelectionMode="Single"
CaseEvaluationGrid: SelectionUnit="Cell", SelectionMode="Single"
LotEvalDataGrid: HeadersVisibility="None", MinHeight="32" (ColumnHeaderStyle 없음 -- 정상)
InterimRecovery/Expense: IsReadOnly="True", ScrollViewer 양방향 Disabled
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 15개 DataGrid 공통 속성 통일 (AlternatingRowBackground + BorderThickness + BorderBrush)</name>
  <files>src/NPLogic.App/Views/EvaluationTab.xaml</files>
  <action>
EvaluationTab.xaml 내 9개 DataGrid에 아래 3가지 수정 패턴을 적용한다. 기능/바인딩/ViewModel 변경 없이 XAML 속성만 수정.

**패턴 A: AlternatingRowBackground + Background + RowBackground 추가 (4개 DataGrid)**

아래 4개 DataGrid에 3개 속성 추가 (RowHeight="32" 줄 근처에 삽입):
```xml
Background="White"
RowBackground="White"
AlternatingRowBackground="#FAFAFA"
```

대상:
1. RealTransactionGrid (line ~259) -- BorderBrush="{StaticResource BorderBrush}" 도 함께 추가
2. CaseEvaluationGrid (line ~668) -- BorderBrush="{StaticResource BorderBrush}" 도 함께 추가
3. InterimRecovery DataGrid (line ~3333, unnamed, ItemsSource=InterimRecoveryRows) -- BorderBrush="{StaticResource BorderBrush}" 도 함께 추가
4. InterimExpense DataGrid (line ~3407, unnamed, ItemsSource=InterimExpenseRows) -- BorderBrush="{StaticResource BorderBrush}" 도 함께 추가

**패턴 B: BorderThickness="1" -> "0" 변경 (5개 DataGrid)**

아래 5개 DataGrid에서 `BorderThickness="1"`을 `BorderThickness="0"`으로 변경:
1. FactoryInquiryDataGrid (line ~1182)
2. FactoryInquiryResultDataGrid (line ~1283)
3. RentalIncomeDataGrid (line ~1971)
4. InquiryDataGrid (line ~2188)
5. InquiryResultDataGrid (line ~2290)

**GRID-01 확인 (MaxHeight):**
RealTransactionGrid만 MaxHeight="280" 보유. 다른 DataGrid에 MaxHeight 없음을 확인. 변경 불필요.

**GRID-02 확인 (FontSize):**
모든 DataGrid 컬럼이 SectionDataGridTextBlockStyle/SectionDataGridTextBoxStyle 사용 중 (FontSizeBody=12pt). 변경 불필요.

**주의사항:**
- 각 DataGrid의 고유 속성(SelectionUnit, SelectionMode, MaxHeight, Visibility 바인딩 등)은 절대 변경하지 않음
- LotEvalDataGrid(HeadersVisibility="None")는 이미 Background/RowBackground/AlternatingRowBackground 완비 -- 수정 불필요
- 이미 올바른 DataGrid 8개(FactoryScenario1/2, CommercialScenario1/2, HouseScenario1/2, LotEval, 나머지)는 건드리지 않음
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic && dotnet build NPLogic.sln --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
- 4개 DataGrid에 Background="White" RowBackground="White" AlternatingRowBackground="#FAFAFA" BorderBrush="{StaticResource BorderBrush}" 추가됨
- 5개 DataGrid에서 BorderThickness="1"이 "0"으로 변경됨
- 빌드 성공 (0 errors)
- GRID-01: MaxHeight 현재 상태 유지 확인됨
- GRID-02: FontSize FontSizeBody 통일 확인됨
- GRID-03: 공통 속성(AlternatingRowBackground, BorderThickness, BorderBrush) 15개 DataGrid 전체 통일됨
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 전체 DataGrid 시각적 일관성 확인</name>
  <action>
사용자가 앱을 실행하여 5개 평가 유형을 전환하며 DataGrid 줄무늬/테두리 통일 결과를 시각적으로 확인한다.
  </action>
  <what-built>15개 DataGrid의 AlternatingRowBackground/BorderThickness/BorderBrush 통일</what-built>
  <how-to-verify>
    1. 앱을 실행하고 평가 탭 진입
    2. 아파트 유형 선택 -> 실거래가 DataGrid에 번갈아 배경색(흰/회색) 확인
    3. 사례평가 DataGrid에도 번갈아 배경색 확인
    4. 공장/창고 유형 -> 탐문 내역/결과 DataGrid 외곽 이중 테두리가 사라졌는지 확인 (BorderThickness 0)
    5. 연립다세대 유형 -> 탐문 내역/결과 DataGrid 동일하게 테두리 제거 확인
    6. 인터림 회수/지출 DataGrid에 번갈아 배경색 확인
    7. 전체적으로 DataGrid 간 시각적 일관성 확보 여부 판단
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 수정 필요 사항 설명</resume-signal>
</task>

</tasks>

<verification>
1. `dotnet build NPLogic.sln` 성공 (XAML 파싱 오류 없음)
2. grep으로 확인:
   - `BorderThickness="1"` 이 EvaluationTab.xaml DataGrid에서 0건 (전부 "0"으로 변경됨)
   - `AlternatingRowBackground="#FAFAFA"` 가 15개 이상 존재 (모든 DataGrid에 적용)
3. 시각적 확인: 모든 평가 유형에서 DataGrid 줄무늬/테두리 일관성
</verification>

<success_criteria>
- 15개 DataGrid 모두 Background="White", RowBackground="White", AlternatingRowBackground="#FAFAFA" 보유
- 15개 DataGrid 모두 BorderThickness="0"
- 15개 DataGrid 모두 BorderBrush="{StaticResource BorderBrush}" 명시
- RowHeight="32" 전체 유지
- MaxHeight는 RealTransactionGrid만 280 (사용자 결정대로 현재 상태 유지)
- FontSize는 SectionDataGridTextBlockStyle 통해 FontSizeBody 통일 유지
- 빌드 성공, 기능 변경 없음
</success_criteria>

<output>
After completion, create `.planning/phases/02-datagrid-style/02-01-SUMMARY.md`
</output>
