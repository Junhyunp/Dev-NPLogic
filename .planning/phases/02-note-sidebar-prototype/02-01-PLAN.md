---
phase: 02-note-sidebar-prototype
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NPLogic.App/ViewModels/EvaluationTabViewModel.cs
  - src/NPLogic.App/ViewModels/SeniorRightsViewModel.cs
  - src/NPLogic.App/ViewModels/PropertyDetailViewModel.cs
  - src/NPLogic.App/Views/EvaluationTab.xaml
  - src/NPLogic.App/Views/SeniorRightsView.xaml
  - src/NPLogic.App/Views/CollateralPropertyView.xaml
autonomous: false
requirements: [NOTE-01, NOTE-02, NOTE-03]

must_haves:
  truths:
    - "담보물건/선순위/평가 탭에서 토글 버튼을 누르면 오른쪽에 250px 비고 패널이 즉시 나타난다"
    - "비고 패널의 TextBox에 여러 줄 텍스트를 자유롭게 입력할 수 있다"
    - "기존 저장 버튼을 누르면 비고 내용이 DB에 저장된다"
    - "탭 재진입(물건 전환) 시 저장된 비고가 복원된다"
    - "비고 패널이 닫힌 상태에서 메인 콘텐츠 레이아웃이 정상이다"
  artifacts:
    - path: "src/NPLogic.App/ViewModels/EvaluationTabViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/SeniorRightsViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/PropertyDetailViewModel.cs"
      provides: "CollateralNoteText, IsCollateralNotePanelVisible, ToggleCollateralNotePanel, LoadCollateralNoteAsync, SaveCollateralNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/Views/EvaluationTab.xaml"
      provides: "비고 사이드 패널 + 토글 버튼"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/SeniorRightsView.xaml"
      provides: "비고 사이드 패널 + 토글 버튼"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/CollateralPropertyView.xaml"
      provides: "비고 사이드 패널 + 토글 버튼"
      contains: "NotePanelColumn"
  key_links:
    - from: "EvaluationTabViewModel.SaveAsync"
      to: "PropertyNoteRepository.UpsertAsync"
      via: "SaveNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.UpsertAsync"
    - from: "SeniorRightsViewModel.SaveAllAsync"
      to: "PropertyNoteRepository.UpsertAsync"
      via: "SaveNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.UpsertAsync"
    - from: "PropertyDetailViewModel.SaveAllCollateralAsync"
      to: "PropertyNoteRepository.UpsertAsync"
      via: "SaveCollateralNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.UpsertAsync"
    - from: "EvaluationTabViewModel.LoadAsync"
      to: "PropertyNoteRepository.GetByPropertyAndTabAsync"
      via: "LoadNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.GetByPropertyAndTabAsync"
    - from: "SeniorRightsViewModel.OnSelectedPropertyChanged"
      to: "PropertyNoteRepository.GetByPropertyAndTabAsync"
      via: "LoadNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.GetByPropertyAndTabAsync"
    - from: "PropertyDetailViewModel.InitializeAsync"
      to: "PropertyNoteRepository.GetByPropertyAndTabAsync"
      via: "LoadCollateralNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.GetByPropertyAndTabAsync"
---

<objective>
담보물건, 선순위, 평가 3개 탭에 오른쪽 사이드 패널 비고란을 추가한다. 토글 버튼(저장 버튼 옆)으로 250px 패널을 즉시 열고 닫을 수 있으며, 기존 저장 명령 실행 시 비고도 함께 DB에 저장된다. 탭 진입(물건 전환) 시 저장된 비고가 자동 로드된다.

Purpose: Phase 2 프로토타입으로 사이드 패널 비고란 UI 패턴을 검증한다. 이 패턴이 확인되면 Phase 3에서 나머지 탭 전체로 확산한다.
Output: 3개 ViewModel + 3개 View 수정 완료, 비고 입력/저장/로드 동작
</objective>

<execution_context>
@C:/Users/kim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-data-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Phase 1에서 생성된 Repository - 이 인터페이스를 직접 사용한다 -->

From src/NPLogic.Core/Models/PropertyNote.cs:
```csharp
public class PropertyNote
{
    public Guid Id { get; set; }
    public Guid PropertyId { get; set; }
    public string TabName { get; set; } = string.Empty;
    public string NoteText { get; set; } = string.Empty;
    public DateTime UpdatedAt { get; set; }
}
```

From src/NPLogic.Data/Repositories/PropertyNoteRepository.cs:
```csharp
public class PropertyNoteRepository
{
    public PropertyNoteRepository(Services.SupabaseService supabaseService);
    public async Task<List<PropertyNote>> GetByPropertyIdAsync(Guid propertyId);
    public async Task<PropertyNote?> GetByPropertyAndTabAsync(Guid propertyId, string tabName);
    public async Task<PropertyNote> UpsertAsync(PropertyNote note);
    public async Task DeleteAsync(Guid noteId);
}
```

기존 ViewModel Save 패턴:
- EvaluationTabViewModel: `[RelayCommand] private async Task SaveAsync()` (line 1890)
- SeniorRightsViewModel: `[RelayCommand] private async Task SaveAllAsync()` (line 2581)
- PropertyDetailViewModel: `[RelayCommand] private async Task SaveAllCollateralAsync()` (line 4245)

기존 ViewModel Load 패턴:
- EvaluationTabViewModel: `public async Task LoadAsync()` (line 987), `_propertyId` (Guid)
- SeniorRightsViewModel: `partial void OnSelectedPropertyChanged(Property? value)` (line 1321), `SelectedProperty.Id`
- PropertyDetailViewModel: `public async Task InitializeAsync()` (line 1354), `_propertyId` (Guid?)

기존 ViewModel 생성자 DI:
- EvaluationTabViewModel: `App.ServiceProvider?.GetService(typeof(X))` 패턴 사용 (생성자 파라미터 추가 없이)
- SeniorRightsViewModel: 생성자 파라미터로 optional 주입
- PropertyDetailViewModel: 생성자 파라미터로 optional 주입

기존 View 루트 레이아웃:
- EvaluationTab.xaml: `<ScrollViewer>` -> `<Grid>` (line 96-103)
- SeniorRightsView.xaml: `<Grid Margin="12,8,12,12">` (line 236)
- CollateralPropertyView.xaml: `<Grid Background="...">` -> `<ScrollViewer>` (line 86-89)

저장 버튼 위치:
- EvaluationTab.xaml: Grid.Column="2" 헤더 영역 (line 157-160)
- SeniorRightsView.xaml: HorizontalAlignment="Right" 섹션 헤더 내부 (line 267-273)
- CollateralPropertyView.xaml: HorizontalAlignment="Right" 지도 헤더 내부 (line 111-117)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3개 ViewModel에 비고 속성/명령/로드/저장 로직 추가</name>
  <files>
    src/NPLogic.App/ViewModels/EvaluationTabViewModel.cs
    src/NPLogic.App/ViewModels/SeniorRightsViewModel.cs
    src/NPLogic.App/ViewModels/PropertyDetailViewModel.cs
  </files>
  <action>
각 ViewModel에 PropertyNoteRepository를 주입하고, 비고 관련 속성/명령/메서드를 추가한다.

**A. EvaluationTabViewModel.cs:**

1. 필드 추가 (line 342 부근, 기존 필드들 아래):
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
```

2. 생성자에서 DI 해결 (line 349-350 패턴 따름):
```csharp
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;
```

3. ObservableProperty 추가 (SaveCommand 근처 적절한 위치에):
```csharp
[ObservableProperty]
private string _noteText = string.Empty;

[ObservableProperty]
private bool _isNotePanelVisible;
```

4. 토글 명령 추가:
```csharp
[RelayCommand]
private void ToggleNotePanel()
{
    IsNotePanelVisible = !IsNotePanelVisible;
}
```

5. 비고 로드 메서드 추가:
```csharp
private async Task LoadNoteAsync()
{
    if (_propertyNoteRepository == null || _propertyId == Guid.Empty) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(_propertyId, "evaluation");
        NoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        Debug.WriteLine($"[EvaluationTab] 비고 로드 실패: {ex.Message}");
    }
}
```

6. 비고 저장 메서드 추가:
```csharp
private async Task SaveNoteAsync()
{
    if (_propertyNoteRepository == null || _propertyId == Guid.Empty) return;
    if (string.IsNullOrWhiteSpace(NoteText)) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new NPLogic.Core.Models.PropertyNote
        {
            PropertyId = _propertyId,
            TabName = "evaluation",
            NoteText = NoteText
        });
    }
    catch (Exception ex)
    {
        Debug.WriteLine($"[EvaluationTab] 비고 저장 실패: {ex.Message}");
    }
}
```

7. `LoadAsync()` 메서드 (line 987) 안에서 기존 데이터 로드 완료 후 `await LoadNoteAsync();` 추가 (try 블록 끝, IsLoading = false 직전)

8. `SaveAsync()` 메서드 (line 1890) 안에서 기존 저장 로직 완료 후 `await SaveNoteAsync();` 추가 (IsDirty = false 직전)

**B. SeniorRightsViewModel.cs:**

1. 필드 추가 (line 28 부근):
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
```

2. 생성자 파라미터에 optional 추가 (line 663, 마지막 파라미터 뒤에):
```csharp
PropertyNoteRepository? propertyNoteRepository = null
```
그리고 생성자 본문에 `_propertyNoteRepository = propertyNoteRepository;` 추가.

주의: App.xaml.cs의 DI 등록에서 SeniorRightsViewModel은 이미 자동 해결되므로 PropertyNoteRepository도 Singleton으로 등록되어 있어 자동 주입된다.

3. ObservableProperty 추가 (IsLoading 근처):
```csharp
[ObservableProperty]
private string _noteText = string.Empty;

[ObservableProperty]
private bool _isNotePanelVisible;
```

4. ToggleNotePanel 명령 추가 (EvaluationTab과 동일 패턴).

5. LoadNoteAsync 추가 - `SelectedProperty?.Id`를 사용하고 tabName은 `"senior_rights"`:
```csharp
private async Task LoadNoteAsync()
{
    if (_propertyNoteRepository == null || SelectedProperty == null) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(SelectedProperty.Id, "senior_rights");
        NoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        Debug.WriteLine($"[SeniorRights] 비고 로드 실패: {ex.Message}");
    }
}
```

6. SaveNoteAsync 추가 - `SelectedProperty?.Id`를 사용하고 tabName은 `"senior_rights"`:
```csharp
private async Task SaveNoteAsync()
{
    if (_propertyNoteRepository == null || SelectedProperty == null) return;
    if (string.IsNullOrWhiteSpace(NoteText)) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new NPLogic.Core.Models.PropertyNote
        {
            PropertyId = SelectedProperty.Id,
            TabName = "senior_rights",
            NoteText = NoteText
        });
    }
    catch (Exception ex)
    {
        Debug.WriteLine($"[SeniorRights] 비고 저장 실패: {ex.Message}");
    }
}
```

7. `OnSelectedPropertyChanged` (line 1321)에 `_ = LoadNoteAsync();` 추가.

8. `SaveAllAsync()` (line 2581)에서 기존 저장 완료 후 (ToastService 직전) `await SaveNoteAsync();` 추가.

**C. PropertyDetailViewModel.cs:**

CollateralProperty 탭이 PropertyDetailViewModel을 DataContext로 사용하므로, 기존 프로퍼티와 이름 충돌을 피하기 위해 접두사 `Collateral` 사용.

1. 필드 추가 (line 258 부근):
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
```

2. 생성자 파라미터에 optional 추가 (line 1079, 마지막 파라미터 뒤에):
```csharp
PropertyNoteRepository? propertyNoteRepository = null
```
그리고 `_propertyNoteRepository = propertyNoteRepository;` 추가.

3. ObservableProperty 추가:
```csharp
[ObservableProperty]
private string _collateralNoteText = string.Empty;

[ObservableProperty]
private bool _isCollateralNotePanelVisible;
```

4. ToggleCollateralNotePanel 명령 추가:
```csharp
[RelayCommand]
private void ToggleCollateralNotePanel()
{
    IsCollateralNotePanelVisible = !IsCollateralNotePanelVisible;
}
```

5. LoadCollateralNoteAsync 추가 - `_propertyId`를 사용하고 tabName은 `"collateral_property"`:
```csharp
private async Task LoadCollateralNoteAsync()
{
    if (_propertyNoteRepository == null || _propertyId == null) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(_propertyId.Value, "collateral_property");
        CollateralNoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        Debug.WriteLine($"[CollateralProperty] 비고 로드 실패: {ex.Message}");
    }
}
```

6. SaveCollateralNoteAsync 추가 - `_propertyId`를 사용하고 tabName은 `"collateral_property"`:
```csharp
private async Task SaveCollateralNoteAsync()
{
    if (_propertyNoteRepository == null || _propertyId == null) return;
    if (string.IsNullOrWhiteSpace(CollateralNoteText)) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new NPLogic.Core.Models.PropertyNote
        {
            PropertyId = _propertyId.Value,
            TabName = "collateral_property",
            NoteText = CollateralNoteText
        });
    }
    catch (Exception ex)
    {
        Debug.WriteLine($"[CollateralProperty] 비고 저장 실패: {ex.Message}");
    }
}
```

7. `InitializeAsync()` (line 1354)에서 물건 정보 로드 완료 후 (2단계 병렬 로드 전) `await LoadCollateralNoteAsync();` 추가.

8. `SaveAllCollateralAsync()` (line 4245)에서 기존 저장 완료 후 (SuccessMessage 직전) `await SaveCollateralNoteAsync();` 추가.

**주의사항:**
- `using NPLogic.Data.Repositories;`가 없는 ViewModel에는 추가 필요 (또는 fully qualified name 사용)
- `using NPLogic.Core.Models;`가 없는 ViewModel에는 추가 필요 (또는 fully qualified name 사용)
- PropertyDetailViewModel은 다른 탭(Home 등)에서도 사용되지만, Collateral 접두사로 충돌 방지
- SaveNoteAsync에서 NoteText가 빈 문자열이면 저장 스킵 (불필요한 DB 호출 방지)
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic &amp;&amp; dotnet build NPLogic.sln --no-restore 2>&amp;1 | head -30</automated>
  </verify>
  <done>
3개 ViewModel 모두에 NoteText/IsNotePanelVisible/ToggleNotePanel 속성과 LoadNote/SaveNote 메서드가 추가되고, 기존 Save/Load 흐름에 비고 저장/로드가 통합되어 빌드 성공
  </done>
</task>

<task type="auto">
  <name>Task 2: 3개 View에 사이드 패널 XAML + 토글 버튼 추가</name>
  <files>
    src/NPLogic.App/Views/EvaluationTab.xaml
    src/NPLogic.App/Views/SeniorRightsView.xaml
    src/NPLogic.App/Views/CollateralPropertyView.xaml
  </files>
  <action>
각 View의 루트 레이아웃을 수정하여 메인 콘텐츠 + 사이드 패널 2열 구조로 변경하고, 저장 버튼 옆에 토글 버튼을 추가한다.

**사이드 패널 공통 XAML 패턴** (3개 View에서 동일한 구조 사용):
```xml
<!-- 비고 사이드 패널 -->
<Border x:Name="NotePanelColumn"
        Width="250"
        Visibility="{Binding IsNotePanelVisible, Converter={StaticResource BoolToVisibilityConverter}}"
        Background="{StaticResource SurfaceColor}"
        BorderBrush="{StaticResource BorderBrush}"
        BorderThickness="1,0,0,0"
        Padding="12">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top"
                   Text="&#x1F4DD; 비고"
                   FontWeight="SemiBold"
                   FontSize="{StaticResource FontSizeBody}"
                   Foreground="{StaticResource PrimaryBrush}"
                   Margin="0,0,0,8"/>
        <TextBox Text="{Binding NoteText, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 VerticalAlignment="Stretch"
                 FontSize="{StaticResource FontSizeBody}"
                 Padding="8"
                 BorderBrush="{StaticResource BorderBrush}"
                 BorderThickness="1"
                 Background="White"/>
    </DockPanel>
</Border>
```

**토글 버튼 공통 패턴** (저장 버튼 옆에 배치):
```xml
<Button Command="{Binding ToggleNotePanelCommand}"
        Style="{StaticResource MaterialDesignIconButton}"
        ToolTip="비고 패널 열기/닫기"
        Width="32" Height="32"
        Padding="0"
        Margin="8,0,0,0">
    <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
</Button>
```

**A. EvaluationTab.xaml:**

현재 구조: `<ScrollViewer>` -> `<Grid>` (2 Row)
변경: `<ScrollViewer>` 위에 새 `<Grid>` 래퍼 추가 (2 Column: Main + SidePanel)

1. UserControl 루트 자식을 다음 구조로 변경:
```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <!-- 기존 ScrollViewer (Grid.Column="0" 추가) -->
    <ScrollViewer Grid.Column="0" ... (기존 속성 유지)>
        ... (기존 내용 그대로)
    </ScrollViewer>

    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    <Border Grid.Column="1" ... (위 공통 패턴) />
</Grid>
```

2. 헤더 영역 (line 118-161)의 Grid.Column="2" 저장 버튼 옆에 토글 버튼 추가:
- 기존: `<Button Grid.Column="2" Content="저장" .../>` 만 존재
- 변경: 저장 버튼을 StackPanel Orientation="Horizontal"로 감싸고 토글 버튼 추가:
```xml
<StackPanel Grid.Column="2" Orientation="Horizontal">
    <Button Content="저장"
            Command="{Binding SaveCommand}"
            Style="{StaticResource MaterialDesignRaisedButton}"
            Padding="12,6"/>
    <Button Command="{Binding ToggleNotePanelCommand}"
            Style="{StaticResource MaterialDesignIconButton}"
            ToolTip="비고 패널 열기/닫기"
            Width="32" Height="32"
            Padding="0"
            Margin="8,0,0,0">
        <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
    </Button>
</StackPanel>
```

**B. SeniorRightsView.xaml:**

현재 구조: `<Grid Margin="12,8,12,12">` -> `<Border>` -> `<ScrollViewer>`
변경: 루트 Grid에 2 Column 추가

1. 루트 `<Grid Margin="12,8,12,12">`를 2열 구조로 변경:
```xml
<Grid Margin="12,8,12,12">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <!-- 기존 Border (Grid.Column="0" 추가) -->
    <Border Grid.Column="0" Background="{StaticResource SurfaceColor}" ...>
        ... (기존 내용 그대로)
    </Border>

    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    <Border Grid.Column="1" ... (위 공통 패턴) />
</Grid>
```

2. 섹션 헤더 (line 258-274)에서 저장 버튼 옆에 토글 버튼 추가:
- 기존 `<Button Content="저장" ... HorizontalAlignment="Right"/>` 를 StackPanel으로 감싸기:
```xml
<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
    <Button Content="저장"
            Command="{Binding SaveAllCommand}"
            Style="{StaticResource MaterialDesignRaisedButton}"
            Padding="12,6"
            Foreground="White"/>
    <Button Command="{Binding ToggleNotePanelCommand}"
            Style="{StaticResource MaterialDesignIconButton}"
            ToolTip="비고 패널 열기/닫기"
            Width="32" Height="32"
            Padding="0"
            Margin="8,0,0,0"
            Foreground="White">
        <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
    </Button>
</StackPanel>
```

**C. CollateralPropertyView.xaml:**

현재 구조: `<Grid Background="...">` -> `<ScrollViewer>` -> `<StackPanel>`
변경: 루트 Grid에 2 Column 추가

주의: CollateralPropertyView는 PropertyDetailViewModel이 DataContext이므로 바인딩 이름이 다르다:
- `IsCollateralNotePanelVisible` (not IsNotePanelVisible)
- `CollateralNoteText` (not NoteText)
- `ToggleCollateralNotePanelCommand` (not ToggleNotePanelCommand)

1. 루트 `<Grid Background="...">`를 2열 구조로 변경:
```xml
<Grid Background="{StaticResource BackgroundBrush}">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <!-- 기존 ScrollViewer (Grid.Column="0" 추가) -->
    <ScrollViewer Grid.Column="0" x:Name="MainScrollViewer" ...>
        ... (기존 내용 그대로)
    </ScrollViewer>

    <!-- 비고 사이드 패널 -->
    <Border Grid.Column="1"
            Width="250"
            Visibility="{Binding IsCollateralNotePanelVisible, Converter={StaticResource BoolToVisibilityConverter}}"
            ... (나머지 공통 패턴과 동일하되 TextBox 바인딩은 CollateralNoteText) />
</Grid>
```

2. 지도 헤더 (line 105-118)에서 저장 버튼 옆에 토글 버튼 추가:
```xml
<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
    <Button Content="저장"
            Command="{Binding SaveAllCollateralCommand}"
            Style="{StaticResource MaterialDesignRaisedButton}"
            Padding="12,6"
            Foreground="White"/>
    <Button Command="{Binding ToggleCollateralNotePanelCommand}"
            Style="{StaticResource MaterialDesignIconButton}"
            ToolTip="비고 패널 열기/닫기"
            Width="32" Height="32"
            Padding="0"
            Margin="8,0,0,0"
            Foreground="White">
        <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
    </Button>
</StackPanel>
```

**주의사항:**
- BoolToVisibilityConverter는 이미 App.xaml 리소스에 등록되어 있음 (EvaluationTab에서 이미 사용 중)
- MaterialDesignIconButton 스타일은 MaterialDesignInXAML 패키지에 포함 (이미 프로젝트에 설치됨)
- PackIcon Kind="NoteEdit"는 MaterialDesign 아이콘 세트에 존재. 없으면 "NoteEditOutline" 또는 "NotePlusOutline" 사용
- 사이드 패널 Width="250"은 Visibility="Collapsed"일 때 자동으로 0px이 됨 (Auto Column)
- CollateralPropertyView에는 WebView2 에어스페이스 이슈가 있으므로 사이드 패널이 WebView2 위에 겹치지 않도록 Grid Column 분리가 올바른 접근 (기존 에어스페이스 수정과 호환됨)
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic &amp;&amp; dotnet build NPLogic.sln --no-restore 2>&amp;1 | head -30</automated>
  </verify>
  <done>
3개 View에 사이드 패널 XAML과 토글 버튼이 추가되어 빌드 성공. 토글 버튼 클릭 시 250px 사이드 패널이 나타나고 사라짐
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 비고 사이드 패널 동작 검증</name>
  <what-built>담보물건/선순위/평가 3개 탭에 비고 사이드 패널 추가 (토글 버튼, 입력, 저장, 로드)</what-built>
  <how-to-verify>
    1. 앱 실행: `dotnet run --project src/NPLogic.App/NPLogic.App.csproj`
    2. 물건 선택 -> 비핵심 -> 담보물건 탭 진입
    3. 지도 섹션 헤더 오른쪽에 메모 아이콘 버튼이 보이는지 확인
    4. 메모 아이콘 클릭 -> 오른쪽에 250px "비고" 패널이 즉시 나타나는지 확인
    5. TextBox에 여러 줄 텍스트 입력 -> "저장" 버튼 클릭
    6. 다른 물건으로 전환 후 -> 다시 원래 물건으로 돌아왔을 때 비고가 복원되는지 확인
    7. 메모 아이콘 다시 클릭 -> 패널이 닫히고 메인 콘텐츠가 정상 표시되는지 확인
    8. 선순위 탭, 평가 탭에서도 동일 검증 (토글, 입력, 저장, 복원)
  </how-to-verify>
  <resume-signal>"approved" 또는 수정 사항 설명</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build NPLogic.sln` 성공
- 3개 ViewModel에 NoteText/IsNotePanelVisible 속성 존재
- 3개 View에 NotePanelColumn Border와 토글 Button 존재
- 기존 SaveAll 명령에 SaveNoteAsync 호출 통합
- 기존 Load 흐름에 LoadNoteAsync 호출 통합
</verification>

<success_criteria>
1. 담보물건/선순위/평가 탭에서 토글 버튼 클릭 시 250px 사이드 패널 즉시 표시/숨김
2. 사이드 패널 TextBox에 여러 줄 자유 입력 가능
3. 기존 저장 버튼 클릭 시 비고가 DB에 저장됨
4. 탭 재진입(물건 전환) 시 저장된 비고가 자동 로드됨
5. 패널 닫힘 상태에서 메인 레이아웃 정상
</success_criteria>

<output>
After completion, create `.planning/phases/02-note-sidebar-prototype/02-01-SUMMARY.md`
</output>
