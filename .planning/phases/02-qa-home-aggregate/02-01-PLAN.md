---
phase: 02-qa-home-aggregate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NPLogic.App/Views/HomeTab.xaml
  - src/NPLogic.App/Views/QASummaryTab.xaml
autonomous: true
requirements: [QA-HOME-01, QA-HOME-02, QA-AGG-01, QA-AGG-02]

must_haves:
  truths:
    - "HomeTab QA 카드가 질의일자/질의내용/회신일자/답변내용 4열 DataGrid 표로 표시된다"
    - "HomeTab QA 카드에는 현재 선택된 물건의 QA만 표시된다 (QaList 바인딩)"
    - "QASummaryTab 우측 패널이 차주번호/차주명/질의일자/질의내용/회신일자/답변내용 6열 DataGrid 표로 표시된다"
    - "QASummaryTab에서 차주 선택/전체 보기 시 6열 표에 QA가 표시된다"
  artifacts:
    - path: "src/NPLogic.App/Views/HomeTab.xaml"
      provides: "QA 요약 카드를 DataGrid 4열 표로 교체"
      contains: "DataGrid"
    - path: "src/NPLogic.App/Views/QASummaryTab.xaml"
      provides: "QA집계 우측 패널을 6열 DataGrid 표로 교체"
      contains: "차주번호"
  key_links:
    - from: "HomeTab.xaml DataGrid"
      to: "PropertyDetailViewModel.QaList"
      via: "ItemsSource binding"
      pattern: "ItemsSource.*QaList"
    - from: "QASummaryTab.xaml DataGrid"
      to: "QASummaryViewModel.QaList"
      via: "ItemsSource binding"
      pattern: "ItemsSource.*QaList"
---

<objective>
HomeTab QA 요약 카드와 QASummaryTab 우측 패널의 DataGrid를 Phase 1 QAPopupWindow과 동일한 표 형식으로 교체한다.

Purpose: 3곳 QA UI(팝업/전체탭/QA집계)의 표 형식 통일로 일관된 사용자 경험 제공
Output: HomeTab.xaml Row 4 교체, QASummaryTab.xaml 우측 DataGrid 교체
</objective>

<execution_context>
@C:/Users/kim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-qa-popup-table/01-01-SUMMARY.md

<interfaces>
<!-- PropertyDetailViewModel (HomeTab의 DataContext) — 이미 QaList 존재 -->
From src/NPLogic.App/ViewModels/PropertyDetailViewModel.cs:
```csharp
// line 482: QA 목록 (이미 LoadQAListAsync에서 로드됨)
[ObservableProperty]
private ObservableCollection<PropertyQa> _qaList = new();

[ObservableProperty]
private bool _hasNoQA = true;
```

<!-- PropertyQa 모델 -->
From src/NPLogic.Core/Models/Property.cs:
```csharp
public class PropertyQa
{
    public Guid Id { get; set; }
    public Guid PropertyId { get; set; }
    public string Question { get; set; } = "";
    public string? Answer { get; set; }
    public DateTime CreatedAt { get; set; }
    public DateTime? AnsweredAt { get; set; }
    public string? BorrowerNumber { get; set; }
    public string? BorrowerName { get; set; }
    public bool IsAnswered => !string.IsNullOrWhiteSpace(Answer);
}
```

<!-- QASummaryViewModel (QASummaryTab의 DataContext) — 이미 QaList 존재 -->
From src/NPLogic.App/ViewModels/QASummaryViewModel.cs:
```csharp
[ObservableProperty]
private ObservableCollection<PropertyQa> _qaList = new();
```

<!-- Phase 1에서 확립된 DataGrid 스타일 패턴 (QAPopupWindow.xaml) -->
```xml
<!-- SectionDataGridColumnHeaderStyle: BlueGray100Brush, SemiBold, 12pt, Center -->
<!-- SectionDataGridCellStyle: Foreground=Black, BorderBrush, Padding 10,4 -->
<!-- SectionDataGridRowStyle: VerticalContentAlignment=Center -->
<!-- SectionDataGridTextBlockStyle: FontSizeBody, Center -->
<!-- RowHeight="30", BorderThickness="0" -->
<!-- 4열: 질의일자(100px)/질의내용(3*)/회신일자(120px)/답변내용(3*) -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomeTab QA 요약 카드를 4열 DataGrid로 교체</name>
  <files>src/NPLogic.App/Views/HomeTab.xaml</files>
  <action>
HomeTab.xaml의 Row 4 "QA 요약" 섹션(line 802~932)을 DataGrid 4열 읽기 전용 표로 교체한다.

**교체 범위:** `<!-- ========== Row 4: QA 요약 ========== -->` 주석부터 해당 Border 끝까지. 기존 6개 카테고리별 카운트 Grid + 안내 문구를 모두 제거하고 DataGrid로 대체.

**교체 내용:**
```xml
<!-- ========== Row 4: QA 요약 ========== -->
<Border Grid.Row="4" Style="{StaticResource SummaryCardStyle}">
    <StackPanel>
        <TextBlock Text="QA 요약" Style="{StaticResource CardHeaderStyle}"/>

        <Border BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Background="White">
            <DataGrid ItemsSource="{Binding QaList}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      RowHeight="30"
                      Background="White"
                      RowBackground="White"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      MaxHeight="250">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        <Setter Property="Padding" Value="10,4"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Foreground" Value="Black"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="질의일자"
                                        Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}"
                                        Width="100"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="TextAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="질의내용"
                                        Binding="{Binding Question}"
                                        Width="3*"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="4,0"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="회신일자"
                                        Binding="{Binding AnsweredAt, StringFormat=yyyy-MM-dd}"
                                        Width="100"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="TextAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="답변내용"
                                        Binding="{Binding Answer}"
                                        Width="3*"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="4,0"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </StackPanel>
</Border>
```

**주요 포인트:**
- `ItemsSource="{Binding QaList}"` — PropertyDetailViewModel의 기존 QaList 사용 (이미 property별 필터됨, LoadQAListAsync에서 GetByPropertyIdAsync 호출)
- `IsReadOnly="True"` — 읽기 전용 (CRUD는 팝업에서)
- `MaxHeight="250"` — HomeTab 전체 스크롤 내에서 QA 카드가 과도하게 커지지 않도록 제한
- 스타일은 Phase 1 QAPopupWindow과 동일한 패턴 (BlueGray100Brush 헤더, 30px row)
- ViewModel/code-behind 수정 불필요 — QaList가 이미 로드됨
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic && rtk dotnet build NPLogic.sln --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
- HomeTab Row 4에 질의일자/질의내용/회신일자/답변내용 4열 DataGrid 표시
- 기존 6개 카테고리별 카운트 카드 제거됨
- QaList 바인딩으로 현재 물건의 QA만 표시 (QA-HOME-01, QA-HOME-02 충족)
- 읽기 전용, 빌드 성공
  </done>
</task>

<task type="auto">
  <name>Task 2: QASummaryTab 우측 패널을 6열 DataGrid로 교체</name>
  <files>src/NPLogic.App/Views/QASummaryTab.xaml</files>
  <action>
QASummaryTab.xaml의 우측 패널(Grid.Column="1") 내부 DataGrid(line 432~563)를 6열 읽기 전용 DataGrid로 교체한다.

**교체 범위:** 기존 `<DataGrid Grid.Row="2" ...>` 블록 전체(line 432~563). 기존 7열(차주번호/파트/질문/답변/상태/생성일/답변일) + 인라인 스타일을 새로운 6열(차주번호/차주명/질의일자/질의내용/회신일자/답변내용) + Phase 1 통일 스타일로 교체.

**교체 내용:**
기존 DataGrid를 아래로 교체 (Grid.Row="2" 위치 유지):
```xml
<Border Grid.Row="2"
        BorderBrush="{StaticResource BorderBrush}"
        BorderThickness="1,0,1,1"
        Background="White">
    <DataGrid ItemsSource="{Binding QaList}"
              SelectedItem="{Binding SelectedQa}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              IsReadOnly="True"
              HeadersVisibility="Column"
              GridLinesVisibility="Horizontal"
              BorderThickness="0"
              RowHeight="30"
              Background="White"
              RowBackground="White"
              VerticalScrollBarVisibility="Auto"
              HorizontalScrollBarVisibility="Auto">
        <DataGrid.ColumnHeaderStyle>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{StaticResource BlueGray100Brush}"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,1,1"/>
            </Style>
        </DataGrid.ColumnHeaderStyle>
        <DataGrid.CellStyle>
            <Style TargetType="DataGridCell">
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,1,0"/>
                <Setter Property="Padding" Value="10,4"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </DataGrid.CellStyle>
        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="Black"/>
            </Style>
        </DataGrid.RowStyle>
        <DataGrid.Columns>
            <DataGridTextColumn Header="차주번호"
                                Binding="{Binding BorrowerNumber}"
                                Width="90"
                                IsReadOnly="True">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="TextAlignment" Value="Center"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Header="차주명"
                                Binding="{Binding BorrowerName}"
                                Width="100"
                                IsReadOnly="True">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="TextAlignment" Value="Center"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Header="질의일자"
                                Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}"
                                Width="100"
                                IsReadOnly="True">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="TextAlignment" Value="Center"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Header="질의내용"
                                Binding="{Binding Question}"
                                Width="2*"
                                IsReadOnly="True">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="4,0"/>
                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Header="회신일자"
                                Binding="{Binding AnsweredAt, StringFormat=yyyy-MM-dd}"
                                Width="100"
                                IsReadOnly="True">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="TextAlignment" Value="Center"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Header="답변내용"
                                Binding="{Binding Answer}"
                                Width="2*"
                                IsReadOnly="True">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="4,0"/>
                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
        </DataGrid.Columns>
    </DataGrid>
</Border>
```

**주요 포인트:**
- 기존 7열(차주번호/파트/질문/답변/상태/생성일/답변일) -> 새로운 6열(차주번호/차주명/질의일자/질의내용/회신일자/답변내용)
- `ItemsSource="{Binding QaList}"` 유지 — QASummaryViewModel의 기존 QaList 사용
- `SelectedItem="{Binding SelectedQa}"` 유지 — 기존 선택 기능 보존
- `IsReadOnly="True"` — 읽기 전용 표시
- 기존 ColumnHeaderStyle/RowStyle/CellStyle(인라인) 제거하고 Phase 1 통일 스타일 적용
- 파트(Part) 열 제거, 상태(IsAnswered) 열 제거 — 요구사항에 없음
- 차주명(BorrowerName) 열 추가 — QA집계에서 차주 식별 요구사항
- ViewModel/code-behind 수정 불필요 — 기존 LoadAllQaAsync/LoadQaListAsync가 이미 전체/차주별 QA를 로드함
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic && rtk dotnet build NPLogic.sln --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
- QASummaryTab 우측에 차주번호/차주명/질의일자/질의내용/회신일자/답변내용 6열 DataGrid 표시
- 차주 선택 시 해당 차주 QA 표시, 전체 보기 시 모든 QA 표시 (QA-AGG-01, QA-AGG-02 충족)
- Phase 1과 동일한 DataGrid 스타일 (BlueGray100Brush 헤더, 30px row)
- 읽기 전용, 빌드 성공
  </done>
</task>

</tasks>

<verification>
1. `dotnet build NPLogic.sln` 성공 (XAML 파싱 오류 없음)
2. HomeTab.xaml에 `ItemsSource="{Binding QaList}"` + 4열 DataGrid 존재
3. QASummaryTab.xaml에 `ItemsSource="{Binding QaList}"` + 6열 DataGrid 존재 (차주번호/차주명 포함)
4. 양쪽 DataGrid 모두 IsReadOnly="True"
</verification>

<success_criteria>
- HomeTab QA 카드: 질의일자/질의내용/회신일자/답변내용 4열 DataGrid, 현재 물건 QA만 표시
- QASummaryTab: 차주번호/차주명/질의일자/질의내용/회신일자/답변내용 6열 DataGrid, 전체/차주별 QA 표시
- 3곳 QA UI(팝업/전체탭/QA집계) 모두 동일한 DataGrid 스타일 사용
- 빌드 성공, 기능 변경 없음 (ViewModel/code-behind 수정 불필요)
</success_criteria>

<output>
After completion, create `.planning/phases/02-qa-home-aggregate/02-01-SUMMARY.md`
</output>
