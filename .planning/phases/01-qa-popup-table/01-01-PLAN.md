---
phase: 01-qa-popup-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NPLogic.App/Views/QAPopupWindow.xaml
  - src/NPLogic.App/Views/QAPopupWindow.xaml.cs
  - src/NPLogic.App/Views/NonCoreView.xaml.cs
autonomous: true
requirements: [QA-POP-01, QA-POP-02]

must_haves:
  truths:
    - "QA 버튼 클릭 시 n x 4 DataGrid 표가 있는 팝업이 열린다"
    - "팝업에 질의일자/질의내용/회신일자/답변내용 4열이 표시된다"
    - "해당 물건의 과거 QA 이력이 모두 로드되어 표에 누적 표시된다"
    - "QA 이력이 많으면 스크롤로 전체를 확인할 수 있다"
  artifacts:
    - path: "src/NPLogic.App/Views/QAPopupWindow.xaml"
      provides: "QA 팝업 XAML - DataGrid 4열 표"
      contains: "DataGrid"
    - path: "src/NPLogic.App/Views/QAPopupWindow.xaml.cs"
      provides: "QA 팝업 코드비하인드 - 데이터 로드 및 표시"
      contains: "PropertyQaRepository"
    - path: "src/NPLogic.App/Views/NonCoreView.xaml.cs"
      provides: "QAButton_Click에서 새 QAPopupWindow 호출"
      contains: "QAPopupWindow"
  key_links:
    - from: "src/NPLogic.App/Views/NonCoreView.xaml.cs"
      to: "src/NPLogic.App/Views/QAPopupWindow.xaml.cs"
      via: "QAButton_Click -> new QAPopupWindow(propertyId, borrowerNumber, borrowerName)"
      pattern: "new QAPopupWindow"
    - from: "src/NPLogic.App/Views/QAPopupWindow.xaml.cs"
      to: "PropertyQaRepository.GetByPropertyIdAsync"
      via: "팝업 로드 시 DB 조회"
      pattern: "GetByPropertyIdAsync"
---

<objective>
QA 팝업을 DataGrid 표 형식으로 교체하고 과거 이력을 누적 표시한다.

Purpose: 기존 QAInputDialog(단순 텍스트 입력)를 DataGrid 기반 QAPopupWindow로 대체하여, 사용자가 질의/답변 이력을 표 형식으로 한눈에 확인할 수 있게 한다.
Output: QAPopupWindow.xaml/cs 신규 생성, NonCoreView.xaml.cs QAButton_Click 수정
</objective>

<execution_context>
@C:/Users/kim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/NPLogic.App/Views/QAInputDialog.xaml
@src/NPLogic.App/Views/QAInputDialog.xaml.cs
@src/NPLogic.App/Views/NonCoreView.xaml.cs
@src/NPLogic.App/Views/CollateralPropertyView.xaml
@src/NPLogic.Data/Repositories/PropertyQaRepository.cs
@src/NPLogic.Core/Models/Property.cs

<interfaces>
<!-- 기존 코드의 핵심 인터페이스. Executor가 직접 참조. -->

From src/NPLogic.Core/Models/Property.cs (line 545):
```csharp
public class PropertyQa
{
    public Guid Id { get; set; }
    public Guid PropertyId { get; set; }
    public string Question { get; set; } = "";
    public string? Answer { get; set; }
    public Guid? CreatedBy { get; set; }
    public Guid? AnsweredBy { get; set; }
    public DateTime CreatedAt { get; set; }
    public DateTime? AnsweredAt { get; set; }
    public DateTime UpdatedAt { get; set; }
    public string? Part { get; set; }
    public string? BorrowerNumber { get; set; }
    public string? BorrowerName { get; set; }
    public bool IsAnswered => !string.IsNullOrWhiteSpace(Answer);
}
```

From src/NPLogic.Data/Repositories/PropertyQaRepository.cs:
```csharp
public class PropertyQaRepository
{
    public async Task<List<PropertyQa>> GetByPropertyIdAsync(Guid propertyId);
    public async Task<PropertyQa> CreateAsync(PropertyQa qa);
    public async Task<PropertyQa> UpdateAsync(PropertyQa qa);
    public async Task<bool> DeleteAsync(Guid id);
}
```

From src/NPLogic.App/ViewModels/NonCoreViewModel.cs:
```csharp
public partial class PropertyTabItem : ObservableObject
{
    public Guid PropertyId { get; set; }
    public string BorrowerNumber { get; set; } = "";
    public string BorrowerName { get; set; } = "";
}

public partial class NonCoreViewModel : ObservableObject
{
    public PropertyTabItem? SelectedPropertyTab { get; set; }
}
```

DI 등록 (App.xaml.cs line 167):
```csharp
services.AddSingleton<Data.Repositories.PropertyQaRepository>();
```

DataGrid 스타일 패턴 (CollateralPropertyView.xaml에서 반복 사용):
```xml
<DataGrid AutoGenerateColumns="False"
          CanUserAddRows="False"
          CanUserDeleteRows="False"
          IsReadOnly="False"
          HeadersVisibility="Column"
          GridLinesVisibility="Horizontal"
          BorderThickness="0"
          RowHeight="32"
          Background="White"
          RowBackground="White"
          ColumnHeaderStyle="{StaticResource SectionDataGridColumnHeaderStyle}"
          CellStyle="{StaticResource SectionDataGridCellStyle}"
          RowStyle="{StaticResource SectionDataGridRowStyle}">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QAPopupWindow XAML + 코드비하인드 생성 (DataGrid 4열 표 + 이력 로드)</name>
  <files>src/NPLogic.App/Views/QAPopupWindow.xaml, src/NPLogic.App/Views/QAPopupWindow.xaml.cs</files>
  <action>
새 Window 파일 `QAPopupWindow.xaml` + `QAPopupWindow.xaml.cs`를 `src/NPLogic.App/Views/`에 생성한다.

**XAML (QAPopupWindow.xaml):**
- namespace: `NPLogic.Views` (기존 QAInputDialog와 동일)
- Window 속성: Title="QA 질의/답변", Width="900", Height="550", WindowStartupLocation="CenterOwner", ResizeMode="CanResize", MinWidth="700", MinHeight="400"
- Background="{StaticResource BackgroundBrush}"
- 상단 헤더: 차주번호 + "QA 질의/답변" 타이틀 (MaterialDesign PackIcon Kind="CommentQuestion", Orange 색상)
- 메인 영역: DataGrid (x:Name="QaDataGrid") with 4 columns:
  1. "질의일자" — `DataGridTextColumn`, Binding=`{Binding CreatedAt, StringFormat=yyyy-MM-dd}`, Width="100", IsReadOnly=True
  2. "질의내용" — `DataGridTextColumn`, Binding=`{Binding Question}`, Width="3*", IsReadOnly=True (기존 질의는 수정 불가)
  3. "회신일자" — `DataGridTextColumn`, Binding=`{Binding AnsweredAt, StringFormat=yyyy-MM-dd}`, Width="100", IsReadOnly=True (Plan 02에서 편집 가능으로 전환)
  4. "답변내용" — `DataGridTextColumn`, Binding=`{Binding Answer}`, Width="3*", IsReadOnly=True (Plan 02에서 편집 가능으로 전환)
- DataGrid 스타일: CollateralPropertyView.xaml의 SectionDataGrid 스타일을 Window.Resources에 동일하게 정의 (SectionDataGridColumnHeaderStyle, SectionDataGridCellStyle, SectionDataGridRowStyle, SectionDataGridTextBlockStyle). BlueGray100 헤더, Height="30" 행.
- DataGrid 속성: AutoGenerateColumns="False", CanUserAddRows="False", CanUserDeleteRows="False", HeadersVisibility="Column", GridLinesVisibility="Horizontal", BorderThickness="0", RowHeight="30", Background="White", RowBackground="White"
- 하단: 닫기 버튼 (SecondaryButton 스타일), 나중에 Plan 02에서 "행 추가" + "저장" 버튼 추가할 공간 확보 (StackPanel Orientation="Horizontal" HorizontalAlignment="Right")
- DataGrid를 ScrollViewer 없이 사용 (DataGrid 자체 스크롤 활용). VerticalScrollBarVisibility="Auto"는 DataGrid에 자동 적용됨.

**코드비하인드 (QAPopupWindow.xaml.cs):**
- namespace: `NPLogic.Views`
- 생성자: `QAPopupWindow(Guid propertyId, string borrowerNumber, string borrowerName)`
- 필드: `_propertyId`, `_borrowerNumber`, `_borrowerName`, `_qaRepository` (PropertyQaRepository)
- `_qaRepository`는 `App.ServiceProvider.GetRequiredService<PropertyQaRepository>()`로 가져옴
- `Loaded` 이벤트에서 `LoadQaDataAsync()` 호출
- `LoadQaDataAsync()`: `_qaRepository.GetByPropertyIdAsync(_propertyId)` 호출, 결과를 `ObservableCollection<PropertyQa>`에 저장, `QaDataGrid.ItemsSource`에 바인딩. 정렬: CreatedAt Ascending (오래된 것 위, 새 것 아래). try/catch로 에러 시 MessageBox 표시.
- `_qaItems` 필드: `ObservableCollection<PropertyQa>` — 나중에 Plan 02에서 행 추가/저장에 사용
- 닫기 버튼 핸들러: `Close()`
- 빈 데이터 처리: QA가 0건이면 DataGrid는 빈 상태로 표시 (빈 행 없이 헤더만 보임)
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic && dotnet build NPLogic.sln --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>QAPopupWindow.xaml/cs가 생성되고, 빌드가 성공한다. DataGrid에 4열(질의일자/질의내용/회신일자/답변내용)이 정의되어 있다.</done>
</task>

<task type="auto">
  <name>Task 2: NonCoreView QAButton_Click을 QAPopupWindow로 교체</name>
  <files>src/NPLogic.App/Views/NonCoreView.xaml.cs</files>
  <action>
`NonCoreView.xaml.cs`의 `QAButton_Click` 메서드를 수정한다.

**변경 내용:**
기존 코드 (QAInputDialog 사용):
```csharp
var dialog = new QAInputDialog(currentTab);
dialog.Owner = Window.GetWindow(this);
if (dialog.ShowDialog() == true) { ... MessageBox ... }
```

새 코드:
```csharp
private void QAButton_Click(object sender, RoutedEventArgs e)
{
    try
    {
        var selectedProperty = _viewModel?.SelectedPropertyTab;
        if (selectedProperty == null || selectedProperty.PropertyId == Guid.Empty)
        {
            MessageBox.Show("물건을 먼저 선택해주세요.", "알림", MessageBoxButton.OK, MessageBoxImage.Information);
            return;
        }

        var popup = new QAPopupWindow(
            selectedProperty.PropertyId,
            selectedProperty.BorrowerNumber,
            selectedProperty.BorrowerName);
        popup.Owner = Window.GetWindow(this);
        popup.ShowDialog();
    }
    catch (Exception ex)
    {
        MessageBox.Show($"QA 팝업을 열 수 없습니다: {ex.Message}", "오류", MessageBoxButton.OK, MessageBoxImage.Error);
    }
}
```

**주의사항:**
- `using NPLogic.Views;`는 이미 같은 namespace이므로 추가 불필요
- QAInputDialog.xaml/cs 파일은 삭제하지 않는다 (다른 곳에서 참조 가능성, 안전하게 유지)
- 기존 `var currentTab = _viewModel?.ActiveTab ?? "Home";` 라인 제거 (더 이상 필요 없음)
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic && dotnet build NPLogic.sln --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>QA 버튼 클릭 시 QAPopupWindow가 열리고, 선택된 물건의 PropertyId/BorrowerNumber/BorrowerName이 전달된다. 물건 미선택 시 안내 메시지가 표시된다.</done>
</task>

</tasks>

<verification>
1. `dotnet build NPLogic.sln` — 빌드 성공
2. 앱 실행 후 비핵심 화면에서 물건 선택 -> QA 버튼 클릭 -> QAPopupWindow 팝업 열림
3. 팝업에 질의일자/질의내용/회신일자/답변내용 4열 DataGrid 표시 확인
4. 해당 물건에 기존 QA 데이터가 있으면 표에 누적 표시, 스크롤 가능 확인
5. 물건 미선택 상태에서 QA 버튼 클릭 시 안내 메시지 표시
</verification>

<success_criteria>
- QAPopupWindow가 DataGrid 4열 표로 QA 이력을 표시한다
- 과거 이력이 누적 표시되고 스크롤로 전체 확인 가능하다
- 기존 QAInputDialog 대신 QAPopupWindow가 호출된다
- 빌드가 성공한다
</success_criteria>

<output>
After completion, create `.planning/phases/01-qa-popup-table/01-01-SUMMARY.md`
</output>
