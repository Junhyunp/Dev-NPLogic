---
phase: 01-layout-header-spacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NPLogic.App/Views/EvaluationTab.xaml
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-03
  - SPC-01
  - SPC-02
  - HDR-01

must_haves:
  truths:
    - "로컬 CardStyle에 Margin=0,0,0,16 Setter가 포함되어 모든 섹션에 일관 적용된다"
    - "상가구분 Border의 Margin이 0,0,0,8에서 0,0,0,16으로 변경되었다"
    - "공통 섹션(사례지도, 사례평가, 사례로드뷰, 경매사건검색, 낙찰통계) 6개의 PrimaryBrush 헤더가 SectionHeader 이모지+텍스트로 교체되었다"
    - "교체된 섹션의 외곽 Border가 CardStyle을 사용하며 Padding=16이 적용된다"
    - "DesignHeight가 900으로 확인된다"
  artifacts:
    - path: "src/NPLogic.App/Views/EvaluationTab.xaml"
      provides: "공통 섹션 CardStyle/SectionHeader 변환"
      contains: "CardStyle"
  key_links:
    - from: "로컬 CardStyle 정의"
      to: "각 섹션 Border"
      via: "Style={StaticResource CardStyle}"
      pattern: "Style=\"\\{StaticResource CardStyle\\}\""
    - from: "SectionHeader 이모지+텍스트"
      to: "각 섹션 헤더 TextBlock"
      via: "Style={StaticResource SectionHeader}"
      pattern: "Style=\"\\{StaticResource SectionHeader\\}\""
---

<objective>
EvaluationTab.xaml의 로컬 CardStyle에 Margin Setter를 추가하고, 공통 섹션 6개(상가구분 + 사례지도/사례평가/사례로드뷰/경매사건검색/낙찰통계)의 PrimaryBrush 헤더를 SectionHeader 이모지+텍스트 패턴으로 변환한다.

Purpose: 전체 섹션 변환의 기반을 마련하고, 공통(모든 유형에 표시되는) 섹션을 우선 통일하여 구조적 골격을 확립한다.
Output: 공통 섹션 6개가 CardStyle + SectionHeader 패턴으로 변환된 EvaluationTab.xaml
</objective>

<execution_context>
@C:/Users/kim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-layout-header-spacing/01-CONTEXT.md
@.planning/phases/01-layout-header-spacing/01-RESEARCH.md

<interfaces>
<!-- EvaluationTab.xaml 로컬 리소스 (line 15-28) -->
From src/NPLogic.App/Views/EvaluationTab.xaml:
```xml
<Style x:Key="SectionHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource SectionHeader}">
    <Setter Property="FontSize" Value="{StaticResource FontSizeH4}"/>
</Style>
<Style x:Key="CardStyle" TargetType="Border" BasedOn="{StaticResource CardBorder}">
    <Setter Property="Padding" Value="16"/>
</Style>
```

<!-- Typography.xaml 참조 스타일 -->
From src/NPLogic.UI/Styles/Typography.xaml:
```
SectionHeader: FontSizeH4 (14pt), SemiBold, Margin 0,0,0,12
CardBorder: SurfaceBrush, BorderBrush, BorderThickness=1, CornerRadius=8, Padding=16, Margin=0,0,0,12, CardShadow
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CardStyle Margin 업데이트 + 상가구분 Border 통일 + DesignHeight 확인</name>
  <files>src/NPLogic.App/Views/EvaluationTab.xaml</files>
  <action>
1. 로컬 CardStyle(line 26-28)에 Margin Setter 추가:
```xml
<Style x:Key="CardStyle" TargetType="Border" BasedOn="{StaticResource CardBorder}">
    <Setter Property="Padding" Value="16"/>
    <Setter Property="Margin" Value="0,0,0,16"/>
</Style>
```
이렇게 하면 CardStyle 적용 시 자동으로 Margin="0,0,0,16" + Padding="16"이 적용된다.

2. 상가구분 Border(line 170-208)를 CardStyle로 변환:
- 기존 인라인 속성(Background, BorderBrush, BorderThickness, CornerRadius, Padding, Margin)을 `Style="{StaticResource CardStyle}"`로 교체
- 상가구분에는 PrimaryBrush 헤더가 없으므로 내부 StackPanel은 그대로 유지
- Padding이 16,10 -> 16으로 변경되나 미미한 차이(상하 6px)

3. DesignHeight=900 확인 (line 12): 이미 900이므로 변경 불필요. 확인만 수행.

주의사항:
- Typography.xaml의 CardBorder 기본 Margin(0,0,0,12)은 변경하지 않는다 (다른 탭에 영향)
- EvaluationTab.xaml 로컬 CardStyle에서만 오버라이드한다
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic && dotnet build NPLogic.sln --no-restore</automated>
  </verify>
  <done>
- CardStyle에 Margin="0,0,0,16" Setter가 추가됨
- 상가구분 Border가 CardStyle을 사용하며 Margin이 0,0,0,16으로 통일됨
- DesignHeight=900 확인됨
- 빌드 성공
  </done>
</task>

<task type="auto">
  <name>Task 2: 공통 섹션 5개 PrimaryBrush 헤더를 SectionHeader로 변환</name>
  <files>src/NPLogic.App/Views/EvaluationTab.xaml</files>
  <action>
아래 5개 공통 섹션의 PrimaryBrush+PackIcon 헤더를 SectionHeader 이모지+텍스트로 교체하고, 외곽 Border를 CardStyle로 변환한다.

변환 대상 (모두 공통 섹션, Visibility 조건 없음 또는 공통):

**1. 사례지도 및 실거래가 (line ~211)**
- 외곽 Border: 인라인 속성 -> `Style="{StaticResource CardStyle}"`
- PrimaryBrush 헤더(line ~218-237) 제거 -> Grid로 교체 (버튼 포함 패턴):
```xml
<Grid Margin="0,0,0,12">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="🗺 " FontSize="{StaticResource FontSizeH4}" VerticalAlignment="Center"/>
        <TextBlock Text="{Binding CaseMapSectionTitle}"
                   Style="{StaticResource SectionHeader}"
                   Margin="0"/>
    </StackPanel>
    <Button Content="가져오기"
            Command="{Binding LoadCaseMapCommand}"
            Padding="12,4"
            FontSize="{StaticResource FontSizeBody}"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"/>
</Grid>
```
주의: CaseMapSectionTitle은 Binding이므로 이모지를 별도 TextBlock으로 배치 (ViewModel 수정 없이 UI만 변경 원칙). Foreground="White" 제거.

**2. 사례평가 (line ~661)**
- 외곽 Border: 인라인 속성 -> `Style="{StaticResource CardStyle}"`
- PrimaryBrush 헤더 제거 -> `<TextBlock Text="📋 사례평가" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>`

**3. 사례 로드뷰 (line ~729)**
- 외곽 Border: 인라인 속성 -> `Style="{StaticResource CardStyle}"`
- Visibility 바인딩은 외곽 Border에 유지: `Visibility="{Binding IsFactoryOrCommercialOrHouseType, Converter={StaticResource BoolToVisibilityConverter}}"`
- PrimaryBrush 헤더 제거 -> `<TextBlock Text="🏠 사례 로드뷰" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>`

**4. 경매사건검색 (line ~834)**
- 외곽 Border: 인라인 속성 -> `Style="{StaticResource CardStyle}"`
- PrimaryBrush 헤더 제거 -> `<TextBlock Text="🔍 경매사건검색" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>`

**5. 낙찰통계 (line ~917)**
- 외곽 Border: 인라인 속성 -> `Style="{StaticResource CardStyle}"`
- PrimaryBrush 헤더 제거 -> `<TextBlock Text="📈 낙찰통계" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>`
- 주의: 낙찰통계 내부(line ~1009)에 PrimaryBrush가 테이블 셀 헤더로도 사용됨 -> 이것은 섹션 헤더가 아니므로 제거하지 않는다. CornerRadius="8,8,0,0" 패턴이 없는 것으로 구분.

각 변환 시 공통 패턴:
1. 외곽 Border의 인라인 속성 5개(Background, BorderBrush, BorderThickness, CornerRadius, Margin) 제거 -> `Style="{StaticResource CardStyle}"` 적용
2. PrimaryBrush 내부 Border 전체(CornerRadius="8,8,0,0" 포함) 삭제
3. PackIcon 요소 삭제
4. SectionHeader TextBlock 추가 (이모지+텍스트)
5. 버튼이 있는 경우 Foreground="White", Background="Transparent", BorderThickness="0" 속성 제거
6. StackPanel은 유지 (구조 변경 최소화)
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic && dotnet build NPLogic.sln --no-restore</automated>
  </verify>
  <done>
- 5개 공통 섹션 모두 PrimaryBrush+PackIcon 헤더가 제거되고 SectionHeader 이모지+텍스트로 교체됨
- 5개 섹션 외곽 Border가 CardStyle을 사용 (Padding=16, Margin=0,0,0,16 자동 적용)
- 사례지도 섹션의 가져오기 버튼이 정상 동작 가능 상태 (Foreground 색상 정상)
- 낙찰통계 내부 테이블 셀의 PrimaryBrush는 유지됨
- 빌드 성공
  </done>
</task>

</tasks>

<verification>
- `dotnet build NPLogic.sln` 빌드 성공
- EvaluationTab.xaml에서 `CornerRadius="8,8,0,0"` 검색 시 변환된 공통 섹션에서는 발견되지 않음 (낙찰통계 내부 셀 제외)
- `Style="{StaticResource CardStyle}"` 적용된 Border 수 확인 (상가구분 포함 6개)
</verification>

<success_criteria>
- 로컬 CardStyle이 Padding=16 + Margin=0,0,0,16으로 정의됨
- 상가구분 + 공통 5개 섹션(총 6개)이 CardStyle + SectionHeader 패턴으로 변환됨
- DesignHeight=900 확인됨
- 빌드 오류 없음
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-header-spacing/01-01-SUMMARY.md`
</output>
