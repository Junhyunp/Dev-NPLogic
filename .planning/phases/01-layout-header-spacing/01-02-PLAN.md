---
phase: 01-layout-header-spacing
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/NPLogic.App/Views/EvaluationTab.xaml
autonomous: false
requirements:
  - HDR-01
  - HDR-02
  - LAYOUT-02

must_haves:
  truths:
    - "유형별 전용 섹션(탐문, 임대분석, 수익가치, 평가결과, 지번별 평가, 회수전략, 인터림) 16개의 PrimaryBrush 헤더가 SectionHeader 이모지+텍스트로 교체되었다"
    - "4개 평가결과 섹션 헤더가 모두 동일한 이모지+SectionHeader 패턴을 사용한다"
    - "버튼이 있는 섹션(8개)의 +/- 버튼이 PrimaryBrush 제거 후에도 시각적으로 정상 표시된다"
    - "전체 22개 섹션이 CardStyle + SectionHeader 패턴으로 통일되었다"
    - "섹션 순서가 변경되지 않았다"
  artifacts:
    - path: "src/NPLogic.App/Views/EvaluationTab.xaml"
      provides: "전체 22개 섹션 CardStyle/SectionHeader 변환 완료"
      contains: "SectionHeader"
  key_links:
    - from: "평가결과 헤더 4개"
      to: "SectionHeader 스타일"
      via: "동일한 이모지+텍스트 패턴"
      pattern: "✅ 평가결과"
    - from: "버튼 있는 섹션 헤더"
      to: "Grid 레이아웃"
      via: "SectionHeader + HorizontalAlignment=Right 버튼"
      pattern: "HorizontalAlignment=\"Right\""
---

<objective>
EvaluationTab.xaml의 나머지 유형별 전용 섹션 16개(탐문 내역/결과, 임대호가분석, 무상임대분석, 탐문 수익가치, 현재 임대 수익가치, 평가결과 4종, 지번별 평가, 회수전략 요약, 인터림 상계/회수, 인터림 지출)의 PrimaryBrush 헤더를 SectionHeader 이모지+텍스트 패턴으로 변환하고, 전체 일관성을 시각적으로 검증한다.

Purpose: Phase 1의 모든 헤더/여백 통일 작업을 완료하여, 어떤 평가 유형을 열어도 동일한 시각적 패턴으로 느껴지게 한다.
Output: 전체 22개 섹션이 통일된 EvaluationTab.xaml + 시각적 검증 완료
</objective>

<execution_context>
@C:/Users/kim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-layout-header-spacing/01-CONTEXT.md
@.planning/phases/01-layout-header-spacing/01-RESEARCH.md
@.planning/phases/01-layout-header-spacing/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 정의된 로컬 CardStyle (Margin 포함) -->
```xml
<Style x:Key="CardStyle" TargetType="Border" BasedOn="{StaticResource CardBorder}">
    <Setter Property="Padding" Value="16"/>
    <Setter Property="Margin" Value="0,0,0,16"/>
</Style>
```

<!-- 변환 패턴: 버튼 없는 섹션 -->
```xml
<Border Style="{StaticResource CardStyle}">
    <StackPanel>
        <TextBlock Text="이모지 섹션명"
                   Style="{StaticResource SectionHeader}"
                   Margin="0,0,0,12"/>
        <!-- 콘텐츠 -->
    </StackPanel>
</Border>
```

<!-- 변환 패턴: +/- 버튼 있는 섹션 -->
```xml
<Border Style="{StaticResource CardStyle}">
    <StackPanel>
        <Grid Margin="0,0,0,12">
            <TextBlock Text="이모지 섹션명"
                       Style="{StaticResource SectionHeader}"
                       Margin="0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="+" ... />
                <Button Content="--" ... />
            </StackPanel>
        </Grid>
        <!-- 콘텐츠 -->
    </StackPanel>
</Border>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 유형별 전용 섹션 16개 PrimaryBrush 헤더 변환</name>
  <files>src/NPLogic.App/Views/EvaluationTab.xaml</files>
  <action>
나머지 16개 섹션의 PrimaryBrush+PackIcon 헤더를 SectionHeader 이모지+텍스트로 교체하고, 외곽 Border를 CardStyle로 변환한다. Plan 01에서 정립된 패턴을 동일하게 적용한다.

**버튼 없는 섹션 (9개) -- 단순 변환:**

| # | 섹션 | Line | 이모지 | Visibility |
|---|------|------|--------|------------|
| 6 | 탐문 내역 (공장/상가/주택) | ~1187 | 📞 | IsFactoryOrCommercialOrHouseType |
| 14 | 평가결과 (아파트/연립) | ~2378 | ✅ | !IsFactoryOrCommercialOrHouseType |
| 15 | 평가결과 (공장/창고) | ~2479 | ✅ | IsFactoryType |
| 16 | 평가결과 (상가/아파트형공장) | ~2699 | ✅ | IsCommercialType |
| 17 | 평가결과 (주택/근린시설/토지/기타) | ~2878 | ✅ | IsHouseLandType |
| 18 | 지번별 평가 (공장/창고) | ~3021 | 📍 | IsFactoryType |
| 19 | 회수 전략 요약 | ~3213 | 💰 | IsFirstPropertyInBorrower |
| 20 | 인터림 상계/회수 | ~3397 | 🔄 | 없음 |
| 21 | 인터림 지출 | ~3476 | 💸 | 없음 |

각 변환:
1. 외곽 Border -> `Style="{StaticResource CardStyle}"` (Visibility 바인딩은 유지)
2. PrimaryBrush 내부 Border(CornerRadius="8,8,0,0") 전체 삭제
3. PackIcon 요소 삭제
4. `<TextBlock Text="이모지 섹션명" Style="{StaticResource SectionHeader}" Margin="0,0,0,12"/>` 추가

**버튼 있는 섹션 (7개) -- Grid 패턴 적용:**

| # | 섹션 | Line | 이모지 | 버튼 |
|---|------|------|--------|------|
| 7 | 탐문 결과 (공장/상가/주택) | ~1272 | 📝 | +/- (Create/DestroyInquiryResultTableCommand) |
| 8 | 임대호가분석 | ~1359 | 📊 | +/- |
| 9 | 무상임대분석 | ~1549 | 🆓 | +/- |
| 10 | 탐문 수익가치 | ~1788 | 💰 | +/- |
| 11 | 현재 임대 수익가치 | ~1968 | 🏢 | +/- |
| 12 | 탐문 내역 (연립다세대) | ~2186 | 📞 | +/- |
| 13 | 탐문 결과 (연립다세대) | ~2291 | 📝 | +/- |

각 변환:
1. 외곽 Border -> `Style="{StaticResource CardStyle}"` (Visibility 바인딩 유지)
2. PrimaryBrush 내부 Border 전체 삭제
3. Grid 패턴 적용:
```xml
<Grid Margin="0,0,0,12">
    <TextBlock Text="이모지 섹션명"
               Style="{StaticResource SectionHeader}"
               Margin="0"/>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="+" FontSize="16" FontWeight="Bold"
                HorizontalAlignment="Right" VerticalAlignment="Center"
                Command="{Binding XXXCommand}"
                Visibility="{Binding XXX, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                Padding="8,0" MinHeight="0" Height="20" Cursor="Hand"
                ToolTip="XXX 테이블 생성"/>
        <Button Content="−" FontSize="16" FontWeight="Bold"
                HorizontalAlignment="Right" VerticalAlignment="Center"
                Command="{Binding XXXCommand}"
                Visibility="{Binding XXX, Converter={StaticResource BoolToVisibilityConverter}}"
                Padding="8,0" MinHeight="0" Height="20" Cursor="Hand"
                ToolTip="XXX 테이블 제거"/>
    </StackPanel>
</Grid>
```
4. 중요: 기존 버튼의 `Foreground="White"`, `Background="Transparent"`, `BorderThickness="0"` 속성을 제거한다. PrimaryBrush 배경이 없어지므로 기본 버튼 스타일이 적용되어야 한다.
5. 각 버튼의 Command, Visibility 바인딩, ToolTip은 기존 값 그대로 유지한다.

공통 주의사항:
- 섹션 순서를 절대 변경하지 않는다 (LAYOUT-02)
- 낙찰통계 내부 PrimaryBrush 셀 헤더(CornerRadius="8,8,0,0" 아님)는 그대로 유지
- 회수 전략 요약 내부 시나리오 라벨의 PrimaryBrush도 그대로 유지
- 각 섹션의 DataGrid, 바인딩, Visibility 조건은 변경하지 않는다
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic && dotnet build NPLogic.sln --no-restore</automated>
  </verify>
  <done>
- 16개 유형별 섹션 모두 PrimaryBrush+PackIcon 헤더가 제거되고 SectionHeader 이모지+텍스트로 교체됨
- 4개 평가결과 섹션(아파트/연립, 공장/창고, 상가/아파트형공장, 주택/근린시설/토지/기타) 모두 "✅ 평가결과" 동일 패턴
- 7개 버튼 있는 섹션의 +/- 버튼에서 Foreground="White" 제거됨
- 전체 22개 섹션 외곽 Border가 CardStyle 사용
- 빌드 성공
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 전체 UI 시각적 검증</name>
  <action>
사용자가 앱을 실행하여 5개 평가 유형을 전환하며 헤더/여백/레이아웃 통일 결과를 시각적으로 확인한다.
  </action>
  <what-built>EvaluationTab.xaml 전체 22개 섹션의 헤더/여백/레이아웃 통일 작업 완료</what-built>
  <how-to-verify>
1. 앱 실행: `dotnet run --project src/NPLogic.App/NPLogic.App.csproj`
2. 평가 탭으로 이동
3. 아래 5개 유형을 순서대로 전환하며 확인:
   - 아파트: 섹션 헤더가 이모지+텍스트, 여백 일관
   - 연립다세대: 탐문 내역/결과 +/- 버튼 정상 표시, 이모지 헤더
   - 공장/창고: 지번별 평가 포함 모든 섹션 헤더 통일
   - 상가/아파트형공장: 상가구분 선택 영역 정상, 임대호가/무상임대 섹션 헤더
   - 주택/근린시설/토지/기타: 평가결과 섹션 헤더

4. 확인 포인트:
   - 모든 섹션에 PrimaryBrush(파란색) 배경 헤더가 없어야 함
   - 모든 섹션 헤더가 이모지+텍스트 형태여야 함
   - 섹션 간 간격이 일관되어야 함 (16px)
   - +/- 버튼이 보이고 클릭 가능해야 함 (흰색 글자가 아님)
   - 낙찰통계 내부 테이블 헤더의 파란색 배경은 유지되어야 함
  </how-to-verify>
  <resume-signal>"approved"를 입력하거나, 발견된 문제를 설명해 주세요</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build NPLogic.sln` 빌드 성공
- EvaluationTab.xaml에서 `CornerRadius="8,8,0,0"` 검색 시 섹션 헤더에서는 발견되지 않음
- `Style="{StaticResource CardStyle}"` 적용된 Border 수 = 22개 (상가구분 포함)
- PrimaryBrush 사용 위치가 낙찰통계 내부 테이블 셀과 회수전략 시나리오 라벨에만 남음
- 5개 평가 유형 전환 시 시각적 일관성 확인 (human-verify)
</verification>

<success_criteria>
- 전체 22개 섹션이 CardStyle + SectionHeader 패턴으로 통일됨
- 4개 평가결과 섹션 헤더가 동일한 "✅ 평가결과" 패턴
- 섹션 순서 변경 없음
- 버튼이 있는 섹션의 기능 정상 동작
- 사용자 시각적 검증 통과
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-header-spacing/01-02-SUMMARY.md`
</output>
