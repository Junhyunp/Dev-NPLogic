---
phase: 03-all-tabs-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NPLogic.App/ViewModels/BorrowerOverviewViewModel.cs
  - src/NPLogic.App/ViewModels/RightsAnalysisTabViewModel.cs
  - src/NPLogic.App/ViewModels/BasicDataTabViewModel.cs
  - src/NPLogic.App/ViewModels/QASummaryViewModel.cs
  - src/NPLogic.App/Views/BorrowerOverviewView.xaml
  - src/NPLogic.App/Views/RightsAnalysisTab.xaml
  - src/NPLogic.App/Views/BasicDataTab.xaml
  - src/NPLogic.App/Views/QASummaryTab.xaml
  - src/NPLogic.App/App.xaml.cs
autonomous: true
requirements: [SCOPE-01, SCOPE-02]

must_haves:
  truths:
    - "차주개요/권리분석/기초데이터/QA집계 탭에서 토글 버튼을 누르면 250px 비고 패널이 즉시 나타난다"
    - "비고 패널의 TextBox에 여러 줄 텍스트를 입력할 수 있다"
    - "기존 저장 버튼을 누르면 비고도 함께 DB에 저장된다"
    - "탭 재진입(물건 전환) 시 저장된 비고가 복원된다"
  artifacts:
    - path: "src/NPLogic.App/ViewModels/BorrowerOverviewViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/RightsAnalysisTabViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/BasicDataTabViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/QASummaryViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/Views/BorrowerOverviewView.xaml"
      provides: "비고 사이드 패널 + 토글 버튼"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/RightsAnalysisTab.xaml"
      provides: "비고 사이드 패널 + 토글 버튼"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/BasicDataTab.xaml"
      provides: "비고 사이드 패널 + 토글 버튼"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/QASummaryTab.xaml"
      provides: "비고 사이드 패널 + 토글 버튼"
      contains: "NotePanelColumn"
  key_links:
    - from: "BorrowerOverviewViewModel.SaveBorrowerAsync"
      to: "PropertyNoteRepository.UpsertAsync"
      via: "SaveNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.UpsertAsync"
    - from: "RightsAnalysisTabViewModel.SaveAsync"
      to: "PropertyNoteRepository.UpsertAsync"
      via: "SaveNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.UpsertAsync"
    - from: "BasicDataTabViewModel.SaveBasicInfoAsync"
      to: "PropertyNoteRepository.UpsertAsync"
      via: "SaveNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.UpsertAsync"
    - from: "QASummaryViewModel.SaveQaAsync"
      to: "PropertyNoteRepository.UpsertAsync"
      via: "SaveNoteAsync 호출"
      pattern: "_propertyNoteRepository\\.UpsertAsync"
---

<objective>
저장 버튼이 있는 4개 탭(차주개요, 권리분석, 기초데이터, QA집계)에 비고 사이드 패널을 추가한다. Phase 2에서 확립한 패턴(NoteText/IsNotePanelVisible/ToggleNotePanel + 사이드 패널 XAML)을 그대로 적용하되, 각 탭의 기존 저장 커맨드에 비고 저장을 통합한다.

Purpose: 저장 버튼이 있는 탭부터 비고란을 확산하여 사용자가 저장 워크플로우에서 자연스럽게 비고를 사용할 수 있게 한다.
Output: 4개 ViewModel에 비고 로직 추가, 4개 View에 사이드 패널 XAML 추가, App.xaml.cs DI 업데이트
</objective>

<execution_context>
@C:/Users/kim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-data-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Phase 1에서 생성된 Repository -->

From src/NPLogic.Core/Models/PropertyNote.cs:
```csharp
public class PropertyNote
{
    public Guid Id { get; set; }
    public Guid PropertyId { get; set; }
    public string TabName { get; set; } = string.Empty;
    public string NoteText { get; set; } = string.Empty;
    public DateTime UpdatedAt { get; set; }
}
```

From src/NPLogic.Data/Repositories/PropertyNoteRepository.cs:
```csharp
public class PropertyNoteRepository
{
    public PropertyNoteRepository(Services.SupabaseService supabaseService);
    public async Task<List<PropertyNote>> GetByPropertyIdAsync(Guid propertyId);
    public async Task<PropertyNote?> GetByPropertyAndTabAsync(Guid propertyId, string tabName);
    public async Task<PropertyNote> UpsertAsync(PropertyNote note);
    public async Task DeleteAsync(Guid noteId);
}
```

Phase 2 ViewModel 패턴 (EvaluationTabViewModel에서 검증됨):
```csharp
// 필드
private readonly PropertyNoteRepository? _propertyNoteRepository;

// 생성자에서 App.ServiceProvider 패턴 (생성자 파라미터 변경 없이):
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;

// 또는 생성자 파라미터 추가 패턴 (SeniorRightsViewModel에서 검증됨):
PropertyNoteRepository? propertyNoteRepository = null
// 그리고 생성자에서: _propertyNoteRepository = propertyNoteRepository;

// ObservableProperty
[ObservableProperty] private string _noteText = string.Empty;
[ObservableProperty] private bool _isNotePanelVisible;

// 토글 명령
[RelayCommand] private void ToggleNotePanel() { IsNotePanelVisible = !IsNotePanelVisible; }

// 비고 로드/저장
private async Task LoadNoteAsync() { ... }
private async Task SaveNoteAsync() { ... }
```

Phase 2 View 패턴 (EvaluationTab.xaml에서 검증됨):
```xml
<!-- 루트에 2열 Grid 래퍼 -->
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <!-- 기존 콘텐츠 (Grid.Column="0") -->
    ...
    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    <Border x:Name="NotePanelColumn" Grid.Column="1" Width="250"
            Visibility="{Binding IsNotePanelVisible, Converter={StaticResource BoolToVisibilityConverter}}"
            Background="{StaticResource SurfaceColor}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0" Padding="12">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="&#x1F4DD; 비고" FontWeight="SemiBold"
                       FontSize="{StaticResource FontSizeBody}" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,8"/>
            <TextBox Text="{Binding NoteText, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     VerticalAlignment="Stretch" FontSize="{StaticResource FontSizeBody}" Padding="8"
                     BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="White"/>
        </DockPanel>
    </Border>
</Grid>

<!-- 토글 버튼 (저장 버튼 옆에 배치) -->
<Button Command="{Binding ToggleNotePanelCommand}"
        Style="{StaticResource MaterialDesignIconButton}"
        ToolTip="비고 패널 열기/닫기" Width="32" Height="32" Padding="0" Margin="8,0,0,0">
    <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
</Button>
```

대상 ViewModel 구조:
- BorrowerOverviewViewModel: 생성자(BorrowerRepository, PropertyRepository, PropertyQaRepository, AuthService, PermissionService), `SelectedProperty?.Id`로 물건 ID 접근, `SaveBorrowerAsync()` [RelayCommand]
- RightsAnalysisTabViewModel: 생성자(RightAnalysisRepository, RegistryRepository), `_propertyId` (Guid?), `SaveAsync()` [RelayCommand], `LoadDataAsync()`
- BasicDataTabViewModel: 생성자(ProgramRepository), 프로그램 레벨 — 물건 ID 없음, `SaveBasicInfoAsync()` [RelayCommand], `InitializeAsync(Guid programId)`
- QASummaryViewModel: 생성자(PropertyQaRepository, QaNotificationRepository, PropertyRepository, AuthService, PermissionService), 프로그램 레벨 — 물건 ID 없음, `SaveQaAsync()` [RelayCommand], `InitializeAsync()`

DashboardView에서 탭 로드 방식:
- 권리분석: `rightsViewModel.SetPropertyId(property.Id)` — 물건 ID 직접 설정됨
- 기초데이터: `serviceProvider.GetService<ProgramSettingsTab>()` — BasicDataTab이 아닌 ProgramSettingsTab이 로드됨 (주의!)
- QA집계: `serviceProvider.GetService<QASummaryTab>()` — ViewModel 설정 없이 바로 view만 반환

NonCoreView에서 탭 로드 방식:
- 차주개요: `borrowerVm.SetSelectedPropertyAsync(property)` — 물건 property 전달됨
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4개 ViewModel에 비고 속성/명령/로드/저장 로직 추가 + DI 업데이트</name>
  <files>
    src/NPLogic.App/ViewModels/BorrowerOverviewViewModel.cs
    src/NPLogic.App/ViewModels/RightsAnalysisTabViewModel.cs
    src/NPLogic.App/ViewModels/BasicDataTabViewModel.cs
    src/NPLogic.App/ViewModels/QASummaryViewModel.cs
    src/NPLogic.App/App.xaml.cs
  </files>
  <action>
Phase 2에서 확립한 패턴을 4개 ViewModel에 적용한다. 각 ViewModel 특성에 맞게 PropertyNoteRepository 주입 방식과 물건 ID 접근 방식을 조정한다.

**공통: PropertyNoteRepository 주입 방식 결정**

모든 4개 ViewModel에서 `App.ServiceProvider` 패턴을 사용한다 (EvaluationTabViewModel 선례). 이유: 생성자 파라미터를 바꾸면 App.xaml.cs DI 등록도 함께 변경해야 하고, 기존 factory 패턴이 있는 ViewModel은 파라미터 추가가 번거롭다. App.ServiceProvider 패턴이면 DI 등록 변경 없이 ViewModel 내부에서 해결된다.

**A. BorrowerOverviewViewModel.cs:**

1. using 추가 (없으면): `using NPLogic.Data.Repositories;` (이미 있음), `using NPLogic.Core.Models;` (이미 있음)

2. 필드 추가 (line 23 부근, 기존 readonly 필드들 아래):
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
```

3. 생성자 끝에서 (line 141, `InitializeFilterOptions()` 직후):
```csharp
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;
```

4. ObservableProperty 추가 (IsLoading 근처, line 75 부근):
```csharp
[ObservableProperty]
private string _noteText = string.Empty;

[ObservableProperty]
private bool _isNotePanelVisible;
```

5. ToggleNotePanel 명령 추가:
```csharp
[RelayCommand]
private void ToggleNotePanel()
{
    IsNotePanelVisible = !IsNotePanelVisible;
}
```

6. LoadNoteAsync 추가 — `SelectedProperty?.Id`를 사용하고 tabName은 `"borrower_overview"`:
```csharp
private async Task LoadNoteAsync()
{
    if (_propertyNoteRepository == null || SelectedProperty == null) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(SelectedProperty.Id, "borrower_overview");
        NoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[BorrowerOverview] 비고 로드 실패: {ex.Message}");
    }
}
```

7. SaveNoteAsync 추가:
```csharp
private async Task SaveNoteAsync()
{
    if (_propertyNoteRepository == null || SelectedProperty == null) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new PropertyNote
        {
            PropertyId = SelectedProperty.Id,
            TabName = "borrower_overview",
            NoteText = NoteText ?? string.Empty
        });
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[BorrowerOverview] 비고 저장 실패: {ex.Message}");
    }
}
```

8. `SetSelectedPropertyAsync` 메서드 (실제 물건 설정 시점) 또는 `InitializeAsync()` 안에서 `LoadSingleBorrowerDataAsync()` 호출 직후에 `await LoadNoteAsync();` 추가.
   BorrowerOverview는 `IsSingleBorrowerMode` 분기가 있음. `LoadSingleBorrowerDataAsync()` 리턴 직후 (line 171 `return;` 직전)에 `await LoadNoteAsync();` 추가.

9. `SaveBorrowerAsync()` (line 789) 안에서 `LoadBorrowersAsync()` 호출 직후, ToastService 직전에 `await SaveNoteAsync();` 추가.

**B. RightsAnalysisTabViewModel.cs:**

1. 필드 추가 (line 24 부근, `_propertyId` 아래):
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
```

2. 생성자 끝에서 (line 66, `InitializeSeniorRightsItems()` 직후):
```csharp
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;
```

3. ObservableProperty 추가 (기존 ObservableProperty들 근처):
```csharp
[ObservableProperty]
private string _noteText = string.Empty;

[ObservableProperty]
private bool _isNotePanelVisible;
```

4. ToggleNotePanel, LoadNoteAsync, SaveNoteAsync 추가 — `_propertyId`를 사용하고 tabName은 `"rights_analysis"`:
```csharp
[RelayCommand]
private void ToggleNotePanel()
{
    IsNotePanelVisible = !IsNotePanelVisible;
}

private async Task LoadNoteAsync()
{
    if (_propertyNoteRepository == null || _propertyId == null) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(_propertyId.Value, "rights_analysis");
        NoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[RightsAnalysis] 비고 로드 실패: {ex.Message}");
    }
}

private async Task SaveNoteAsync()
{
    if (_propertyNoteRepository == null || _propertyId == null) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new NPLogic.Core.Models.PropertyNote
        {
            PropertyId = _propertyId.Value,
            TabName = "rights_analysis",
            NoteText = NoteText ?? string.Empty
        });
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[RightsAnalysis] 비고 저장 실패: {ex.Message}");
    }
}
```

5. `LoadDataAsync()` (line 106)에서 데이터 로드 완료 후, IsLoading = false 직전에 `await LoadNoteAsync();` 추가.

6. `SaveAsync()` (line 376)에서 기존 저장 로직 완료 후, ToastService 직전에 `await SaveNoteAsync();` 추가.

**C. BasicDataTabViewModel.cs:**

BasicDataTab은 프로그램 레벨이므로 물건 ID가 없다. 하지만 DashboardView에서 실제로는 ProgramSettingsTab이 로드된다 (BasicDataTab은 NonCoreView에서 사용되지 않고 DashboardView에서도 "basicdata" 케이스에서 ProgramSettingsTab을 로드).

그러나 CONTEXT.md의 결정에 따라 BasicDataTab에 비고란을 추가해야 한다. 이 탭은 프로그램 수준이므로 비고도 프로그램 수준으로 처리한다. `_programId`를 property_id로 사용한다 (property_notes 테이블에 program_id를 저장하는 방식).

1. using 추가: `using NPLogic.Data.Repositories;` (확인 후 없으면 추가)

2. 필드 추가:
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
```

3. 생성자 끝에서:
```csharp
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;
```

4. ObservableProperty 추가:
```csharp
[ObservableProperty]
private string _noteText = string.Empty;

[ObservableProperty]
private bool _isNotePanelVisible;
```

5. ToggleNotePanel, LoadNoteAsync, SaveNoteAsync 추가 — `_programId`를 propertyId로 사용하고 tabName은 `"basic_data"`:
```csharp
[RelayCommand]
private void ToggleNotePanel()
{
    IsNotePanelVisible = !IsNotePanelVisible;
}

private async Task LoadNoteAsync()
{
    if (_propertyNoteRepository == null || _programId == Guid.Empty) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(_programId, "basic_data");
        NoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[BasicData] 비고 로드 실패: {ex.Message}");
    }
}

private async Task SaveNoteAsync()
{
    if (_propertyNoteRepository == null || _programId == Guid.Empty) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new NPLogic.Core.Models.PropertyNote
        {
            PropertyId = _programId,
            TabName = "basic_data",
            NoteText = NoteText ?? string.Empty
        });
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[BasicData] 비고 저장 실패: {ex.Message}");
    }
}
```

6. `InitializeAsync(Guid programId)` (line 137)에서 `LoadProgramDataAsync()` 완료 직후에 `await LoadNoteAsync();` 추가.

7. `SaveBasicInfoAsync()` (line 203)에서 성공 저장 후 (IsLoading = false 직전)에 `await SaveNoteAsync();` 추가.

**D. QASummaryViewModel.cs:**

QASummaryViewModel도 프로그램 레벨이다. 비고를 저장하려면 ID가 필요한데, 특정 물건이 아닌 QA 집계 전체에 대한 비고이다. 이 경우 AuthService의 세션에서 userId를 사용하여 사용자별+탭 비고로 저장하거나, 차선으로 Guid.Empty를 사용.

실용적 접근: QASummaryViewModel은 `InitializeAsync()`에서 세션 userId를 가져오므로, userId를 property_id로 사용한다 (사용자별 QA 비고).

1. using 추가: `using NPLogic.Data.Repositories;` (확인 후 없으면 추가)

2. 필드 추가 (line 29 부근):
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
private Guid _noteOwnerId = Guid.Empty;
```

3. 생성자 끝에서:
```csharp
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;
```

4. ObservableProperty + ToggleNotePanel + LoadNoteAsync + SaveNoteAsync 추가.
   tabName은 `"qa_summary"`, propertyId는 `_noteOwnerId`:
```csharp
[ObservableProperty]
private string _noteText = string.Empty;

[ObservableProperty]
private bool _isNotePanelVisible;

[RelayCommand]
private void ToggleNotePanel()
{
    IsNotePanelVisible = !IsNotePanelVisible;
}

private async Task LoadNoteAsync()
{
    if (_propertyNoteRepository == null || _noteOwnerId == Guid.Empty) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(_noteOwnerId, "qa_summary");
        NoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[QASummary] 비고 로드 실패: {ex.Message}");
    }
}

private async Task SaveNoteAsync()
{
    if (_propertyNoteRepository == null || _noteOwnerId == Guid.Empty) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new NPLogic.Core.Models.PropertyNote
        {
            PropertyId = _noteOwnerId,
            TabName = "qa_summary",
            NoteText = NoteText ?? string.Empty
        });
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[QASummary] 비고 저장 실패: {ex.Message}");
    }
}
```

5. `InitializeAsync()` (line 117)에서 세션 userId 추출 후:
```csharp
if (session?.User?.Id != null)
{
    var userId = Guid.Parse(session.User.Id);
    CurrentUser = new User { Id = userId };
    _noteOwnerId = userId;  // 추가
}
```
`LoadBorrowerSummariesAsync()` 완료 직후에 `await LoadNoteAsync();` 추가.

6. `SaveQaAsync()` (line 336)에서 기존 저장 로직 완료 후 `await SaveNoteAsync();` 추가.

**E. App.xaml.cs DI 업데이트:**

이 Plan에서는 App.ServiceProvider 패턴을 사용하므로 DI 등록 변경이 불필요하다. 하지만 4개 ViewModel에 `using NPLogic.Data.Repositories;`가 있는지 확인하고 없으면 추가.

**주의사항:**
- `SaveNoteAsync`에서 NoteText가 null이면 빈 문자열로 대체 (`NoteText ?? string.Empty`) — null 저장 방지
- Phase 2 버그 교훈: 빈 문자열이어도 저장하도록 함 (이전 비고를 지우는 케이스 대응). `if (string.IsNullOrWhiteSpace(NoteText)) return;`는 사용하지 않음.
- BasicDataTab/QASummaryTab은 물건 레벨이 아닌 프로그램/사용자 레벨이므로, property_id에 programId/userId를 사용. Phase 4에서 종합 표시 시 이 특수 케이스를 고려해야 함.
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic &amp;&amp; rtk dotnet build NPLogic.sln --no-restore</automated>
  </verify>
  <done>
4개 ViewModel 모두에 NoteText/IsNotePanelVisible/ToggleNotePanel 속성과 LoadNoteAsync/SaveNoteAsync 메서드가 추가되고, 기존 Save/Load 흐름에 비고 저장/로드가 통합되어 빌드 성공
  </done>
</task>

<task type="auto">
  <name>Task 2: 4개 View에 사이드 패널 XAML + 토글 버튼 추가</name>
  <files>
    src/NPLogic.App/Views/BorrowerOverviewView.xaml
    src/NPLogic.App/Views/RightsAnalysisTab.xaml
    src/NPLogic.App/Views/BasicDataTab.xaml
    src/NPLogic.App/Views/QASummaryTab.xaml
  </files>
  <action>
Phase 2에서 검증된 패턴을 4개 View에 적용한다. 각 View의 루트 레이아웃을 2열 Grid로 감싸고, 저장 버튼 옆에 토글 버튼을 추가한다.

**공통 사이드 패널 XAML** (4개 View에 동일 구조):
```xml
<!-- 비고 사이드 패널 -->
<Border x:Name="NotePanelColumn" Grid.Column="1"
        Width="250"
        Visibility="{Binding IsNotePanelVisible, Converter={StaticResource BoolToVisibilityConverter}}"
        Background="{StaticResource SurfaceColor}"
        BorderBrush="{StaticResource BorderBrush}"
        BorderThickness="1,0,0,0"
        Padding="12">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top"
                   Text="&#x1F4DD; 비고"
                   FontWeight="SemiBold"
                   FontSize="{StaticResource FontSizeBody}"
                   Foreground="{StaticResource PrimaryBrush}"
                   Margin="0,0,0,8"/>
        <TextBox Text="{Binding NoteText, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 VerticalAlignment="Stretch"
                 FontSize="{StaticResource FontSizeBody}"
                 Padding="8"
                 BorderBrush="{StaticResource BorderBrush}"
                 BorderThickness="1"
                 Background="White"/>
    </DockPanel>
</Border>
```

**공통 토글 버튼**:
```xml
<Button Command="{Binding ToggleNotePanelCommand}"
        Style="{StaticResource MaterialDesignIconButton}"
        ToolTip="비고 패널 열기/닫기"
        Width="32" Height="32"
        Padding="0"
        Margin="8,0,0,0">
    <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
</Button>
```

**A. BorrowerOverviewView.xaml:**

현재 구조: `<ScrollViewer>` -> `<Grid Margin="12,8,12,12">` (5 Row)
변경: `<ScrollViewer>` 위에 2열 Grid 래퍼 추가

1. UserControl 직접 자식을 다음 구조로 변경:
```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <!-- 기존 ScrollViewer (Grid.Column="0" 추가) -->
    <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        ... (기존 내용 그대로)
    </ScrollViewer>

    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    (위 공통 패턴)
</Grid>
```

2. 저장/삭제 버튼 StackPanel (line 768) 안에 저장 버튼 뒤에 토글 버튼 추가:
```xml
<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
    <Button Content="삭제" ... />
    <Button Content="저장" Command="{Binding SaveBorrowerCommand}" ... />
    <Button Command="{Binding ToggleNotePanelCommand}"
            Style="{StaticResource MaterialDesignIconButton}"
            ToolTip="비고 패널 열기/닫기"
            Width="32" Height="32" Padding="0" Margin="8,0,0,0">
        <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
    </Button>
</StackPanel>
```

materialDesign xmlns이 없으면 UserControl에 추가: `xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"`

**B. RightsAnalysisTab.xaml:**

현재 구조: `<UserControl.Resources>` 다음에 `<ScrollViewer>` -> `<Grid>` (main content)
확인 필요: 루트 직접 자식이 Resources 다음에 무엇인지 확인. `<ScrollViewer>` 또는 `<Grid>`.

변경: UserControl의 직접 자식을 2열 Grid로 감싼다.

1. 기존 루트 콘텐츠 요소 위에 2열 Grid 래퍼:
```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <!-- 기존 콘텐츠 (Grid.Column="0" 추가) -->
    <ScrollViewer Grid.Column="0" ...>
        ...
    </ScrollViewer>

    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    (위 공통 패턴)
</Grid>
```

2. 저장 버튼 StackPanel (line 501)에 토글 버튼 추가:
```xml
<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
    <Button Content="자동 계산" ... />
    <Button Content="저장" Command="{Binding SaveCommand}" ... />
    <Button Command="{Binding ToggleNotePanelCommand}"
            Style="{StaticResource MaterialDesignIconButton}"
            ToolTip="비고 패널 열기/닫기"
            Width="32" Height="32" Padding="0" Margin="8,0,0,0">
        <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
    </Button>
</StackPanel>
```

materialDesign xmlns 확인 후 없으면 추가.

**C. BasicDataTab.xaml:**

현재 구조: `<Grid Background="{StaticResource BackgroundBrush}">` -> `<ScrollViewer>` -> `<Grid Margin="24">` (3 Column: 좌측 2*, 간격 24, 우측 *)
변경: 루트 Grid를 2열로 확장하되, 기존 구조를 Grid.Column="0"에 넣고 사이드 패널을 Grid.Column="1"에 추가.

1. 루트 `<Grid Background="...">` 를 2열로 변경:
```xml
<Grid Background="{StaticResource BackgroundBrush}">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <!-- 기존 ScrollViewer (Grid.Column="0" 추가) -->
    <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        ... (기존 내용 그대로)
    </ScrollViewer>

    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    (위 공통 패턴)
</Grid>
```

2. 저장 버튼 (line 398) 옆에 토글 버튼 추가. 기존 `<Button Content="저장" Command="{Binding SaveBasicInfoCommand}" .../>` 를 StackPanel로 감싸거나, 이미 StackPanel 안이면 옆에 추가:
```xml
<Button Command="{Binding ToggleNotePanelCommand}"
        Style="{StaticResource MaterialDesignIconButton}"
        ToolTip="비고 패널 열기/닫기"
        Width="32" Height="32" Padding="0" Margin="8,0,0,0">
    <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
</Button>
```

materialDesign xmlns 확인 후 없으면 추가.

**D. QASummaryTab.xaml:**

현재 구조: `<UserControl.Resources>` 다음에 루트 콘텐츠 요소.
저장 버튼: `SaveQaCommand` (line 689)

변경: 루트 콘텐츠를 2열 Grid로 감싸고, 사이드 패널 추가. 저장 버튼 옆에 토글 추가.

materialDesign xmlns은 이미 있음 (`xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"`).

**주의사항:**
- BoolToVisibilityConverter는 App.xaml 리소스에 이미 등록됨 (Phase 2에서 확인)
- MaterialDesignIconButton 스타일은 MaterialDesignInXAML 패키지에 포함 (이미 설치)
- 사이드 패널 Width="250"은 Visibility="Collapsed"일 때 Auto Column이므로 자동 0px
- materialDesign xmlns이 없는 View에는 반드시 추가해야 함 — BorrowerOverviewView, RightsAnalysisTab, BasicDataTab에서 확인 필요
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic &amp;&amp; rtk dotnet build NPLogic.sln --no-restore</automated>
  </verify>
  <done>
4개 View에 사이드 패널 XAML과 토글 버튼이 추가되어 빌드 성공. 토글 버튼 클릭 시 250px 사이드 패널이 나타나고 사라짐
  </done>
</task>

</tasks>

<verification>
- `dotnet build NPLogic.sln` 성공
- 4개 ViewModel에 NoteText/IsNotePanelVisible 속성 존재
- 4개 View에 NotePanelColumn Border와 ToggleNotePanelCommand 토글 Button 존재
- 기존 Save 명령에 SaveNoteAsync 호출 통합
- 기존 Load 흐름에 LoadNoteAsync 호출 통합
</verification>

<success_criteria>
1. 차주개요/권리분석/기초데이터/QA집계 4개 탭에서 토글 버튼 클릭 시 250px 사이드 패널 즉시 표시/숨김
2. 사이드 패널 TextBox에 여러 줄 자유 입력 가능
3. 기존 저장 버튼 클릭 시 비고가 DB에 저장됨
4. 탭 재진입 시 저장된 비고가 자동 로드됨
5. 패널 닫힘 상태에서 메인 레이아웃 정상
</success_criteria>

<output>
After completion, create `.planning/phases/03-all-tabs-notes/03-01-SUMMARY.md`
</output>
