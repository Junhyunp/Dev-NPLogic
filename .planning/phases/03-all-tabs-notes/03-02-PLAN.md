---
phase: 03-all-tabs-notes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/NPLogic.App/ViewModels/LoanSheetViewModel.cs
  - src/NPLogic.App/ViewModels/AuctionScheduleDetailViewModel.cs
  - src/NPLogic.App/ViewModels/InterimTabViewModel.cs
  - src/NPLogic.App/ViewModels/CashFlowSummaryViewModel.cs
  - src/NPLogic.App/ViewModels/XnpvComparisonViewModel.cs
  - src/NPLogic.App/ViewModels/PropertyDetailViewModel.cs
  - src/NPLogic.App/Views/Loan/LoanSheetView.xaml
  - src/NPLogic.App/Views/Loan/LoanSheetView.xaml.cs
  - src/NPLogic.App/Views/AuctionPublicSaleView.xaml
  - src/NPLogic.App/Views/AuctionPublicSaleView.xaml.cs
  - src/NPLogic.App/Views/InterimTab.xaml
  - src/NPLogic.App/Views/InterimTab.xaml.cs
  - src/NPLogic.App/Views/CashFlowSummaryView.xaml
  - src/NPLogic.App/Views/CashFlowSummaryView.xaml.cs
  - src/NPLogic.App/Views/XnpvComparisonView.xaml
  - src/NPLogic.App/Views/XnpvComparisonView.xaml.cs
  - src/NPLogic.App/Views/ClosingTab.xaml
  - src/NPLogic.App/Views/ClosingTab.xaml.cs
autonomous: false
requirements: [SCOPE-01, SCOPE-02]

must_haves:
  truths:
    - "Loan/경공매일정/인터림/현금흐름집계/NPV비교/마감 6개 탭에서 토글 버튼을 누르면 250px 비고 패널이 즉시 나타난다"
    - "비고 패널의 TextBox에 여러 줄 텍스트를 입력할 수 있다"
    - "비고 TextBox에서 포커스를 잃으면 비고가 자동으로 DB에 저장된다"
    - "탭 재진입(물건 전환) 시 저장된 비고가 복원된다"
    - "10개 탭 전체에서 비고가 탭별로 독립적으로 저장/조회된다 (탭 간 간섭 없음)"
  artifacts:
    - path: "src/NPLogic.App/ViewModels/LoanSheetViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/AuctionScheduleDetailViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/InterimTabViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/CashFlowSummaryViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/XnpvComparisonViewModel.cs"
      provides: "NoteText, IsNotePanelVisible, ToggleNotePanel, LoadNoteAsync, SaveNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/ViewModels/PropertyDetailViewModel.cs"
      provides: "ClosingNoteText, IsClosingNotePanelVisible, ToggleClosingNotePanel, LoadClosingNoteAsync, SaveClosingNoteAsync"
      contains: "PropertyNoteRepository"
    - path: "src/NPLogic.App/Views/Loan/LoanSheetView.xaml"
      provides: "비고 사이드 패널 + 토글 버튼 + LostFocus 자동 저장"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/AuctionPublicSaleView.xaml"
      provides: "비고 사이드 패널 + 토글 버튼 + LostFocus 자동 저장"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/InterimTab.xaml"
      provides: "비고 사이드 패널 + 토글 버튼 + LostFocus 자동 저장"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/CashFlowSummaryView.xaml"
      provides: "비고 사이드 패널 + 토글 버튼 + LostFocus 자동 저장"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/XnpvComparisonView.xaml"
      provides: "비고 사이드 패널 + 토글 버튼 + LostFocus 자동 저장"
      contains: "NotePanelColumn"
    - path: "src/NPLogic.App/Views/ClosingTab.xaml"
      provides: "비고 사이드 패널 + 토글 버튼 + LostFocus 자동 저장"
      contains: "NotePanelColumn"
  key_links:
    - from: "LoanSheetView NoteTextBox_LostFocus"
      to: "LoanSheetViewModel.SaveNoteAsync"
      via: "code-behind에서 ViewModel 메서드 호출"
      pattern: "SaveNoteCommand\\.Execute|SaveNoteAsync"
    - from: "AuctionPublicSaleView NoteTextBox_LostFocus"
      to: "AuctionScheduleDetailViewModel.SaveNoteAsync"
      via: "code-behind에서 ViewModel 메서드 호출"
      pattern: "SaveNoteCommand\\.Execute|SaveNoteAsync"
---

<objective>
저장 버튼이 없는 6개 탭(Loan, 경공매일정, 인터림, 현금흐름집계, NPV비교, 마감)에 비고 사이드 패널을 추가한다. 이 탭들은 저장 버튼이 없으므로 TextBox의 LostFocus 이벤트로 자동 저장한다. 마지막에 전체 10개 탭 동작을 검증하는 체크포인트를 포함한다.

Purpose: 저장 버튼이 없는 탭에도 비고란을 확산하여, 전체 탭에서 비고 기능을 사용할 수 있게 한다. LostFocus 자동 저장으로 사용자가 별도 저장 조작 없이 자연스럽게 비고를 저장한다.
Output: 6개 ViewModel에 비고 로직 추가, 6개 View에 사이드 패널 + LostFocus 자동 저장 추가
</objective>

<execution_context>
@C:/Users/kim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-data-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Phase 1에서 생성된 Repository -->

From src/NPLogic.Data/Repositories/PropertyNoteRepository.cs:
```csharp
public class PropertyNoteRepository
{
    public async Task<PropertyNote?> GetByPropertyAndTabAsync(Guid propertyId, string tabName);
    public async Task<PropertyNote> UpsertAsync(PropertyNote note);
}
```

Phase 2 ViewModel 패턴 (검증됨):
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;

// App.ServiceProvider 패턴:
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;

[ObservableProperty] private string _noteText = string.Empty;
[ObservableProperty] private bool _isNotePanelVisible;

[RelayCommand] private void ToggleNotePanel() { IsNotePanelVisible = !IsNotePanelVisible; }
private async Task LoadNoteAsync() { ... }
private async Task SaveNoteAsync() { ... }

// LostFocus 자동 저장용 Public 명령 (code-behind에서 호출):
[RelayCommand]
private async Task SaveNote() { await SaveNoteAsync(); }
```

LostFocus 자동 저장 패턴 (code-behind):
```csharp
private void NoteTextBox_LostFocus(object sender, RoutedEventArgs e)
{
    if (DataContext is SomeViewModel vm && vm.SaveNoteCommand.CanExecute(null))
    {
        vm.SaveNoteCommand.Execute(null);
    }
}
```

대상 ViewModel 구조:
- LoanSheetViewModel: `SelectedProperty?.Id`로 물건 ID 접근, `InitializeAsync()`, `SetSelectedPropertyAsync(Property)`, 생성자(LoanRepository, BorrowerRepository, CreditGuaranteeRepository, AuthService, ExcelService)
- AuctionScheduleDetailViewModel: `PropertyId` (Guid? ObservableProperty), `SetPropertyId(Guid)`, `InitializeAsync()`, 생성자(SupabaseService, AuctionScheduleRepository, PropertyRepository, EvaluationRepository, RightAnalysisRepository)
- InterimTabViewModel: 프로그램 레벨 — `InitializeAsync(Guid programId, string? borrowerNumber)`, 생성자(InterimRepository, PropertyRepository, ExcelService)
- CashFlowSummaryViewModel: 프로그램 레벨 — 물건 ID 없음, `InitializeAsync()`, 생성자(BorrowerRepository, LoanRepository, ExcelService)
- XnpvComparisonViewModel: 프로그램 레벨 — 물건 ID 없음, `InitializeAsync()`, 생성자(BorrowerRepository, LoanRepository, ExcelService)
- ClosingTab: DataContext = PropertyDetailViewModel (이미 `_propertyNoteRepository` 필드 있음, Phase 2에서 추가됨), `_propertyId` (Guid?)

View 루트 레이아웃:
- LoanSheetView: `<UserControl.Resources>` -> `<Grid Background="{StaticResource BackgroundColor}">` (3 Row)
- AuctionPublicSaleView: `<UserControl.Resources>` -> 루트 콘텐츠 (AuctionSchedule + PublicSale 두 ViewModel을 사용하는 복합 뷰)
- InterimTab: `<UserControl.Resources>` -> `<Grid>` (로딩 오버레이 포함)
- CashFlowSummaryView: `<Grid>` (3 Row)
- XnpvComparisonView: `<Grid Margin="12,8,12,12">` (3 Row)
- ClosingTab: `<ScrollViewer>` -> `<Grid Margin="24">` (4 Row), d:DataContext = PropertyDetailViewModel

NonCoreView 탭 로드:
- Loan: `loanSheetVm.SetSelectedPropertyAsync(property)` — property 전달됨 (SelectedProperty.Id 사용 가능)
- AuctionSchedule: `auctionVm.SetPropertyId(selectedPropertyId.Value)` — PropertyId 직접 설정
- Interim: `interimVm.InitializeAsync(programId, borrowerNumber)` — programId만, 물건 ID 없음
- CashFlow: `serviceProvider.GetRequiredService<CashFlowSummaryView>()` — 물건 컨텍스트 없음
- XnpvComparison: `serviceProvider.GetRequiredService<XnpvComparisonView>()` — 물건 컨텍스트 없음

DashboardView 탭 로드:
- Closing: `closingTab.DataContext = new { Property = property }` — 익명 객체 (ViewModel 아님!)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 6개 ViewModel에 비고 속성/명령/로드/저장 로직 추가</name>
  <files>
    src/NPLogic.App/ViewModels/LoanSheetViewModel.cs
    src/NPLogic.App/ViewModels/AuctionScheduleDetailViewModel.cs
    src/NPLogic.App/ViewModels/InterimTabViewModel.cs
    src/NPLogic.App/ViewModels/CashFlowSummaryViewModel.cs
    src/NPLogic.App/ViewModels/XnpvComparisonViewModel.cs
    src/NPLogic.App/ViewModels/PropertyDetailViewModel.cs
  </files>
  <action>
6개 ViewModel에 비고 기능을 추가한다. 저장 버튼이 없는 탭이므로 `SaveNoteCommand`를 RelayCommand로 노출하여 code-behind의 LostFocus 이벤트에서 호출할 수 있게 한다.

**모든 ViewModel 공통 패턴:**
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;

// 생성자에서:
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;

[ObservableProperty]
private string _noteText = string.Empty;

[ObservableProperty]
private bool _isNotePanelVisible;

[RelayCommand]
private void ToggleNotePanel()
{
    IsNotePanelVisible = !IsNotePanelVisible;
}

// LostFocus 자동 저장을 위한 Public RelayCommand
[RelayCommand]
private async Task SaveNote()
{
    await SaveNoteAsync();
}
```

**A. LoanSheetViewModel.cs:**

1. 필드 추가 (line 30 부근):
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
```

2. 생성자 끝에서 (line 217, `InitializeAllocationItems()` 직후):
```csharp
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;
```

3. ObservableProperty + ToggleNotePanel + SaveNoteCommand + LoadNoteAsync + SaveNoteAsync 추가.
   물건 ID: `SelectedProperty?.Id`, tabName: `"loan"`

```csharp
private async Task LoadNoteAsync()
{
    if (_propertyNoteRepository == null || SelectedProperty == null) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(SelectedProperty.Id, "loan");
        NoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[Loan] 비고 로드 실패: {ex.Message}");
    }
}

private async Task SaveNoteAsync()
{
    if (_propertyNoteRepository == null || SelectedProperty == null) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new NPLogic.Core.Models.PropertyNote
        {
            PropertyId = SelectedProperty.Id,
            TabName = "loan",
            NoteText = NoteText ?? string.Empty
        });
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[Loan] 비고 저장 실패: {ex.Message}");
    }
}
```

4. `SetSelectedPropertyAsync` (line 271) 또는 `InitializeAsync()` (line 238) 끝에서 `await LoadNoteAsync();` 추가.
   가장 적절한 위치: `SetSelectedPropertyAsync` 안의 `await InitializeAsync();` 직후에 `await LoadNoteAsync();`.
   또한 일반 모드 `InitializeAsync()` 끝 (IsLoading = false 직전)에도 `await LoadNoteAsync();`.

**B. AuctionScheduleDetailViewModel.cs:**

1. 필드 추가 (line 28 부근, `_propertyId` 아래):
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
```

2. 파라미터 있는 생성자 끝에서 (line 378-391 파라미터 있는 생성자):
```csharp
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;
```

3. ObservableProperty + ToggleNotePanel + SaveNoteCommand + LoadNoteAsync + SaveNoteAsync 추가.
   물건 ID: `PropertyId` (Guid? ObservableProperty), tabName: `"auction_schedule"`

```csharp
private async Task LoadNoteAsync()
{
    if (_propertyNoteRepository == null || !PropertyId.HasValue) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(PropertyId.Value, "auction_schedule");
        NoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[AuctionSchedule] 비고 로드 실패: {ex.Message}");
    }
}

private async Task SaveNoteAsync()
{
    if (_propertyNoteRepository == null || !PropertyId.HasValue) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new NPLogic.Core.Models.PropertyNote
        {
            PropertyId = PropertyId.Value,
            TabName = "auction_schedule",
            NoteText = NoteText ?? string.Empty
        });
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[AuctionSchedule] 비고 저장 실패: {ex.Message}");
    }
}
```

4. `InitializeAsync()` (line 392) 끝에서 `await LoadNoteAsync();` 추가.
   또는 `OnPropertyIdChanged` (line 837)에서 `_ = LoadNoteAsync();` 추가 (물건 전환 시 비고 리로드).

**C. InterimTabViewModel.cs:**

프로그램 레벨이므로 물건 ID가 없다. NonCoreView에서 `selectedPropertyId`가 전달되긴 하지만 InterimTabViewModel에는 도달하지 않는다.
접근: `_notePropertyId` 필드를 추가하고 public 메서드 `SetNotePropertyId(Guid id)`로 설정. NonCoreView.CreateAndCacheTabViewAsync에서 호출.

하지만 NonCoreView.xaml.cs를 수정하는 것은 이 Plan의 scope가 커진다. 대안: App.ServiceProvider 패턴처럼, InterimTab도 `_programId`를 property_id로 사용 (기초데이터와 동일 접근).

`_programId`를 사용한다. InterimTabViewModel에는 `InitializeAsync(Guid programId, ...)` 파라미터로 programId가 전달됨.

1. 필드 추가:
```csharp
private readonly PropertyNoteRepository? _propertyNoteRepository;
private Guid _noteContextId = Guid.Empty;
```

2. 생성자 끝에서:
```csharp
_propertyNoteRepository = App.ServiceProvider?
    .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;
```

3. `InitializeAsync(Guid programId, ...)` 시작 부분에서: `_noteContextId = programId;`

4. ObservableProperty + ToggleNotePanel + SaveNoteCommand + LoadNoteAsync + SaveNoteAsync 추가.
   tabName: `"interim"`, propertyId: `_noteContextId`

5. `InitializeAsync` 끝에서 `await LoadNoteAsync();` 추가.

**D. CashFlowSummaryViewModel.cs:**

프로그램 레벨, 물건 ID 없음. 이 ViewModel은 차주 목록을 로드하므로 borrowerRepository를 사용. 물건 컨텍스트가 전혀 없다.
접근: `Guid.Empty`를 사용하되, 고유성을 위해 탭 이름만으로 구분하면 모든 사용자의 비고가 섞인다.

더 실용적 접근: AuthService처럼 세션에서 userId를 가져오거나, 아예 프로그램 레벨 공용 비고로 사용. CashFlowSummaryViewModel에는 `_authService`가 없지만 `App.ServiceProvider`로 접근 가능.

최종 결정: `App.ServiceProvider`로 AuthService를 가져와서 userId를 `_noteOwnerId`로 사용.

```csharp
private Guid _noteOwnerId = Guid.Empty;

// 생성자에서:
try
{
    var authService = App.ServiceProvider?.GetService(typeof(AuthService)) as Data.Services.AuthService;
    var session = authService?.GetSession();
    if (session?.User?.Id != null)
        _noteOwnerId = Guid.Parse(session.User.Id);
}
catch { /* 세션 없으면 비고 비활성 */ }
```

tabName: `"cashflow_summary"`, propertyId: `_noteOwnerId`

`InitializeAsync()` 끝에서 `await LoadNoteAsync();` 추가.

**E. XnpvComparisonViewModel.cs:**

CashFlowSummaryViewModel과 동일한 접근. userId를 `_noteOwnerId`로.

tabName: `"npv_comparison"`, propertyId: `_noteOwnerId`

`InitializeAsync()` 끝에서 `await LoadNoteAsync();` 추가.

**F. PropertyDetailViewModel.cs (마감 탭용):**

ClosingTab은 `d:DataContext=PropertyDetailViewModel`이지만 실제 DashboardView에서는 `closingTab.DataContext = new { Property = property }` (익명 객체!)로 설정된다. 따라서 PropertyDetailViewModel의 바인딩이 동작하지 않을 가능성이 높다.

두 가지 시나리오:
1. ClosingTab이 NonCoreView에서도 사용될 수 있음 → 그때는 PropertyDetailViewModel이 DataContext
2. DashboardView에서만 사용됨 → 익명 객체가 DataContext

DashboardView 코드 확인 결과, ClosingTab은 DashboardView의 상위 탭 "closing" 케이스에서만 로드되며, `DataContext = new { Property = property }`로 설정됨.

**접근 변경**: ClosingTab에서 비고 기능을 구현하려면 DashboardView의 ClosingTab 로드 코드를 수정하여 적절한 ViewModel을 설정해야 한다. 하지만 이는 DashboardView 수정이 필요하므로 복잡도가 증가.

**실용적 접근**: ClosingTab의 code-behind에서 직접 PropertyNoteRepository를 사용하여 비고를 관리한다. View의 code-behind에 로직을 넣는 것은 MVVM 패턴에서 이상적이지 않지만, 익명 DataContext 상황에서 가장 간단한 해법이다.

PropertyDetailViewModel에는 이미 `_propertyNoteRepository`가 있으므로 (Phase 2에서 추가), Closing 전용 비고 속성을 추가한다.
이름 충돌 방지를 위해 `Closing` 접두사 사용 (Collateral 접두사와 동일 패턴):

```csharp
[ObservableProperty]
private string _closingNoteText = string.Empty;

[ObservableProperty]
private bool _isClosingNotePanelVisible;

[RelayCommand]
private void ToggleClosingNotePanel()
{
    IsClosingNotePanelVisible = !IsClosingNotePanelVisible;
}

[RelayCommand]
private async Task SaveClosingNote()
{
    await SaveClosingNoteAsync();
}

private async Task LoadClosingNoteAsync()
{
    if (_propertyNoteRepository == null || _propertyId == null) return;
    try
    {
        var note = await _propertyNoteRepository.GetByPropertyAndTabAsync(_propertyId.Value, "closing");
        ClosingNoteText = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[Closing] 비고 로드 실패: {ex.Message}");
    }
}

private async Task SaveClosingNoteAsync()
{
    if (_propertyNoteRepository == null || _propertyId == null) return;
    try
    {
        await _propertyNoteRepository.UpsertAsync(new NPLogic.Core.Models.PropertyNote
        {
            PropertyId = _propertyId.Value,
            TabName = "closing",
            NoteText = ClosingNoteText ?? string.Empty
        });
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[Closing] 비고 저장 실패: {ex.Message}");
    }
}
```

`InitializeAsync()` 끝에서 `await LoadClosingNoteAsync();` 추가.

**하지만** DashboardView에서 ClosingTab의 DataContext를 익명 객체로 설정하므로, PropertyDetailViewModel의 바인딩이 ClosingTab에서 작동하지 않는다. 이 문제를 해결하려면 DashboardView.xaml.cs를 수정해야 한다.

**DashboardView.xaml.cs 수정** (추가 파일):
`case "closing":` 부분을 다음과 같이 변경:
```csharp
case "closing":
    var closingTab = serviceProvider.GetService<ClosingTab>();
    if (closingTab != null)
    {
        // PropertyDetailViewModel을 사용하여 비고 기능 지원
        var closingVm = serviceProvider.GetService<PropertyDetailViewModel>();
        if (closingVm != null && property != null)
        {
            closingVm.SetPropertyId(property.Id);
            await closingVm.LoadClosingNoteAsync();
        }
        closingTab.DataContext = closingVm;
        view = closingTab;
    }
    break;
```

주의: 이렇게 하면 ClosingTab의 기존 바인딩(`{Binding IsClosingComplete}`, `{Binding Property.xxx}` 등)이 PropertyDetailViewModel에서 해결되어야 한다. PropertyDetailViewModel에 이미 `Property` 프로퍼티와 `IsClosingComplete` 등이 있는지 확인 필요. 없으면 ClosingTab의 기존 바인딩이 깨질 수 있다.

**안전한 대안**: ClosingTab의 code-behind에서 직접 비고를 관리한다. DataContext는 익명 객체로 유지하되, 비고 기능만 code-behind에서 처리:

ClosingTab.xaml.cs에 추가:
```csharp
private PropertyNoteRepository? _noteRepository;
private Guid? _propertyId;

private void ClosingTab_Loaded(object sender, RoutedEventArgs e)
{
    _noteRepository = App.ServiceProvider?
        .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;

    // DataContext에서 Property 추출 (익명 객체 또는 PropertyDetailViewModel)
    if (DataContext != null)
    {
        var propertyInfo = DataContext.GetType().GetProperty("Property");
        if (propertyInfo?.GetValue(DataContext) is Property property)
        {
            _propertyId = property.Id;
            _ = LoadNoteAsync();
        }
    }
}

private async Task LoadNoteAsync()
{
    if (_noteRepository == null || _propertyId == null) return;
    try
    {
        var note = await _noteRepository.GetByPropertyAndTabAsync(_propertyId.Value, "closing");
        NoteTextBox.Text = note?.NoteText ?? string.Empty;
    }
    catch { }
}

private async void NoteTextBox_LostFocus(object sender, RoutedEventArgs e)
{
    if (_noteRepository == null || _propertyId == null) return;
    try
    {
        await _noteRepository.UpsertAsync(new PropertyNote
        {
            PropertyId = _propertyId.Value,
            TabName = "closing",
            NoteText = NoteTextBox.Text ?? string.Empty
        });
    }
    catch { }
}

// 토글
private bool _isNotePanelVisible;
private void ToggleNotePanel_Click(object sender, RoutedEventArgs e)
{
    _isNotePanelVisible = !_isNotePanelVisible;
    NotePanelColumn.Visibility = _isNotePanelVisible ? Visibility.Visible : Visibility.Collapsed;
}
```

**최종 결정**: ClosingTab은 code-behind 방식을 사용한다. 다른 5개 탭은 ViewModel 패턴을 사용한다. 이유: ClosingTab만 익명 DataContext를 사용하는 특수 케이스이고, DashboardView 수정은 기존 ClosingTab 바인딩을 깨뜨릴 위험이 있다.

따라서 PropertyDetailViewModel.cs에는 Closing 관련 비고 코드를 추가하지 않는다. ClosingTab.xaml.cs에서 직접 처리한다.

**주의사항:**
- `using NPLogic.Data.Repositories;`와 `using NPLogic.Core.Models;`를 ViewModel/code-behind에 추가
- SaveNoteCommand는 `[RelayCommand] private async Task SaveNote()` 형태로 — CommunityToolkit.Mvvm이 자동으로 `SaveNoteCommand` 생성
- LostFocus 이벤트는 TextBox에 `LostFocus="NoteTextBox_LostFocus"` 추가하고 code-behind에서 ViewModel의 SaveNoteCommand.Execute(null) 호출
- ClosingTab은 특수 케이스: code-behind에서 직접 Repository 사용
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic &amp;&amp; rtk dotnet build NPLogic.sln --no-restore</automated>
  </verify>
  <done>
6개 ViewModel/code-behind에 비고 로직 추가, 빌드 성공. LostFocus 자동 저장 패턴 구현 완료.
  </done>
</task>

<task type="auto">
  <name>Task 2: 6개 View에 사이드 패널 XAML + LostFocus 자동 저장 추가</name>
  <files>
    src/NPLogic.App/Views/Loan/LoanSheetView.xaml
    src/NPLogic.App/Views/Loan/LoanSheetView.xaml.cs
    src/NPLogic.App/Views/AuctionPublicSaleView.xaml
    src/NPLogic.App/Views/AuctionPublicSaleView.xaml.cs
    src/NPLogic.App/Views/InterimTab.xaml
    src/NPLogic.App/Views/InterimTab.xaml.cs
    src/NPLogic.App/Views/CashFlowSummaryView.xaml
    src/NPLogic.App/Views/CashFlowSummaryView.xaml.cs
    src/NPLogic.App/Views/XnpvComparisonView.xaml
    src/NPLogic.App/Views/XnpvComparisonView.xaml.cs
    src/NPLogic.App/Views/ClosingTab.xaml
    src/NPLogic.App/Views/ClosingTab.xaml.cs
  </files>
  <action>
6개 View에 사이드 패널 XAML을 추가하고, LostFocus 자동 저장 이벤트를 연결한다.

**공통 사이드 패널 XAML** (5개 ViewModel-기반 탭에서 사용):
```xml
<Border x:Name="NotePanelColumn" Grid.Column="1"
        Width="250"
        Visibility="{Binding IsNotePanelVisible, Converter={StaticResource BoolToVisibilityConverter}}"
        Background="{StaticResource SurfaceColor}"
        BorderBrush="{StaticResource BorderBrush}"
        BorderThickness="1,0,0,0"
        Padding="12">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="&#x1F4DD; 비고" FontWeight="SemiBold"
                   FontSize="{StaticResource FontSizeBody}" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,8"/>
        <TextBox x:Name="NoteTextBox"
                 Text="{Binding NoteText, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 VerticalAlignment="Stretch" FontSize="{StaticResource FontSizeBody}" Padding="8"
                 BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="White"
                 LostFocus="NoteTextBox_LostFocus"/>
    </DockPanel>
</Border>
```

**공통 LostFocus code-behind** (5개 ViewModel-기반 탭):
```csharp
private void NoteTextBox_LostFocus(object sender, RoutedEventArgs e)
{
    if (DataContext is SomeViewModel vm)
    {
        vm.SaveNoteCommand.Execute(null);
    }
}
```

**공통 토글 버튼** (적절한 위치에 배치):
```xml
<Button Command="{Binding ToggleNotePanelCommand}"
        Style="{StaticResource MaterialDesignIconButton}"
        ToolTip="비고 패널 열기/닫기"
        Width="32" Height="32" Padding="0" Margin="8,0,0,0">
    <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
</Button>
```

**A. LoanSheetView.xaml + .xaml.cs:**

XAML: 루트 `<Grid Background="{StaticResource BackgroundColor}">` (line 258)를 2열 Grid로 변경:
```xml
<Grid Background="{StaticResource BackgroundColor}">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <!-- 기존 콘텐츠를 Grid로 감싸서 Grid.Column="0" -->
    <Grid Grid.Column="0">
        <Grid.RowDefinitions>
            ... (기존 3 Row 유지)
        </Grid.RowDefinitions>
        ... (기존 내용)
    </Grid>

    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    (위 공통 패턴, LostFocus 포함)
</Grid>
```

토글 버튼: 기준일/시나리오 헤더 영역 (line 266)의 Grid.Column="6" (Auto, 맨 오른쪽) 영역에 추가. 기존 버튼들 옆에 배치.

code-behind LostFocus:
```csharp
private void NoteTextBox_LostFocus(object sender, RoutedEventArgs e)
{
    if (DataContext is LoanSheetViewModel vm)
    {
        vm.SaveNoteCommand.Execute(null);
    }
}
```

materialDesign xmlns 확인 후 추가.

**B. AuctionPublicSaleView.xaml + .xaml.cs:**

AuctionPublicSaleView는 AuctionScheduleDetailViewModel과 PublicSaleScheduleViewModel 두 개의 ViewModel을 사용하는 복합 뷰이다. DataContext가 직접 설정되지 않고 `SetViewModels(auctionVm, publicSaleVm)`로 연결됨.

비고는 AuctionScheduleDetailViewModel의 NoteText에 바인딩한다. AuctionPublicSaleView에서 비고 패널의 DataContext를 명시적으로 AuctionScheduleDetailViewModel에 연결해야 한다.

접근: AuctionPublicSaleView.xaml.cs에서 `_auctionVm` 필드에 접근 가능하므로, 비고 패널의 DataContext를 auctionVm으로 설정.

XAML: 루트 콘텐츠를 2열 Grid로 감싸고, 사이드 패널의 DataContext를 code-behind에서 설정.

AuctionPublicSaleView.xaml.cs에 LostFocus + 토글 버튼 초기화 추가:
```csharp
private void NoteTextBox_LostFocus(object sender, RoutedEventArgs e)
{
    if (_auctionVm != null)
    {
        _auctionVm.SaveNoteCommand.Execute(null);
    }
}
```

NotePanelColumn의 DataContext를 SetViewModels에서 설정:
```csharp
// SetViewModels 안에서:
NotePanelColumn.DataContext = auctionVm;
```

materialDesign xmlns 확인 후 추가.

**C. InterimTab.xaml + .xaml.cs:**

XAML: 루트 `<Grid>` (line 22)를 2열 Grid로 변경:
```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <!-- 기존 콘텐츠 (Grid.Column="0"으로 감싸기) -->
    <Grid Grid.Column="0">
        ... (기존 로딩 오버레이 + 메인 콘텐츠)
    </Grid>

    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    (위 공통 패턴, LostFocus 포함)
</Grid>
```

토글 버튼: InterimTab 상단에 적절한 헤더/액션 영역을 찾아서 배치.

code-behind:
```csharp
private void NoteTextBox_LostFocus(object sender, RoutedEventArgs e)
{
    if (DataContext is InterimTabViewModel vm)
    {
        vm.SaveNoteCommand.Execute(null);
    }
}
```

**D. CashFlowSummaryView.xaml + .xaml.cs:**

XAML: 루트 `<Grid>` (line 14)를 2열로 변경:
```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <Grid Grid.Column="0">
        <Grid.RowDefinitions>
            ... (기존 3 Row 유지)
        </Grid.RowDefinitions>
        ... (기존 내용)
    </Grid>

    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    (위 공통 패턴, LostFocus 포함)
</Grid>
```

토글 버튼: 헤더 영역 (Grid.Row="0" 내부)에 추가.

code-behind:
```csharp
private void NoteTextBox_LostFocus(object sender, RoutedEventArgs e)
{
    if (DataContext is CashFlowSummaryViewModel vm)
    {
        vm.SaveNoteCommand.Execute(null);
    }
}
```

**E. XnpvComparisonView.xaml + .xaml.cs:**

XAML: 루트 `<Grid Margin="12,8,12,12">` (line 13)를 2열로 변경:
```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <Grid Grid.Column="0" Margin="12,8,12,12">
        <Grid.RowDefinitions>
            ... (기존 3 Row 유지)
        </Grid.RowDefinitions>
        ... (기존 내용)
    </Grid>

    <!-- 비고 사이드 패널 (Grid.Column="1") -->
    (위 공통 패턴, LostFocus 포함)
</Grid>
```

토글 버튼: 헤더 Grid (Grid.Row="0")의 오른쪽 Auto Column에 추가.

code-behind:
```csharp
private void NoteTextBox_LostFocus(object sender, RoutedEventArgs e)
{
    if (DataContext is XnpvComparisonViewModel vm)
    {
        vm.SaveNoteCommand.Execute(null);
    }
}
```

**F. ClosingTab.xaml + .xaml.cs (특수 케이스 — code-behind 방식):**

ClosingTab은 익명 DataContext(`new { Property = property }`)를 사용하므로 ViewModel 바인딩 대신 code-behind에서 직접 관리한다.

XAML: `<ScrollViewer>` (line 12)를 2열 Grid로 감싸기:
```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        ... (기존 내용 그대로)
    </ScrollViewer>

    <!-- 비고 사이드 패널 (code-behind에서 직접 관리) -->
    <Border x:Name="NotePanelColumn" Grid.Column="1"
            Width="250" Visibility="Collapsed"
            Background="{StaticResource SurfaceColor}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1,0,0,0" Padding="12">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="&#x1F4DD; 비고" FontWeight="SemiBold"
                       FontSize="{StaticResource FontSizeBody}" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,8"/>
            <TextBox x:Name="NoteTextBox"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     VerticalAlignment="Stretch" FontSize="{StaticResource FontSizeBody}" Padding="8"
                     BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="White"
                     LostFocus="NoteTextBox_LostFocus"/>
        </DockPanel>
    </Border>
</Grid>
```

주의: Visibility="Collapsed" (Binding이 아닌 하드코드 — code-behind에서 토글)

토글 버튼: ClosingTab 내부의 헤더 영역(마감 상태 카드, Grid.Row="0")에 추가. Button의 Click 이벤트 사용:
```xml
<Button Click="ToggleNotePanel_Click"
        Style="{StaticResource MaterialDesignIconButton}"
        ToolTip="비고 패널 열기/닫기"
        Width="32" Height="32" Padding="0" Margin="8,0,0,0">
    <materialDesign:PackIcon Kind="NoteEdit" Width="18" Height="18"/>
</Button>
```

materialDesign xmlns을 ClosingTab.xaml에 추가.

code-behind (ClosingTab.xaml.cs) — 전체 비고 관리 로직:
```csharp
using NPLogic.Core.Models;
using NPLogic.Data.Repositories;

// 필드 추가
private PropertyNoteRepository? _noteRepository;
private Guid? _notePropertyId;

// Loaded 이벤트에서 초기화 (기존 Loaded가 없으면 XAML에 Loaded="ClosingTab_Loaded" 추가)
private async void ClosingTab_Loaded(object sender, RoutedEventArgs e)
{
    _noteRepository = App.ServiceProvider?
        .GetService(typeof(PropertyNoteRepository)) as PropertyNoteRepository;

    // 익명 DataContext에서 Property 추출
    if (DataContext != null)
    {
        var propertyInfo = DataContext.GetType().GetProperty("Property");
        if (propertyInfo?.GetValue(DataContext) is Property property)
        {
            _notePropertyId = property.Id;
            await LoadNoteAsync();
        }
    }
}

private async Task LoadNoteAsync()
{
    if (_noteRepository == null || _notePropertyId == null) return;
    try
    {
        var note = await _noteRepository.GetByPropertyAndTabAsync(_notePropertyId.Value, "closing");
        NoteTextBox.Text = note?.NoteText ?? string.Empty;
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[Closing] 비고 로드 실패: {ex.Message}");
    }
}

private async void NoteTextBox_LostFocus(object sender, RoutedEventArgs e)
{
    if (_noteRepository == null || _notePropertyId == null) return;
    try
    {
        await _noteRepository.UpsertAsync(new PropertyNote
        {
            PropertyId = _notePropertyId.Value,
            TabName = "closing",
            NoteText = NoteTextBox.Text ?? string.Empty
        });
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[Closing] 비고 저장 실패: {ex.Message}");
    }
}

private void ToggleNotePanel_Click(object sender, RoutedEventArgs e)
{
    NotePanelColumn.Visibility = NotePanelColumn.Visibility == Visibility.Visible
        ? Visibility.Collapsed
        : Visibility.Visible;
}
```

**주의사항:**
- LostFocus 이벤트에서 async void 사용은 이벤트 핸들러에서만 허용 (fire-and-forget)
- ClosingTab의 code-behind 방식은 MVVM에서 예외적이지만, 익명 DataContext 제약 때문에 불가피
- AuctionPublicSaleView의 비고 패널은 AuctionScheduleDetailViewModel에 바인딩되므로, NotePanelColumn의 DataContext를 code-behind에서 명시적으로 설정해야 함
- 각 View에 materialDesign xmlns 확인 필수
  </action>
  <verify>
    <automated>cd C:/gits/Dev-NPLogic &amp;&amp; rtk dotnet build NPLogic.sln --no-restore</automated>
  </verify>
  <done>
6개 View에 사이드 패널과 LostFocus 자동 저장이 추가되어 빌드 성공. 토글 버튼으로 패널 열고 닫기, TextBox 포커스 잃을 때 자동 저장 동작.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 전체 10개 탭 비고란 통합 검증</name>
  <what-built>Phase 2에서 완성한 3개 탭(담보물건/선순위/평가) + Phase 3에서 추가한 10개 탭 — 총 13개 탭 모두에 비고 사이드 패널 완성</what-built>
  <how-to-verify>
    1. 앱 실행: `dotnet run --project src/NPLogic.App/NPLogic.App.csproj`
    2. 물건 선택 -> 비핵심 탭 진입

    **Group A — 저장 버튼 있는 탭 (기존 저장에 통합):**
    3. 차주개요 탭: 메모 아이콘 확인 -> 클릭 -> 사이드 패널 열림 -> 텍스트 입력 -> "저장" 클릭 -> 다른 물건 전환 후 복귀 -> 비고 복원 확인
    4. 권리분석 탭: 동일 검증 (토글, 입력, 저장, 복원)
    5. 기초데이터 탭: 동일 검증 (프로그램 레벨 비고)
    6. QA집계 탭: 동일 검증 (사용자 레벨 비고)

    **Group B — 저장 버튼 없는 탭 (LostFocus 자동 저장):**
    7. Loan 탭: 메모 아이콘 확인 -> 클릭 -> 사이드 패널 열림 -> 텍스트 입력 -> 패널 밖 클릭 (포커스 이동) -> 다른 물건 전환 후 복귀 -> 비고 복원 확인
    8. 경공매일정 탭: 동일 검증
    9. 인터림 탭: 동일 검증 (프로그램 레벨 비고)
    10. 현금흐름집계 탭: 동일 검증 (사용자 레벨 비고)
    11. NPV비교 탭: 동일 검증 (사용자 레벨 비고)
    12. 마감 탭: 동일 검증

    **통합 검증:**
    13. 여러 탭에서 각각 다른 비고 입력 -> 탭 전환하며 각 비고가 독립적으로 유지되는지 확인
    14. 비고 패널 닫기 -> 메인 콘텐츠 레이아웃 정상인지 확인

    **Phase 2 회귀 검증:**
    15. 담보물건/선순위/평가 탭의 기존 비고 기능이 정상 동작하는지 확인
  </how-to-verify>
  <resume-signal>"approved" 또는 수정 사항 설명</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build NPLogic.sln` 성공
- 6개 ViewModel/code-behind에 NoteText/IsNotePanelVisible 속성 존재
- 6개 View에 NotePanelColumn Border, 토글 버튼, LostFocus 이벤트 존재
- LostFocus 이벤트에서 SaveNoteCommand 호출 (ClosingTab은 직접 Repository 호출)
- 기존 Load 흐름에 LoadNoteAsync 호출 통합
</verification>

<success_criteria>
1. 6개 탭(Loan/경공매/인터림/현금흐름/NPV비교/마감)에서 토글 버튼 클릭 시 250px 사이드 패널 표시/숨김
2. TextBox에 여러 줄 자유 입력 가능
3. TextBox에서 포커스를 잃으면 비고가 자동으로 DB에 저장됨
4. 탭 재진입 시 저장된 비고가 자동 로드됨
5. 전체 10개 탭(+ Phase 2의 3개)에서 비고가 탭별로 독립적으로 동작
6. Phase 2 기존 3개 탭 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/03-all-tabs-notes/03-02-SUMMARY.md`
</output>
